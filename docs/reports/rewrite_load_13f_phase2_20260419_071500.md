# REWRITE load_13f.py — Phase 2 staging validation

Run ID: 20260419_071500
Branch: `rewrite-load-13f`
Rebase base: `main` (22ed3fd, post-Precheck2 merge)

## Phase 1 commits

| Hash | Subject |
|---|---|
| 46f0ca8 | docs(rewrite): load_13f — phase 0 broad audit findings |
| 0a7ae35 | docs(rewrite): load_13f phase 0 addendum — other_managers ghost data investigation |
| 05427c7 | refactor(load_13f): retire dead holdings DROP+CTAS |
| 8e7d5cb | feat(load_13f): CHECKPOINT / data_freshness / --dry-run / fail-fast / flush |
| 14a5152 | feat(load_13f): add OTHERMANAGER2 loader, materialize other_managers |

All three implementation commits passed ruff / pylint / bandit pre-commit.

## Pre-flight

Staging DB seeded by attaching prod read-only and
`CREATE TABLE AS SELECT * FROM prod.<t>` for the six load_13f-owned
tables plus `data_freshness`. Post-seed counts match prod (see "Before
Phase 2 run" column below).

## Dry-run (`--staging --dry-run`)

No DB mutations. TSV pre-flight + per-quarter `read_csv_auto` row
projection against an in-memory scratch DB:

```
2025Q1: 10,765 submissions, 3,440,002 infotable, 10,765 coverpage, 3,811 othermgr2
2025Q2: 10,799 submissions, 3,360,306 infotable, 10,799 coverpage, 3,910 othermgr2
2025Q3: 10,422 submissions, 3,267,091 infotable, 10,422 coverpage, 3,759 othermgr2
2025Q4: 11,372 submissions, 3,473,209 infotable, 11,372 coverpage, 3,925 othermgr2
Totals: 43,358 submissions, 13,540,608 infotable, 43,358 coverpage, 15,405 other_managers
```

Matches Phase 0 findings baseline exactly.

## Real staging run

`python3 -u scripts/load_13f.py --staging`
Wall: **10.8s** total (27.7s user, 8.9s sys, 338% CPU).
Mode: `staging=True  dry_run=False`. Full four-quarter reload.

Exit: 0. No warnings. `Done.` printed.

### Per-quarter progress

| Quarter | Submissions | Infotable | Coverpage | OtherMgr2 |
|---|---:|---:|---:|---:|
| 2025Q1 | 10,765 | 3,440,002 | 10,765 | 3,811 |
| 2025Q2 | 10,799 | 3,360,306 | 10,799 | 3,910 |
| 2025Q3 | 10,422 | 3,267,091 | 10,422 | 3,759 |
| 2025Q4 | 11,372 | 3,473,209 | 11,372 | 3,925 |

### CHECKPOINT cadence

Five CHECKPOINT calls executed during the run: one after each
quarter (×4) and one after `build_filings`, plus one more before
`con.close()`. Total ≥6 in full mode. Verified by inspecting the
script flow — no equivalent to prior runs that committed only at
end-of-process.

## Validation

### 1. Six-table row counts (staging vs prod, post-run)

| Table | Staging | Prod | Delta | Status |
|---|---:|---:|---:|---|
| `raw_submissions` | 43,358 | 43,358 | +0 | **PASS** |
| `raw_infotable` | 13,540,608 | 13,540,608 | +0 | **PASS** |
| `raw_coverpage` | 43,358 | 43,358 | +0 | **PASS** |
| `filings` | 43,358 | 43,358 | +0 | **PASS** |
| `filings_deduped` | 40,140 | 40,140 | +0 | **PASS** |
| `other_managers` | 15,405 | 15,405 | +0 | **PASS** |

### 2. `other_managers` schema parity

Column order and types byte-identical:

```
accession_number VARCHAR, sequence_number VARCHAR, other_cik VARCHAR,
form13f_file_number VARCHAR, crd_number VARCHAR, sec_file_number VARCHAR,
name VARCHAR, quarter VARCHAR
```

**PASS**.

### 3. Row-content parity — authoritative EXCEPT on every table

ATTACH prod read-only; `SELECT * FROM main.<t> EXCEPT SELECT * FROM prod.<t>`
and the reverse:

| Table | staging-only | prod-only | Status |
|---|---:|---:|---|
| `raw_submissions` | 0 | 0 | **PASS** |
| `raw_coverpage` | 0 | 0 | **PASS** |
| `raw_infotable` | 0 | 0 | **PASS** (1.7s EXCEPT over 13.5M rows — DuckDB makes short work of it) |
| `filings` | 0 | 0 | **PASS** |
| `filings_deduped` | 0 | 0 | **PASS** |
| `other_managers` | 0 | 0 | **PASS** |

Cross-check: `SUM(value)`, `SUM(shares)`, `COUNT(DISTINCT cusip)` on
`raw_infotable` per quarter — all four quarters identical to prod
(e.g. 2025Q4: sum_value=71,071,665,530,490; sum_shares=991,862,466,228;
cusips=33,780). **PASS.**

Note on earlier false-positive: an initial MD5-over-`STRING_AGG`
hash flagged a mismatch on `raw_infotable`. That was an artifact of
`STRING_AGG` being non-deterministic over large unsorted aggregates
— replacing it with the authoritative `EXCEPT` comparison (above)
shows full content parity. Real-parity signal is EXCEPT, not the
aggregate hash.

### 4. `data_freshness` entries post-run

All six load_13f-owned tables stamped, within ~360ms of each other:

| Table | last_computed_at | row_count |
|---|---|---:|
| `raw_submissions` | 2026-04-19 07:14:48.847063 | 43,358 |
| `raw_infotable` | 2026-04-19 07:14:48.853239 | 13,540,608 |
| `raw_coverpage` | 2026-04-19 07:14:48.854072 | 43,358 |
| `other_managers` | 2026-04-19 07:14:48.854794 | 15,405 |
| `filings` | 2026-04-19 07:14:49.189448 | 43,358 |
| `filings_deduped` | 2026-04-19 07:14:49.190420 | 40,140 |

**PASS** — all six updated.

### 5. Downstream reader sanity

Emulating the live read paths identified in Phase 0 §3 against staging:

| Reader path | Staging | Prod | Status |
|---|---:|---:|---|
| `api_register.py:252` — `filings WHERE amended = true` accession count | 1,367 | 1,367 | **PASS** |
| `api_register.py:258` — `DISTINCT cik FROM filings` | 11,135 | 11,135 | **PASS** |
| `build_managers.py` / `fetch_13dg_v2.py:113` — `DISTINCT cik FROM filings_deduped` | 11,135 | 11,135 | **PASS** |
| `filings_deduped` 2025Q4 row count (latest quarter) | 10,535 | 10,535 | **PASS** |

No NULL regressions detected.

### 6. Holdings DROP+CTAS retirement

`SELECT COUNT(*) FROM main.holdings` in staging → Catalog Error: table
absent. **PASS**. The script no longer creates the table on run.
Grep of the run log for `FROM holdings\b` (no `_v2` suffix): zero
hits originated by load_13f.py.

## Anomalies

None material. One process note:

- `STRING_AGG`-based hashing was misleading on large tables — the
  authoritative check is `EXCEPT`, which is cheap in DuckDB (1.7s
  over 13.5M rows). Keep that in mind for future block validations.

## Gates summary

| Gate | Result |
|---|---|
| Six-table row counts | PASS |
| `other_managers` schema parity | PASS |
| `other_managers` row content parity (EXCEPT both directions) | PASS |
| `data_freshness` updated for all six | PASS |
| Downstream reader sanity (4 paths) | PASS |
| Zero legacy `holdings` writes | PASS |
| Dry-run behaves as spec'd (no DB mutations, fail-fast on missing TSV) | PASS |

## Phase 3 readiness

All gates PASS. Ready for Phase 3 review and Phase 4 prod sign-off.

### Phase 4 plan (pending sign-off)

1. Snapshot: `EXPORT DATABASE 'data/backups/13f_backup_pre_rewrite_load_13f_20260419'`
2. Execute: `python3 scripts/load_13f.py` (no `--staging`)
3. Validate against prod with the same gate suite as Phase 2
4. Commit: `chore(rewrite): load_13f phase 4 prod apply — <rows per table>`
5. Push branch; open PR; do not merge (Serge reviews commit history)

On any prod-validation failure: immediate `IMPORT DATABASE` restore;
no manual patching.

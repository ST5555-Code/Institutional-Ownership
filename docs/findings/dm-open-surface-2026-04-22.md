# DM Worldview Open Surface — 2026-04-22

_Generated by `dm-scoping` session. Read-only audit of `decision_maker_v1` worldview state against prod (`data/13f.duckdb`, via `data/13f_readonly.duckdb` snapshot). HEAD at audit: `c856562`._

The headline: several items that the comprehensive work plan v3 and MAINTENANCE.md still frame as open are already closed in prod. In particular, all three "new curation items" in MAINTENANCE.md Pending Audit Work were closed on 2026-04-10 by DM12 (commit `ada58ac`). The residual DM open surface is smaller than the v3 brief implies.

---

## 1. ROADMAP claims validated

| DM item | Doc status | Verifiable claim | Prod state | Match |
|---|---|---|---|---|
| DM1 — N-CEN sub-adviser rollup | Done | 20,205 decision_maker_v1 rows (parity with EC) | 26,602 open DM rows, 26,602 open EC rows (parity holds; count has grown since DM1) | ✓ parity preserved |
| DM4 — UI toggle | Done | 12+ endpoints parameterized | Not re-verified in code (Phase 4 out-of-scope for DM4) | assumed ok |
| DM8 — intra-firm collapse | Done | 621 intra-firm cases; top 15 cross-firm residual | Not re-counted; no contradicting evidence found | assumed ok |
| DM9–DM11 — UI badges + context panel | Done | Columns / endpoints shipped 2026-04-10 | Not re-verified | assumed ok |
| DM12 — umbrella trust cleanup | Done | Securian + HC Capital 7 series + CRI 5 series | 9 `entity_overrides_persistent` rows (IDs 27–35) tagged `DM12 Securian sub-adviser routing 2026-04-10`; 14 HC Capital Trust series route to external sub-advisers via DM; 9 CRI funds route to Causeway/WCM/Mercer/Teachers/Lazard/Allspring/Principal | ✓ confirmed (see §3) |
| DM13 — ADV_SCHEDULE_A BlueCove sweep | Done (`ef3f302`) | 35 override rows written, $235.48B+ shifted | 35 rows in `entity_overrides_persistent` with `reason ILIKE '%BlueCove%' OR '%DM13%'` | ✓ exact match |
| DM14 — Layer 1 intra-firm collapses | Done (`d684e4e`) | 8 relationships, override IDs 83–90, $12.15B | 8 rows at override_id 83–90, all `rollup_type='decision_maker_v1'` | ✓ exact match |
| DM14b — graph-completion | Done (2026-04-17) | 7 candidates identified, no writes | Correctly no overrides allocated — marker entry only | ✓ |
| DM15 Layer 1 | Done (`7bb68f5`) | 15 sub-adviser DM retargets | Confirmed via commit presence | ✓ |
| DM15 Layer 2 | Done (`938e435`) | 17 retargets, override IDs 205–221 | 17 rows at override_id 205–221, all `rollup_type='decision_maker_v1'` | ✓ exact match |
| DM15b — N-CEN coverage expansion | Done (`9ce5b17`) | ncen_adviser_map 11,106→11,209 | `SELECT COUNT(*) FROM ncen_adviser_map` = **11,209** | ✓ exact match |
| DM15c — Amundi SA rename + 12 geo reroutes | Done (`9160030`) | 24 override rows IDs 222–245 | 24 rows at override_id 222–245 (12 `economic_control_v1` + 12 `decision_maker_v1`) | ✓ exact match |
| DM2 — ADV Schedule 7.B | Deferred | Blocked on D12 ADV Section 7.B re-parse | No evidence of closure | ✓ still deferred |
| DM3 — N-PORT metadata | Deferred | Blocked on D13 fetch_nport.py extension | No evidence of closure | ✓ still deferred |
| DM5 — voting_control_v1 | Closed (not-built) | Decision taken 2026-04-10 | No voting_control_v1 rows | ✓ |
| DM6 — N-1A prospectus parser | Low / deferred | Pipeline backlog, post-3.5b | No evidence of closure | ✓ still deferred |
| DM15 umbrella-trusts audit | Deferred | ~132 series, 10 umbrella trusts (Sterling/Gotham/NEOS/Segall Bryant/Mairs & Power/Brandes/Crawford/Bridges/Champlain/FPA), blocked on D13/DM6 | ncen_adviser_map coverage: Sterling Capital 19, NEOS 17, Segall Bryant 16, all others (Gotham/Mairs/Brandes/Crawford/Bridges/Champlain/FPA) = **0**. 7 of 10 trusts cannot be resolved via ncen alone. | ✓ still open, confirmed blocker |

**No mismatches between ROADMAP's Done-item claims and prod state.** Every numeric claim spot-checked (override ID ranges, BlueCove/DM12 reason counts, ncen_adviser_map row count) matches exactly.

---

## 2. Prod entity state

Read-only queries against `data/13f_readonly.duckdb` (`MAX(entity_id)` and file mtime confirm it mirrors `13f.duckdb` HEAD):

| Metric | Value | Doc claim |
|---|---|---|
| `entities` total | **26,602** | NEXT_SESSION_CONTEXT ~26,602 ✓ |
| `entity_rollup_history` open (`valid_to=9999-12-31`), `economic_control_v1` | **26,602** | — |
| `entity_rollup_history` open, `decision_maker_v1` | **26,602** | parity with EC (DM1 invariant) ✓ |
| `entity_overrides_persistent` total | **245** | MAINTENANCE "~245" ✓ |
| `entity_overrides_persistent` by rollup_type | decision_maker_v1 = **179**, economic_control_v1 = **65**, null = **1** | — |
| `MAX(override_id)` | **245** | — (no gaps under 245) |
| `ncen_adviser_map` total | **11,209** | DM15b target 11,209 ✓ |
| `MAX(relationship_id)` in `entity_relationships` | **20,812** | — |
| `entity_relationships WHERE relationship_id IN (12171, 12172)` | **0 rows** | DM12 Section 1a deleted them on 2026-04-10 — MAINTENANCE.md reference is stale ✓ |

No discrepancies. DM/EC rollup parity is holding. Override ledger is dense (no gaps under 245).

---

## 3. Three new curation items — scope confirmation

**Verdict: all three were closed on 2026-04-10 by DM12 (commit `ada58ac`, "Section 1 DM12 fixes promoted: Securian fix + HC Capital 7 series + CRI 5 series"). MAINTENANCE.md Pending Audit Work is stale on these three rows and should be moved to COMPLETED.**

### 3.1 Securian / Sterling — **CLOSED**

- `ada58ac` (2026-04-10) — Section 1a: deleted `entity_relationships` 12171 + 12172 (bad fuzzy-match rows from 2026-04-07), SCD-closed 22 rollup rows pointing to Sterling, SCD-opened 22 new rows routing Securian (eid 8592) + 11 SFT funds to Securian self-rollup. `holdings_v2` re-stamped (3,714 rows, $13.21B). Override rows written at IDs 27–35 (`reason = 'DM12 Securian sub-adviser routing 2026-04-10'`). Root cause was ADV parser fuzzy-matching "Securian Financial Group" → "Sterling Financial Group" (eid 5668).
- Later reinforced by `8f8d9f2` (INF9b: write 9 Securian DM12 override rows to staging + prod) which is the same 9 rows.
- **Prod state today:** relid 12171/12172 do not exist; eid 8592 (Securian Asset Management) exists; all 12 Sterling Capital fund entities self-roll to eid 4367 (Sterling Capital Management LLC), unrelated to Securian. Nothing left to do.

### 3.2 HC Capital Trust — **CLOSED**

- `ada58ac` Section 1b: of 14 HC Capital Trust series, 8 were already correctly routed by the `ncen_sub_adviser` pass. 7 remaining re-routed DM-only:
  - S000009376, S000009382, S000009383, S000029854 → Parametric (eid 11164)
  - S000009384 → RhumbLine (eid 6480)
  - S000029852 → Agincourt (eid 19021)
  - S000029853 → Mellon Investments (eid 3050)
- **Prod state today:** all 14 series currently roll to external sub-advisers via DM: Parametric (5), Mellon (4), City of London (2), Agincourt (1), RhumbLine (1), Wellington (1) — confirmed by `entity_identifiers` + `entity_rollup_history` join. None still point to HC Capital Solutions. Nothing left to do.
- Brief listed 5 sub-advisers (Parametric/Mellon/City of London/Wellington/RhumbLine/Agincourt); count is actually 6 — all present.

### 3.3 CRI / Christian Brothers — **CLOSED**

- `ada58ac` Section 1c: of 13 N-CEN-linked series, 5 re-routed DM-only (the brief said 4; Mercer's S000073781 was added per "fix all regardless of size"):
  - S000073781 → Mercer (6361)
  - S000073782 → Wellington (9935)
  - S000073783 → Loomis Sayles (17973)
  - S000073785 → Parametric (11164)
  - S000073786 → Teachers Advisors (9904)
- **Prod state today:** 9 `Catholic Responsible Investments …` fund entities route via DM to: Causeway (1), WCM (1), Mercer (3), Teachers (1), Lazard (1), Allspring (1), Principal (1). None route to Christian Brothers Investment Services (eid 20135). Nothing left to do.
- **Nomenclature note:** the N-CEN registrant is "Catholic Responsible Investments Funds" (CRI Funds). Christian Brothers Investment Services is the adviser entity (eid 20135), not the registrant. Brief wording "CRI / Christian Brothers" is correct but conflates two layers.
- **Carry-forward:** `ada58ac` surfaces INF4 — Loomis Sayles entity fragmentation (eid 17973 used for DM routing vs eid 7650 which holds the $246.9B 13F book). Not a DM bug; separate entity-consolidation work. Tier 2 owner: entity MDM, not DM.

---

## 4. Possibly-closed verification (D10, D11)

### 4.1 D10 — Admin UI for `entity_identifiers_staging` review — **OPEN** (adjacent feature shipped, not equivalent)

- A general pipeline staging-review UI exists: `scripts/admin_bp.py` lines 1302–1661 (endpoints `POST /api/admin/runs/{id}/approve`, `.../reject`, `GET /api/admin/runs/pending`, `GET /api/admin/runs/{id}/diff`) + React components in `web/react-app/src/admin/` (AdminApp.tsx, PendingCards.tsx, DiffModal.tsx). Shipped as p2-07 (commit `6df96cc`). This surface reviews `ingestion_manifest` rows gated by `fetch_status='pending_approval'`.
- It does **not** surface `entity_identifiers_staging`. ROADMAP line 584 still lists D10 as remaining under Phase 3.5 deferred, and line 170 records a ~280-row backlog for entity identifier staging review.
- **Disposition:** D10 remains an open item, but narrower than originally framed — the general staging UI is a partial precedent. A dedicated identifier-staging surface is still needed.

### 4.2 D11 — Auto-promoter for high-confidence staging rows — **PARTIAL** (infra shipped, logic not)

- `admin_preferences` table shipped via migration 016 (`scripts/migrations/016_admin_preferences.py:42–50`) with columns `user_id`, `pipeline_name`, `auto_approve_enabled BOOLEAN`, `auto_approve_conditions JSON`.
- All six pipeline loaders accept a `--auto-approve` CLI flag (e.g., `scripts/pipeline/load_13f_v2.py:824`, `load_13dg.py:824`, `load_adv.py:504`) — but this is blanket auto-approve, not confidence-threshold-gated.
- No code reads `admin_preferences.auto_approve_conditions` and applies conditional logic.
- **Disposition:** D11 is partially closed. Residual scope: implement the conditional reader that consumes `auto_approve_conditions` JSON (confidence thresholds, source whitelists, etc.) and wires it into the approve pipeline. Scope is 1–2 days if the schema is already correct.

---

## 5. Newly-surfaced DM items (flag only — no fix)

### 5.1 Umbrella-trusts audit has a hard floor at 7 of 10 trusts

`ncen_adviser_map` coverage for the 10 trusts named in DM15-umbrella-trusts-deferred:

| Trust | `ncen_adviser_map` rows | Path to resolution |
|---|---|---|
| Sterling Capital | 19 | N-CEN path viable (but already partly handled by DM12 Section 1a Securian-related rollups; residual SFT series may still need DM routing) |
| NEOS | 17 | N-CEN path viable |
| Segall Bryant | 16 | N-CEN path viable |
| Gotham | 0 | Needs DM6 (N-1A) or DM3 (N-PORT metadata) |
| Mairs & Power | 0 | Needs DM6/DM3 |
| Brandes | 0 | Needs DM6/DM3 |
| Crawford | 0 | Needs DM6/DM3 |
| Bridges | 0 | Needs DM6/DM3 |
| Champlain | 0 | Needs DM6/DM3 |
| FPA | 0 | Needs DM6/DM3 |

> **Correction (2026-04-27):** the 19 / 17 / 16 row counts above are `role='adviser'` rows in `ncen_adviser_map`, not `role='subadviser'` rows. None of Sterling Capital Funds, NEOS ETF Trust, or Segall Bryant & Hamill Trust have any `role='subadviser'` rows — they are single-adviser trusts where the namesake firm is both sponsor and sole adviser. The DM15b/L2 retarget pattern requires subadviser rows, so DM15d closed as a no-op. See ROADMAP COMPLETED 2026-04-27.

**Implication:** 3 of 10 umbrella trusts can be resolved by extending the DM15b/L2 pattern (add N-CEN rows, run retarget). 7 of 10 are blocked behind DM6 (N-1A prospectus parser) or DM3 (N-PORT metadata extension). The v3 brief frames DM15 umbrella-trusts as a single Tier 2 item — it should be split: **DM15d** (N-CEN-resolvable: Sterling/NEOS/Segall Bryant) now, and **DM15e** (prospectus-blocked: 7 remaining trusts) behind DM6/DM3.

### 5.2 77 entities with DM self-rollup + `classification='passive'`

Spot-checked sample: all are legitimate (BlackRock Fund Advisors, Charles Schwab, Geode, Parametric Portfolio, etc. — large passive index sponsors that correctly roll to themselves under both EC and DM). **Not an anomaly.** Flagged here for completeness because the Phase 5 brief asked for it; no follow-up required.

### 5.3 No dangling or hot-churn rows

- DM rows pointing at an entity with no open DM self-row: **0**
- Entities with ≥3 distinct `valid_from` dates on DM in last 30 days: **0**
- Entities with 3 distinct historical DM targets: small handful (eids 830, 3562, 8443, 10266, 20139, 4248) — all consistent with DM12+DM14+DM15 iterative reroutes. No pathological churn.

### 5.4 INF4 — Loomis Sayles entity fragmentation

Flagged by `ada58ac`: eid 17973 ("Loomis, Sayles & Company, L.P.") is the DM rollup target for 25+ sub-adviser routings, but eid 7650 ("LOOMIS SAYLES & CO L P") holds the $246.9B 13F book and 4,487 `holdings_v2` rows. Joining through `entity_id` fragments the book. **Owner: entity MDM, not DM.** Already logged as INF4 / DM13 consolidation candidate per the commit.

---

## 6. Confirmed open DM surface (authoritative list after this audit)

Supersedes the v3 brief's Tier 2 Group C framing. The DM-worldview open surface post-audit is:

| Item | Scope | Blocker / prerequisite | Approx size |
|---|---|---|---|
| **DM13 residual** | Remainder of the ~410-row ADV_SCHEDULE_A audit beyond the BlueCove cluster (`ef3f302` closed BlueCove only) | None — manual sweep | ~390 rows to triage |
| **DM14c — Voya residual** | $21.81B / 49 funds flagged in original DM14 scope but not collapsed in Layer 1 | DM14b edge-completion prerequisite (Voya edge missing) | 49 funds / $21.81B |
| **DM15d — N-CEN-resolvable umbrella trusts** (new sub-item) | Sterling Capital, NEOS, Segall Bryant — cover ~52 ncen rows | None — extend DM15b/Layer 2 pattern | ~3 trusts, ~$X AUM (TBD when audit runs) |
| **DM15e — Prospectus-blocked umbrella trusts** (new sub-item) | Gotham, Mairs & Power, Brandes, Crawford, Bridges, Champlain, FPA | DM6 (N-1A parser) or DM3 (N-PORT metadata) | 7 trusts, ~est. 90+ series, bulk of the original ~132-series $105B claim |
| **DM2 / DM3 / DM6** | ADV Schedule 7.B reparse / N-PORT metadata / N-1A parser | Pipeline work — each is a multi-day build | deferred, not in Tier 2 |
| **D10 residual** | Admin UI specifically for `entity_identifiers_staging` (~280-row backlog) | General admin UI (p2-07) shipped, needs narrower surface | ~280 rows to review |
| **D11 residual** | Confidence-threshold auto-approve reader | `admin_preferences` schema + `--auto-approve` flag already shipped | 1–2 days' work |

**Items closed by this audit (remove from any open tracker):**
- Securian / Sterling curation (closed by DM12 Section 1a, commit `ada58ac`)
- HC Capital Trust 5–6 sub-adviser curation (closed by DM12 Section 1b, commit `ada58ac`)
- CRI / Christian Brothers 4-series curation (closed by DM12 Section 1c, commit `ada58ac` — actually 5 series, not 4)

**MAINTENANCE.md action:** move the three "Pending Audit Work" entries (Securian/Sterling, HC Capital, CRI) to COMPLETED with date 2026-04-10 and commit `ada58ac`. Not done by this session per its read-only mandate.

---

## 7. Recommended session sequencing

Given what is actually open, Tier 2 Group C ordering:

1. **DM14c — Voya residual** first. 49 funds / $21.81B is the highest-AUM single cleanup left. Depends on DM14b edge-completion; pair the two into one session ("DM14b+c: Voya edge + collapse").
2. **DM15d — N-CEN-resolvable umbrella trusts** (Sterling Capital / NEOS / Segall Bryant) second. Pure extension of the DM15b+L2 pattern — low risk, one session.
3. **DM13 residual sweep** third. ~390 suspicious ADV_SCHEDULE_A rows. Can be parallelized into two batches of ~200 against two sessions if desired.
4. **D11 conditional auto-approve** fourth. Small infra build; no DM rollup exposure.
5. **D10 identifier staging UI** fifth. Frontend work; can parallel with D11 if a separate session is available.
6. **DM15e — prospectus-blocked trusts** and **DM2/DM3/DM6** are deferred until the underlying parsers land. Do not slot into Tier 2.

Parallelization-safe pairings (no shared DB state):
- (DM14b+c Voya) ∥ (DM15d umbrella trusts) — different entities, different override ID ranges.
- (D10 admin UI) ∥ (D11 conditional logic) — frontend vs backend; separate files.
- DM13 residual sweep should run sequentially, not in parallel with DM rollup retargets, because it writes to the same `entity_overrides_persistent` ledger.

Net effect vs v3 brief: one item (Voya) gets prioritized, one (umbrella trusts) gets split in half, three (Securian/HC/CRI) get struck as already closed, two (D10/D11) enter the DM-adjacent Tier 2 list at reduced scope.

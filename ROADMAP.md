# 13F Institutional Ownership Database — Roadmap

_Last updated: April 19, 2026 — **doc-sync session post-INF39 + pct-of-so.** HEAD: `f22312e` (main, post schema-diff merge). **INF34 CLEARED** (`62ad0eb` — `queries.py` `rollup_type` filter added to `summary_by_parent` / `investor_flows` EC-only read sites). **INF39 CLEARED** (`f22312e` — `scripts/pipeline/validate_schema_parity.py` + `config/schema_parity_accept.yaml` + `make schema-parity-check` pre-flight gate now live; see `docs/BLOCK_SCHEMA_DIFF_FINDINGS.md`). **pct-of-so shipped** (`8925347` merge + `12e172b` follow-on) — migration 008 renamed `holdings_v2.pct_of_float → pct_of_so`, added `pct_of_so_source VARCHAR` audit column, repointed every read site; three-tier ASOF denominator now runs in `enrich_holdings.py` Pass B (`soh_period_accurate` → `market_data_so_latest` → `market_data_float_latest`). **Hardening quartet-minus-one status:** INF39 closed; **INF40** (stable L3 surrogate row-ID for rollback replay), **INF41** (mechanically exhaustive rename sweep), **INF42** (derived-artifact / React-dist / CI-fixture hygiene) remain open as follow-on items to prevent the repeating class of silent failure surfaced by pct-of-so. **Validate baseline corrected to 8 PASS / 2 FAIL / 6 MANUAL** — pre-existing FAILs are `wellington_sub_advisory` + `phase3_resolution_rate` (miscounted as 1 FAIL in prior headers; reality is 2). **Terminology:** `pct_of_float` retired project-wide; `pct_of_so` is canonical going forward. See `docs/DEFERRED_FOLLOWUPS.md` for the full open-items index. Prior session header preserved below._

_Prior header: April 19, 2026 — post-Batch 3 REWRITE queue + BLOCK-MARKET-DATA-WRITER-AUDIT + CIFix/CIFix2. HEAD: `223b4d9`. **Batch 3 REWRITE queue COMPLETE, one more BLOCK merged, CI smoke stabilized.** **(1) BLOCK-SECTOR-COVERAGE-BACKGROUND** (merged `b5aa613`) — backfilled 12 sector-coverage rows; Yahoo-side yield ceiling documented for future refetches. **(2) Batch 3 REWRITE queue #1–#5 COMPLETE** — `build_shares_history.py` (`d7ba1c2`, Rewrite1; prod apply `443e37a`), `build_summaries.py` (`3234c8a`, Rewrite2 — work already shipped at `87ee955`, marked CLEARED this session), `compute_flows.py` (`34710d1`, Rewrite3 — also already shipped at `87ee955`, CLEARED this session), `load_13f.py` (`7e68cf9`, Rewrite4; prod apply `a58c107`; Phase 0 addendum at `0a7ae35` recovered **15,405 OTHERMANAGER2 rows** materialized from ghost data the legacy script silently dropped), `build_managers.py` + `backfill_manager_types.py` (`223b4d9`, Rewrite5; prod apply `7747af2`; repointed to `holdings_v2` with COALESCE preservation of the 14-category taxonomy; pre-rewrite audit snapshot `holdings_v2_manager_type_legacy_snapshot_20260419` created at `c2c2bac`). All five scripts now satisfy PROCESS_RULES + clear their `pipeline_violations.md` entries. **(3) BLOCK-MARKET-DATA-WRITER-AUDIT** (merged `bc43d25`) — cross-cutting audit of `market_data` writers; findings in `docs/BLOCK_MARKET_DATA_WRITER_AUDIT_FINDINGS.md`. Standing hazard remains at `scripts/refetch_missing_sectors.py:254` (writes sector without updating `fetch_date`) — filed as `INF31` follow-on. **(4) CIFix + CIFix2** (`22acbb3` + `d4f6276`) — smoke/lint CI stabilized; CI trigger-expansion shipped alongside. GitHub Actions `actions/checkout@v4` + `actions/setup-python@v5` + `actions/cache@v4` flagged as Node-24 migration candidates (June 2026 deprecation) — filed as `INF33`. **Batch 3 queue closed; legacy `holdings` retirement fully sealed.** Validate **8 PASS / 1 FAIL (wellington baseline) / 7 MANUAL** preserved across all five REWRITE Phase-4 prod applies. Prior session header preserved below._

_Prior header: April 18, 2026 — post-BLOCK closeouts. HEAD: `3299a9f`. **Three BLOCKs merged since session #11; doc-update workstream in flight.** **(1) BLOCK-SECURITIES-DATA-AUDIT** (merged `92b5bf0`) — three root causes identified and fixed (RC1/RC2/RC3). Prod `cusip_classifications` + `securities` promoted 132K → 430K. Audit findings captured in `docs/BLOCK_SECURITIES_DATA_AUDIT_FINDINGS.md`. **(2) BLOCK-3** (merged `0dc0d5d`) — legacy `fetch_nport.py` retired to `scripts/retired/`; `build_benchmark_weights.py` + `build_fund_classes.py` repointed to `fund_holdings_v2`. Closes the last readers of the legacy `holdings` / `fund_holdings` tables (Stage 5 cleanup preceded; see Apr 13 session). **(3) BLOCK-TICKER-BACKFILL** (merged `3299a9f`) — Pass C `is_priceable` gate added; forward-looking subprocess hooks appended to `build_cusip.py` and `normalize_securities.py` so future `securities` updates trigger a ticker re-stamp. `fund_holdings_v2.ticker` populated **3,935,959 → 5,154,223 (+1,218,264)** on prod apply via the ephemeral-helper refetch pattern (no Yahoo re-hit from prod path — pattern now documented in `MAINTENANCE.md § Refetch Pattern for Prod Apply`). Findings in `docs/BLOCK_TICKER_BACKFILL_FINDINGS.md`. **(4) Doc-update workstream** (this session) — architectural-debt note on denormalized v2 columns landed as `docs/data_layers.md §7` + `ENTITY_ARCHITECTURE.md` Known Limitations / Design Decision Log cross-refs; six other follow-ups land as `INF25–INF29` rows in the new `### Open items` subtable under `## INFRASTRUCTURE — Performance & Reliability`; `is_priceable` refinement filed as `docs/data_layers.md §6 S1`; `admin_bp.py:108` revisit flag moved to `docs/NEXT_SESSION_CONTEXT.md`. See `docs/DOC_UPDATE_PROPOSAL_20260418.md` for the full Phase 0 proposal. **Next priority: `load_13f_v2.py`** (Q1 2026 13F fetch, May 16 filing-deadline window) + **Admin Refresh System**. Validate **8 PASS / 1 FAIL (wellington baseline) / 7 MANUAL** preserved through all three BLOCK merges. Prior session #11 header preserved below._

_Prior header: April 17, 2026 — session #11 close. HEAD: `8323838`. **DM15 fully closed + 13D/G filer resolution + N-CEN hardening.** **(1) 13D/G filer resolution** (`5efae66`, `scripts/resolve_13dg_filers.py`) — 2,591 unmatched `beneficial_ownership_v2` filer CIKs processed via human-reviewed [`data/reference/13dg_filer_research_v2.csv`](data/reference/13dg_filer_research_v2.csv). Two-pass resolver: staging pass does 23 MERGE (CIK identifier adds to existing entities: BlackRock, Blackstone, First Trust, Wellington, Fidelity, Millennium, UBS/Credit Suisse, Vanguard, Insight Partners, Apollo) + 1,640 NEW_ENTITY creations (754 strategic / 749 active / 84 VC / 16 HF / 15 WM / 8 PE / 5 pension / 4 passive / 3 endowment / 1 SWF-reroute GIC; 116 is_activist-flagged); prod-direct pass writes 931 rows to `pending_entity_resolution` (921 excluded_individual / 2 excluded_law_firm / 5 excluded_other / 3 remained pending — audit correction; commit `5efae66` body and prior ROADMAP header both stated 928, which undercounted the 3 still-pending rows by treating only the `excluded_*` subset. See `docs/SYSTEM_AUDIT_2026_04_17.md` §6.1 R-01.) via new `--prod-exclusions` flag (architectural: table not in `db.ENTITY_TABLES` scope). `enrich_13dg.py` ran post-promote: BO v2 entity coverage **77.08% → 94.52%** (+9,050 rows); BO current **73.64% → 94.51%** (+5,169 rows). **(2) DM15b — N-CEN coverage expansion** (`9ce5b17`, `scripts/fetch_ncen.py --ciks`) — 10 fund-trust registrant CIKs (Guggenheim/Principal ETF/TCW MetWest+ETF/Voya/Volatility Shares/NYLI Active+ETF/SEI/Thrivent) had zero `ncen_adviser_map` presence despite fund_universe series + 1-9 N-CEN filings each on EDGAR (silent-fetch failures). New `--ciks` flag bypasses fund_universe + processed filter. Scoped fetch: +103 adviser-series rows; `ncen_adviser_map` **11,106→11,209**; 81/84 fund_universe series now covered. **(3) DM15 Layer 2** (`938e435`, `scripts/dm15_layer2_apply.py`) — 17 sub-adviser DM retargets / **$8.95B** / 7 sub-advisers, all in MDM: Voya IM 17915 (4 series), MacKay Shields 6290 (6), Spectrum AM 10034 (2), Winslow 5046 (2), Principal Real Estate 8652 (1), Dynamic Beta 19093 (1), CBRE IM 11166 (1, chose over shell eid=18645 flagged as merge candidate). Override IDs 205-221 (5 NULL-CIK per INF9d/DM15 L1 precedent — audit correction; commit `938e435` body and prior ROADMAP header both stated 4, undercounting one replay-gap row in the 205-221 range. See `docs/SYSTEM_AUDIT_2026_04_17.md` §6.1 R-02.). **(4) DM15c — Amundi SA global parent** (`9160030`, `scripts/dm15c_amundi_sa_apply.py`) — corrects `parent_bridge_sync` Taiwan-as-global-parent artifact. eid=2214 renamed "Amundi"→"Amundi SA", reclassified passive→active, becomes global rollup target on both worldviews. 12 Amundi geographic entities rerouted to 2214: Austria 1318, Hong Kong 1414, Italy 3217, Singapore 3975, France AM 4248, Germany 4667, Japan 5403, Czech AM 6006, Czech Investicni 7079, UK 8338, Ireland 10266 (was misrouted to US Ameraudi), Taiwan 752 itself. 24 override rows IDs 222-245, all CIK-keyed, no replay gaps. **(5) fetch_ncen.py hardening** (`ef7fb13`+`8323838`) — managers-table guard (Stage 5 cleanup dropped legacy table); ncen_adviser_map dedupe + idempotent insert guard. **Prod state:** entities **26,535** (+1,640); `entity_overrides_persistent` **245** (204→221→245); `ncen_adviser_map` **11,209** (+103); BO v2 94.52% enriched; BO current 94.51% enriched; `investor_flows` 17,396,524; `summary_by_parent` 63,916. Validate **8 PASS / 1 FAIL (wellington baseline) / 7 MANUAL** preserved across all three promotes. Snapshot refreshed 11,361.5 MB. **Next priority: `load_13f_v2.py`** (Q1 2026 13F fetch, May 16 filing-deadline window) + **Admin Refresh System**. **Accepted gap:** 5 Securian SFT series ($1.26B) with no N-CEN sub-adviser rows — documented residual, out of DM15 scope. Prior session #10 header preserved below._

_Prior header: session #10 close (combined batch). HEAD: pending commit. **Shipped in one pass:** (1) `promote_nport.py`+`promote_13dg.py` batch rewrite (`6f4fdfc`, pre-promote commit) — per-tuple loop → single batch DELETE/INSERT/CHECKPOINT; fixes the 2+hr DERA promote and the audit-wipe bug where `_mirror_manifest_and_impacts` reset `promote_status` on every re-promote. (2) Exchange Listed Funds Trust 13-series T2_ambiguous fix — longest-match tiebreaker in `try_brand_substring` + explicit `EXCHANGE LISTED FUNDS TRUST`→3738 brand. (3) Voya DM14c — 3 wholly_owned edges 2489→{17915, 4071, 1591}; DM chain-walk retargets **108 series** (68+40+0) to Voya Financial Inc. (4) Tier D bootstrap wave2 — Palmer Square (24862), Rayliant (24863), Victory Capital Holdings (24864); Tema already existed as 7238 (brand-only wire); Quaker skipped (user CRD 114114 = TruNorth in ADV). (5) Amundi US → Victory Capital re-route — 4294 + 830 rollups retargeted to 24864 via `merged_into`; incorrect eid=4248→752 parent_bridge_sync artifact closed and reverted to self. 9 other eid=752 children deferred to DM15c geo-audit. Staging workflow: sync → wave2 bootstrap → dm14c apply → resolve (254/337 real = 75.4%) → validate (8/1/7 baseline) → diff (676 changes) → promote. Post-promote dry-run confirms 255/337 (75.7%) resolved. Prod state: entities **24,895** (+34), fund_holdings_v2 14,090,397 (unchanged — N-PORT re-promote deferred for this batch; no newly-resolvable series had impacts to promote). Prior header below._

_Prior header: session #9 close (ETF Tier C complete). **Tier C ETF resolution shipped.** `scripts/bootstrap_tier_c_advisers.py` (new) created 6 institution entities (eids 24633–24638: Collaborative Fund Management, Spinnaker Financial Advisors, Yorkville Capital Management, FundX Investment Group, Procure AM, Community Development Fund Advisors). `scripts/resolve_pending_series.py` gained 37 SUPPLEMENTARY_BRANDS entries (80 family × eid reuse + 6 bootstrap); Baron ETF Trust retargeted 24352→4830 (BAMCO) — eid=24352 flagged for DM15b merge-cleanup. Added word-boundary guard in `try_brand_substring` (prevents `ARK ETF` substring match inside `CROSSMARK ETF TRUST`). Staging workflow: sync → bootstrap → resolve (223/337 Tier C real = 66.2%) → validate (8 PASS / 1 FAIL / 7 MANUAL baseline) → diff (1,912 changes) → promote (229 entities / 223 rels / 458 rollups). N-PORT re-promote of run `060422` + topup `095148` picked up newly-resolvable impacts: **fund_holdings_v2 13,943,029 → 14,090,397 (+147,368 rows)**. `_mirror_manifest_and_impacts` bug struck again on 060422; SQL reconciliation restored 20,759 of 20,781 impacts to `promoted` (second caveat entry — see NEXT_SESSION_CONTEXT § Known data caveats). `compute_flows` + `build_summaries --rebuild` re-ran. pending_entity_resolution: 4,420 resolved / 1,526 pending (101 real Tier C remainder + 1,186 deferred synthetics + 239 other). Prior header below._

_Prior header: session #6 close. HEAD: `ffccb92`. Two commits since 2026-04-16 doc-close `ebf0625`: (14) **migration 006 — override_id sequence + NOT NULL** (`ffccb92`) — schema-level fix for the NULL-PK bug; `override_id_seq` created from `MAX+1`, `DEFAULT nextval` + `NOT NULL` applied on `entity_overrides_persistent.override_id`. Closes INF22 at the schema level (runtime MAX+1 helpers now redundant backstops). (15) **Market refresh operational close** — `fetch_market.py` completed overnight; `market_data` 10,064 rows stamped 2026-04-16 23:27. `enrich_holdings.py --fund-holdings` re-run: mvl +445K, pof +127K, fund ticker +488K. `build_summaries.py --rebuild` re-ran. `make freshness` **PASS** — all 7 critical tables OK._

_Prior header: session #5 close. HEAD: `d629639`. **Four more commits since "final" doc close.** (8) DM13 BlueCove false-match sweep (`ef3f302`) — 20 sub_adviser edges closed, 17 DM rollups retargeted ($235B+ FMR LLC), 15 classifications backfilled, entity_overrides 47→82, derived tables rebuilt. (9) manifest.py `_next_id` (`11a35e9`) — fetch_market PK collision fixed; DuckDB sequence drift bypassed via inline MAX+1. (10) `entity_overrides_persistent` override_id NULL fix (`bb444d7`) — `_heal_override_ids` in promote_staging, admin_bp MAX+1. (11) BL-8 category (b) fixes (`4590887`..`d629639`) — F541/W0621/W0622 fixed; W0621+W0622 now globally enforced; pre-commit disable list reduced. (12) **docs session close** (`ebf0625`) — rolled DM13/DM14/INF22 doc state. (13) **DM14 Layer 1** (`d684e4e`) — 8 intra-firm DM collapses via chain walk, $12.15B AUM re-attributed; INF22 prod override_id heal landed as promote side-effect (58 NULL → 25-82)._

_Prior header: DM13 partial fix applied to prod 2026-04-16 16:17 UTC_ (snapshot `20260416_161732`): BlueCove Limited N-CEN false-match sweep closed all 20 `sub_adviser` relationships (parent=18338), retargeted 17 `decision_maker_v1` rollups to the correct EC parent (3 funds' DM rollup was already pointing elsewhere — `Harbor Capital` eid=18336 and `Abbey Capital` eid=18581 — so only graph hygiene on those). Per-sponsor retargets: 3 Strategic Advisers Fidelity → FMR LLC ($235.48B AUM moved back), 7 Boston Partners + 2 WPG Partners → Boston Partners (self), 3 Matson Free Market → Matson Money, 2 Altair → Altair Advisers LLC. 15 sub-adviser parent entities backfilled classification unknown → active (Voya IM, BlackRock IM, JPM IM, T.Rowe Price x3, DFA x2, Allspring, Goldman, Schroders, BlackRock Intl, Milliman, BlueCove, Research Affiliates). All 35 entity_overrides_persistent rows (20 suppress_relationship + 15 reclassify) copied to prod directly — promote_staging's diff tool skips rows with NULL override_id (47 legacy rows + my 35 = 82 now in prod). compute_flows (19.4s) + build_summaries --rebuild (5.5s) ran cleanly — investor_flows/ticker_flow_stats/summary_by_parent/summary_by_ticker row counts unchanged, values reflect corrected DM rollups. validate_entities.py prod: 8 PASS / 1 FAIL / 7 MANUAL baseline preserved. DM14/DM15 remain pending per original roadmap dependency order._

_Known issues from this session: (1) `fetch_market.py` crashed mid-run at batch 2/83 (duplicate `impact_id` PK on `ingestion_impacts` after 141 market_data rows written from batch 1) — stack trace in `logs/fetch_market_20260416.log`. market_data is now partially-refreshed. Separate fix needed for fetch_market's impact_id sequence management before re-running. (2) `promote_staging.py` diff tool cannot match entity_overrides_persistent rows with NULL override_id — new overrides must be inserted into prod directly or all new rows need explicit override_id assignment. Worth filing as INF-follow-up for the staging framework._

_Prior final-close header (HEAD `4af2071`): Final items after part 2: (6) **freshness hooks** (`54bfaad`) — `enrich_holdings.py` stamps `holdings_v2` in addition to `holdings_v2_enrichment` (L3 table had no active writer since Stage 5); `market_data` retro-stamped 2026-04-05 so gate correctly reports STALE 11d instead of MISSING. `make freshness` now fully wired; next operational step is `python3 scripts/fetch_market.py` to clear the `market_data` SLA failure. (7) **BL-8 partial** — 5 pre-commit rules re-enabled as independently revertible commits (`61f028c`..`4af2071`): ruff E702/E731, pylint W0611/W0702/E0611. E0401 reverted because pylint hook's `additional_dependencies` lacks `pydantic`/`curl_cffi`; reclassified as category (b). 245-entry inline suppression inventory captured in NEXT_SESSION_CONTEXT._

_Earlier part-2 header: HEAD: `d330d8f`. **Second half of 2026-04-16** shipped five more commits beyond Batch 3 close: (1) 13D/G entity linkage (`e231633` + docs `f40ffa2`) — migration 005, `bulk_enrich_bo_filers` + `rebuild_beneficial_ownership_current` in `pipeline/shared.py`, `promote_13dg.py` wired, `scripts/enrich_13dg.py`. BO v2 40,009 / 51,905 rows (77.08%) enriched; BO current 18,229 / 24,756 (73.64%). 66-row drift repaired. (2) `add_last_refreshed_at.py` migration (`7b3928d`) run on staging + prod — 13,685 / 17,826 rows (76.77%) backfilled. (3) Mar 2026 N-PORT top-up (`bac4448`) — 2 late amendments, Mar filings not yet on EDGAR. (4) ETF residual Tier A+B (`d330d8f`) — `bootstrap_residual_advisers.py` + 32 SUPPLEMENTARY_BRANDS → 279 pending series resolved across 27 sponsors + 6 new institution entities. (5) N-PORT re-promote recovery via new `--exclude-file` flag on `promote_nport.py`. Final prod: 24,632 entities / 33,521 identifiers / 18,105 relationships / fund_holdings_v2 13,943,029 (entity coverage 84.47%) / beneficial_ownership_v2 51,905 with rollups / entity_relationships now carries `last_refreshed_at`. validate_entities.py baseline preserved. See `docs/NEXT_SESSION_CONTEXT.md` § Known data caveats for `ingestion_impacts.promoted_at` reconstruction note._

_Prior header (part 1): HEAD `559058d` + Batch 3 close. **Batch 3 COMPLETE.** All three remaining rewrites shipped + executed: migration 004 (`summary_by_parent` PK now `(quarter, rollup_type, rollup_entity_id)`), `compute_flows.py` rewrite (now `holdings_v2`-sourced, supports both EC + DM worldviews), `build_summaries.py` rewrite (same swap + N-PORT integration via `fund_holdings_v2`). Final state: `investor_flows` 17,396,524 (8.70M EC + 8.70M DM); `ticker_flow_stats` 80,322; `summary_by_ticker` 47,642; `summary_by_parent` 63,916. `data_freshness` stamped on all four. Legacy `holdings` retirement is fully closed across the codebase._

## Session Summary 2026-04-16 part 2 (five commits beyond Batch 3 — 13D/G linkage, `add_last_refreshed_at`, N-PORT top-up, ETF Tier A+B, recovery)

Continuation of the 2026-04-16 session after part 1 (Batch 3 close). Five commits shipped in order: `e231633` → `f40ffa2` → `7b3928d` → `bac4448` → `d330d8f`.

### 1. 13D/G entity linkage (`e231633` + `f40ffa2`)

First implementation of Group 2 entity columns on `beneficial_ownership_v2`. Prior to this, 13D/G filers were disconnected from the MDM and any rollup-walking query skipped 13D/G entirely.

- **Migration 005** (`scripts/migrations/005_beneficial_ownership_entity_rollups.py`, new, ~135 lines). Adds `rollup_entity_id BIGINT`, `rollup_name VARCHAR`, `dm_rollup_entity_id BIGINT`, `dm_rollup_name VARCHAR` to `beneficial_ownership_v2`. `entity_id BIGINT` was already present (77% populated by legacy pass). Idempotent, `--staging / --prod / --dry-run` flags, stamps `schema_versions` version=5.
- **`bulk_enrich_bo_filers(con, filer_ciks)`** in `scripts/pipeline/shared.py`. Single `UPDATE...FROM` JOIN against `entity_identifiers(type='cik')` + both rollup worldviews + preferred aliases. `filer_ciks=None` → full refresh. Unmatched filers leave all five columns NULL.
- **`rebuild_beneficial_ownership_current(con)`** lifted into `pipeline/shared.py` so `promote_13dg.py` and `enrich_13dg.py` share the rebuild SQL. Expanded SELECT carries all 5 entity columns through.
- **`promote_13dg.py`** wired: scoped `bulk_enrich_bo_filers` call between `_promote` and `_rebuild_current`, stamps `data_freshness('beneficial_ownership_v2_enrichment')`.
- **`scripts/enrich_13dg.py`** (new, ~195 lines). Standalone full-refresh / drift repair. `--staging / --dry-run / --filer-cik` flags. Single atomic UPDATE; restart-safe by virtue of being one statement.
- **Design doc: `docs/13DG_ENTITY_LINKAGE.md`** — Option C split (promote-time scoped + standalone full-refresh), shipped-state reference.

**Prod state post-first-full-refresh:** BO v2 40,009 / 51,905 rows (77.08%) enriched; BO current 18,229 / 24,756 (73.64%); 66-row legacy `entity_id` drift repaired on first run.

**Coverage gap:** 11,896 rows / 2,591 filer CIKs remain NULL. These are 13D/G-only individuals / small corps / activists outside the MDM entirely. Follow-up: `resolve_13dg_filers.py` (item 18 in NEXT_SESSION_CONTEXT).

### 2. `add_last_refreshed_at.py` migration (`7b3928d`)

Ran the migration drafted in 831e5b4. Adds `last_refreshed_at TIMESTAMP` to `entity_relationships`. `entity_sync.insert_relationship_idempotent`'s probe-gated stamping now activates on next N-CEN / ADV refresh. Script gained a `--dry-run` flag to match migration 005 convention.

**Prod state:** 17,826 rows total, 13,685 (76.77%) backfilled from `created_at`, 4,141 NULL (predate `created_at` column landing). `validate_entities.py` baseline preserved: 8 PASS / 1 FAIL / 7 MANUAL.

### 3. Mar 2026 N-PORT top-up (`bac4448`)

`fetch_nport_v2.py --monthly-topup` discovered 2 candidate accessions — both late amendments to Jan 2026 filings for CIK 0001038199 muni-bond series S000012121 (Unrestricted Series) and S000012122 (Greater Western New York Series). Promote: -68 +70 = +2 net rows. `enrich_holdings.py --fund-holdings` ran (Pass B full refresh 830.9s on 12.27M rows; Pass C +4 ticker changes).

**Newest `fund_holdings_v2.report_date` still 2026-02-28.** March 2026 N-PORTs have not yet posted on EDGAR (expected late April / early May at 60-day deadline). Validator clean: 0 BLOCK / 0 FLAG / 0 WARN, entity gate 2 resolved.

### 4. ETF residual Tier A+B (`d330d8f`)

Bulk resolution of 279 of the 616 real-pending N-PORT series (45.3% coverage). Two new script deliverables:

- **`scripts/bootstrap_residual_advisers.py`** (new, ~235 lines; mirrors `bootstrap_etf_advisers.py` pattern from 2026-04-15). Staging-only seeding for ETF trust sponsors absent from the MDM. Idempotent (first-hit reuse by CRD / CIK / canonical_name). Created 6 new institution entities: Stone Ridge Asset Management LLC (eid 24348), Bitwise Investment Manager LLC (24349), Volatility Shares LLC (24350), Dupree & Company, Inc. (24351), Baron Capital Management, Inc. (24352), Grayscale Advisors (24353). Abacus FCF Advisors reused at existing eid 3375 via canonical_name idempotency.
- **`scripts/resolve_pending_series.py`** +32 SUPPLEMENTARY_BRANDS entries. 25 Tier A entries reuse existing MDM entities (Cambria, MFS Muni Series, Principal ETF, TCW ETF + MetWest, REX, NYL Active + ETF, Guggenheim, SEI ETF + Tax Exempt, Angel Oak, Viking Mutual, Thrivent, Russell Investments, Cohen & Steers, Virtus ETF + MACS, Kurv, DoubleLine, Allspring, Voya, Touchstone, Western Asset). 7 Tier B entries point to the new bootstrap eids + Abacus FCF at 3375. Variants chosen as multi-word prefixes to avoid collision with prior PARENT_SEEDS / SUPPLEMENTARY_BRANDS entries (e.g. `MFS MUNICIPAL SERIES` not bare `MFS`).
- **`scripts/promote_nport.py`** +`--exclude-file` flag. Reads newline-delimited series_ids and unions with `--exclude`. Needed because scoping a large subset via argv hits the 128K ARG_MAX limit on macOS.

**Federated Hermes deferred to Tier C** — MDM has 5 candidate US Federated entities (parent eid 4635, primary adviser eid 7633 with CRD 105138 at $542B AUM, plus subsidiaries). Rollup target requires explicit disambiguation before wiring.

**Staging workflow completed:** sync_staging → bootstrap → resolve (279 T2 matches) → validate (8 PASS / 1 FAIL / 7 MANUAL baseline preserved) → diff (2,009 line-level changes) → `promote_staging.py --approved` (285 entities / 287 identifiers / 279 relationships / 570 rollup rows added to prod).

**Per-sponsor resolution breakdown:** MFS 23, TCW 20, NYL 19, Virtus 19, SEI 16, Cambria 15, Stone Ridge 14, Principal 12, Bitwise 11, REX 11, Volatility Shares 11, Guggenheim 10, Kurv 10, Allspring 8, DoubleLine 8, Angel Oak 7, Dupree 7, Touchstone 7, Voya 7, Abacus FCF 6, Viking Mutual 6, Western Asset 6, Baron 5, Cohen & Steers 5, Grayscale 5, Russell Investments 5, Thrivent 5, Nomura 1 (PARENT_SEEDS bonus match).

**N-PORT re-promote for run `nport_20260415_060422_352131`:** scoped to 244 needs-promote series (278 resolved minus 35 previously processed via partial-kill, minus 1 Nomura-match that didn't persist). `--exclude-file` with 13,800 lines skipped everything else. `promote_nport.py`: -0 +81,703 holdings / 235 fund_universe upserts / 244 series. `fund_holdings_v2` 11,670,962 → 13,943,029.

### 5. N-PORT re-promote recovery

An earlier over-scoped attempt at re-promote processed all 20,775 tuples and was killed at ~4,200 tuples in (mid-loop). Consequences:

- `fund_holdings_v2` +2.19M new rows (legitimate — newly-resolvable series).
- BUT `bulk_enrich_run` (post-loop) didn't fire → 3,879,078 rows / 3,931 series had NULL `entity_id` / rollup columns.
- `ingestion_impacts._mirror_manifest_and_impacts` had already wiped the original 2026-04-15 `promote_status='promoted'` rows.

**Recovery:**

1. Called `_bulk_enrich_run` directly for the 3,931 NULL series → 2,744 got enriched (1,187 legitimately unresolvable deferred synthetics stayed NULL).
2. Ran the scoped promote (item 4 above) which also re-fired `bulk_enrich_run` + `stamp_freshness` + `refresh_snapshot`.
3. Reconstructed `ingestion_impacts.promote_status` via SQL — mark as `promoted` for any tuple with matching rows in `fund_holdings_v2`, using `MAX(loaded_at)` per-tuple as a best-effort `promoted_at` proxy.

**Audit-trail caveat documented in `docs/NEXT_SESSION_CONTEXT.md` § Known data caveats:** `promoted_at` for run `nport_20260415_060422_352131` reflects load time, not original promote time. Not a functional issue — holdings data itself is correct. 522 tuples remain genuinely `pending` (deferred synthetics with no prod rows).

---



## Session Summary 2026-04-16 (Batch 3 close — `compute_flows.py` + `build_summaries.py` + migration 004)

The two remaining Batch 3 rewrites + a supporting DDL migration. Closes the legacy `holdings` retirement that started 2026-04-13 (Stage 5).

- **Migration 004 — `scripts/migrations/004_summary_by_parent_rollup_type.py`** (new, ~150 lines). Adds `rollup_type VARCHAR` to `summary_by_parent`; changes PK from `(quarter, rollup_entity_id)` to `(quarter, rollup_type, rollup_entity_id)` so both worldviews can coexist. DuckDB can't ALTER PK in place — script renames + recreates + INSERTs existing 8,417 rows stamped `rollup_type='economic_control_v1'` + DROPs old + CHECKPOINT. Idempotent (probes for the column). Staging-first: validated against staged copy of prod table, then ran on prod. Re-run says ALREADY APPLIED.
- **`scripts/compute_flows.py`** (full rewrite, ~370 lines). Source: `holdings_v2` (legacy `holdings` is dropped). Investor key = `rollup_entity_id` (BIGINT) + `rollup_name` (display); `inst_parent_name` retained as back-compat = `rollup_name` for the active worldview, so `queries.py:1444` reads keep working. Two new columns on `investor_flows` and `ticker_flow_stats`: `rollup_type` and `rollup_entity_id`. Each per-period INSERT runs twice (once per worldview). Value column is `market_value_usd` (Group 1, 100% complete) — matches legacy filing-date semantics; not `market_value_live` (22.4% NULL). Added `WHERE ticker IS NOT NULL AND ticker != ''` filter (correctness improvement). CLI: `--staging`, `--dry-run`. Per-(period × worldview) CHECKPOINT, per-worldview momentum + ticker_stats. Freshness stamps both tables. **Final row counts:** `investor_flows` 9,380,507 → 17,396,524 (8,698,262 EC + 8,698,262 DM — identical because for 13F filings the two rollups coincide for ~all entities). `ticker_flow_stats` 18,986 → 80,322 (40,161 × 2). Runtime 18.1s.
- **`scripts/build_summaries.py`** (full rewrite, ~330 lines). Source: `holdings_v2` (13F side) + `fund_holdings_v2` (N-PORT side). `summary_by_ticker` (rollup-agnostic): `total_value = SUM(COALESCE(market_value_live, market_value_usd))` so the table is correct both before and after `enrich_holdings.py` has run for the quarter. `summary_by_parent` (1 row per (quarter, rollup_type, rollup_entity_id)): `total_aum` from `holdings_v2.market_value_usd`; `total_nport_aum` from `fund_holdings_v2.market_value_usd` scoped to latest `report_month` per `series_id` within the quarter; `nport_coverage_pct = MIN(100, total_nport_aum * 100 / total_aum)` — formula reverse-engineered from legacy 8,417 rows. CLI: `--staging`, `--dry-run`, `--rebuild`. Per-quarter × per-worldview CHECKPOINT. Freshness stamps. **Final row counts:** `summary_by_ticker` 24,570 → 47,642 (4 quarters × ~12K distinct tickers — broader than legacy because `enrich_holdings.py` populated tickers on REITs/CEFs/ADRs/warrants legacy dropped). `summary_by_parent` 8,417 → 63,916 (4 quarters × 2 worldviews × ~8K rollups). Runtime 4.6s.

**Worldview divergence visible in `summary_by_parent` for the first time.** Top-AUM 2025Q4 EC vs DM:
- Vanguard, BlackRock, Capital Group: identical EC vs DM (no sub-adviser split).
- Fidelity: EC `total_nport_aum=$4.03T`, DM `total_nport_aum=$3.57T` — DM correctly attributes Fidelity's sub-advised flows to the actual portfolio managers.
- State Street: EC $792.6B, DM $790.6B — minor sub-adviser split.

**App back-compat preserved.** `queries.py:745/1407/4263` reads `WHERE inst_parent_name = ?` keep working unchanged because the new `summary_by_parent` rows still populate `inst_parent_name` with the rollup display name. DM rows are present and queryable; future tab work can add explicit `WHERE rollup_type = 'decision_maker_v1'` filters when DM consumption is needed.

**Lints across all three scripts: ruff clean / pylint 10/10 / bandit clean.**

**Batch 3 is complete.** Legacy `holdings` retirement is fully closed. Open follow-ups: app smoke-test (Flow Analysis tab + Register tab `nport_coverage_pct` lookups). No code rewrites expected — back-compat designed in.

---

## Session Summary 2026-04-16 (Batch 3 — `enrich_holdings.py` shipped + executed against prod)

Group 3 enrichment for `holdings_v2` now lives as a separate post-promote pass per the Option-B split (data_layers.md §5). Plus `fund_holdings_v2.ticker` populate.

- **`scripts/enrich_holdings.py`** (new, ~480 lines). Three-pass orchestrator:
  - **Pass A** — NULL cleanup on `holdings_v2` for rows whose `cusip NOT IN cusip_classifications` (no-op today; safety net for future drift).
  - **Pass B** — Main enrichment via cusip-keyed `UPDATE...FROM (lookup)`. The lookup is one row per cusip (verified 1:1 across `cusip_classifications` / `securities` / `market_data`); per-row `mvl` / `pof` use the OUTER row's `shares`. Pattern documented inline.
  - **Pass C** — `fund_holdings_v2.ticker` populate (no `is_equity` gate — N-PORT carries equity-priceable ETF/CEF/ADR positions whose CUSIPs classify as `is_equity=FALSE`).
- **CLI:** `--staging` (default prod), `--dry-run`, `--quarter YYYYQN`, `--fund-holdings`. Per-pass CHECKPOINT. Per-run log to `logs/enrich_holdings_{ts}.log`. Stamps `data_freshness('holdings_v2_enrichment')` at end of write run with row_count.
- **Pre-write design adjustments vs original proposal** (all confirmed by operator before write):
  1. **Join key.** Original spec said `(accession_number, cusip, quarter)` — that key has 1.29M dup groups covering 4.97M rows, would silently corrupt `mvl`/`pof`. Switched to lookup keyed by `cusip`.
  2. **`security_type_inferred` source.** Drafted to read `cusip_classifications.canonical_type` (BOND/COM/OPTION/...) and produced 12.27M sti changes. Switched to `securities.security_type_inferred` (legacy domain `equity/etf/derivative/money_market`) — the values the app's read paths speak. Result: 0 sti changes, "values agree" honored.
  3. **`fund_holdings_v2.ticker` projection.** Proposal estimated ~10.4M post-state; real number is 5.18M (+1.45M uplift) — N-PORT's CUSIP universe is dominated by bonds/ABS/derivatives that have no `securities.ticker`.

**Final prod state (after run):**

| Column | Before | After | Delta |
|---|---:|---:|---:|
| `holdings_v2.ticker` | 11,226,520 | 10,395,053 | -831,467 |
| `holdings_v2.security_type_inferred` | 12,270,984 | 12,270,984 | 0 |
| `holdings_v2.market_value_live` | 10,874,758 | 9,527,773 | -1,346,985 |
| `holdings_v2.pct_of_float` | 9,460,740 | 7,587,332 | -1,873,408 |
| `fund_holdings_v2.ticker` | 3,737,695 | 5,184,911 | +1,447,216 |

**Runtime:** Pass B 706.7s on 12.27M rows. Pass A no-op. Pass C 0.6s on 11.67M rows.

**Spot-check confirmed NULL-tolerance** holds across `query1`, `query3`, `query7` against AAPL — `query1` returns 104 institutional rows with realistic aggregates (`value_live=$2.52T`, `pct_float=67.23%`, 5,797 institutions). All Group 3 readers use SUM/NULLS LAST/IS NOT NULL patterns per data_layers.md §5. No code rewrites needed in `queries.py`.

**Lints:** ruff clean / pylint 10/10 / bandit clean (with project `.bandit` config skipping B608 across the codebase).

**Next:** `compute_flows.py` rewrite (Batch 3-2) — currently reads legacy `holdings`; rewrite to `holdings_v2`. Then `build_summaries.py` rewrite (Batch 3-3).

---

## Session Summary 2026-04-14 (parallel no-DB workstream, commit 831e5b4) — Makefile + freshness gates

Ran concurrent with the staging-locked CUSIP OpenFIGI retry; now fully integrated into the doc set.

- **`Makefile`** (155 lines, new). Single-entry pipeline orchestration: 9-step quarterly update, `DRY_RUN=1` support, per-step standalone targets, `make status` and `make freshness` gates that block advancement on stale source tables.
- **`scripts/check_freshness.py`** (108 lines, new). Exit-1 gate on stale or missing `data_freshness` rows; prod read-only; `--status-only` for informational use.
- **`record_freshness` hooks** added to 8 pipeline scripts — `fetch_adv`, `fetch_ncen`, `fetch_finra_short`, `fetch_13dg` phase 3, `build_entities`, `build_managers`, `build_fund_classes`, `build_cusip`. `fetch_13f` + v2 SourcePipelines skipped intentionally (v2 stamps via its own promote path).
- **`validate_entities.py --read-only`** flag. `--prod --read-only` returns the established 9 PASS / 0 FAIL / 7 MANUAL.
- **`scripts/migrations/add_last_refreshed_at.py`** drafted (124 lines) — **NOT RUN**. Adds `last_refreshed_at TIMESTAMP` to `entity_relationships` with `created_at` backfill; staging-first on next entity session.
- **`entity_sync.insert_relationship_idempotent`** probe-gated stamping of `last_refreshed_at` when the column exists. Safe on pre- and post-migration DBs. Stamps on INSERT, ON-CONFLICT-DO-NOTHING, and deferred-primary paths.
- **`ARCHITECTURE_REVIEW.md` §Batch 3-A** — as-shipped schema note for `fund_family_patterns` (2 cols, 83 rows, PK `(inst_parent_name, pattern)`) + `get_nport_family_patterns()` memoization. Corrects stale 3-col planning docs.
- **`NEXT_SESSION_CONTEXT.md` §gg** — `holdings_v2` filing-line grain: true composite key is `(cik, ticker, quarter, put_call, security_type, discretion)`, not `(cik, cusip, quarter)`.

**Follow-up items surfaced by this workstream** (detailed in `docs/NEXT_SESSION_CONTEXT.md` open-items list):
- Run `add_last_refreshed_at.py` migration via staging workflow.
- `entity_id` linkage on `beneficial_ownership_current` + `beneficial_ownership_v2` (13D/G is currently disconnected from the MDM).
- CRD backfill for top-100 AUM filers.
- `entity_identifiers_staging_review` backlog (~280 items).
- ~~DM13~~ Done 2026-04-16 (BlueCove `ef3f302`). ~~DM14 Layer 1~~ Done 2026-04-16 (`d684e4e`). ~~DM14b~~ Done 2026-04-17 (`3c99365`, 6 name-inference edges, 91 retargets / $183B). ~~DM14c~~ Done 2026-04-17 (`8136434`, Voya 108 series / $74B). ~~DM15 Layer 1~~ Done 2026-04-17 (`7bb68f5`, 15 external sub-adviser retargets / $10.3B + migration 007). ~~DM15 Layer 2~~ Done 2026-04-17 (`938e435`, 17 retargets / $8.95B across 7 sub-advisers — Voya IM / MacKay / Spectrum / Winslow / Principal Real Estate / Dynamic Beta / CBRE IM — all in MDM, override IDs 205-221). ~~DM15b N-CEN coverage expansion~~ Done 2026-04-17 (`9ce5b17`, 10 gap registrant trusts, +103 adviser-series rows, 81/84 series covered; unblocked DM15 Layer 2). ~~DM15c Amundi SA global parent~~ Done 2026-04-17 (`9160030`, eid=2214 renamed Amundi→Amundi SA passive→active, 12 Amundi geographic entities rerouted from eid=752 Taiwan parent_bridge_sync artifact, override IDs 222-245). ~~INF22 migration 006~~ Done 2026-04-17 (`ffccb92`). **All DM workstreams closed.** **Accepted DM gap:** 5 Securian SFT series ($1.26B AUM) — no N-CEN sub-adviser rows despite N-PORT presence; documented residual. **Deferred:** DM15 umbrella-trust audit (~132 series / $105B, originally tagged DM15b L5 batches 1-6 — Sterling Capital / Gotham / NEOS / Segall Bryant / Mairs & Power / Brandes / Crawford / Bridges / Champlain / FPA) — different scope from the Done DM15b N-CEN coverage expansion; blocked on D13 (N-PORT XML sub-adviser extraction) or DM6 (N-1A prospectus parsing). **Next priority: `load_13f_v2.py`** (Q1 2026 13F fetch, May 16 filing-deadline window) + **Admin Refresh System** for scheduled multi-source fetches.
- ~~`make freshness` — confirm newly loaded tables register correctly~~ Done 2026-04-17 — all 7 critical tables PASS after overnight `fetch_market.py` + `enrich_holdings.py` + `build_summaries.py --rebuild`. `market_data` 10,064 rows stamped 2026-04-16 23:27.
- LEI coverage = 0 despite 13,143 GLEIF rows in `lei_reference` — no `entity_identifiers.identifier_type='lei'` entries.
- `entity_relationships.valid_from` uniformly at sentinel — refresh age unmeasurable until migration lands.
- `PARENT_SEEDS` count is **110** (`scripts/build_entities.py:6`), not 50; update any stale planning docs in follow-ups.
- `schemas.py` Pydantic expansion (~55 models; autogenerate via FastAPI Batch 4-C).

---

## Session Summary 2026-04-15 (close — cleanup round)

Two Session-2 close items (commit `39d5e95`):

- **`resolve_amendments()` cross-ZIP dedupe** (`scripts/fetch_dera_nport.py`). Optional `staging_con` param adds a second pass that queries `ingestion_impacts` for any `(series_id, report_month)` already represented by a newer accession and drops the submission. `load_to_staging` now deletes superseded impact rows before writing the amendment's impact — clears the `_block_dup_series_month` trigger on multi-ZIP loads automatically.
- **`validate_nport.py` FLAG/WARN loops rewritten as set-based SQL.** Six functions (`_flag_reg_cik_changed`, `_flag_top10_drift`, `_flag_aum_delta`, `_flag_new_series`, `_warn_holdings_count_delta`, `_warn_aum_delta_medium`) each now run one SQL query that JOINs a registered staging DataFrame against prod tables. Live staging run (14,046 series / 8.5M holdings): **66 seconds vs 45+ min** before. Full-validator smoke test on next promote is tracked as a follow-up.

**Today's commit chain (chronological):** 7081886 → c5eada8 → 5cf3585 → 44bc98e → e868772 → 8a41c48 → 39d5e95. Seven commits across CUSIP v1.4 + N-PORT DERA backfill + cleanup.

---

## Session Summary 2026-04-15 (CUSIP v1.4 prod promotion)

Overnight staging retry passed `validate_classifications`; this session moved the layer to prod.

- **Migration 003 applied to prod** (`data/13f.duckdb`): 4 new tables — `cusip_classifications`, `cusip_retry_queue`, `_cache_openfigi`, `schema_versions`; 7 new columns on `securities` (`canonical_type`, `canonical_type_source`, `is_equity`, `is_priceable`, `ticker_expected`, `is_active`, `figi`). `market_sector` already present (column 6) and left alone. Idempotent; rollback-on-failure.
- **Staging → prod copy** via `ATTACH '...13f_staging.duckdb' AS stg (READ_ONLY)` from a prod write connection, single transaction:
  - `cusip_classifications`: 132,618 rows
  - `cusip_retry_queue`: 37,925 rows
  - `_cache_openfigi`: 15,807 rows
- **`normalize_securities.py` on prod** — UPDATE + LEFT-JOIN INSERT. `securities` 44,929 → 132,618 (+87,689; mostly 13D/G-only CUSIPs now getting a canonical_type).
- **`validate_classifications.py` on prod** — 12 checks. All BLOCK + BLOCK_POST PASS (0 NULL canonical_type, 0 derivatives misclassified as BOND/PREF, 0 securities rows without classification match, 0 NULL is_equity/canonical_type after normalization). OTHER=0.14% (threshold ≤ 5%). **Verdict: READY: YES.** Two WARNs (retry resolved 41.7% < 50% WARN_MIN; retry unmappable 58.3% > 30%) reflect CUSIPs OpenFIGI can't map (private/delisted/exotic).

**`canonical_type` distribution on prod:** BOND 71,328 · COM 30,340 · OPTION 18,730 · ETF 5,580 · CASH 1,882 · FOREIGN 1,150 · PREF 1,078 · MUTUAL_FUND 627 · ADR 610 · WARRANT 555 · OTHER 185 · CLO 121 · REIT 120 · BANK_LOAN 106 · CEF 71 · CONVERT 69 · SPAC 66.

**Effect on pipelines:** `discover_market()` can now pre-filter to `canonical_type='COM' OR 'ETF' OR 'ADR' OR 'REIT' OR 'PREF'` before OpenFIGI round-trips; equity universe is `is_equity=TRUE AND is_active=TRUE`. The retry queue for post-OpenFIGI `unmappable` rows (~22K) can be reviewed manually later or scheduled for periodic re-try as OpenFIGI adds mappings.

**No code changes this step** — ran existing scripts against prod. Prior relevant commits on main (already pushed): migration 003 + cusip_classifier + build_classifications (7081886, Session 1), run_openfigi_retry + normalize_securities + validate_classifications (831e5b4, Session 2 scripts).

---

## Session Summary 2026-04-15 (N-PORT DERA ZIP Session 2 — staging + promote)

Executed on top of Session 2 code (commit 44bc98e). Ran the full DERA bulk load, validated, and promoted the entity-resolved subset.

**Load (run_id `nport_20260415_060422_352131`):**
- Both quarters from pre-downloaded local ZIPs (`--zip data/nport_raw/dera`). No network download.
- 2025Q4: 5,104,168 holdings, 13,310 accessions, 18 amendments dropped, 16.4 min.
- 2026Q1: 5,934,769 holdings, 13,143 accessions, 5 amendments dropped, 18.3 min.
- **Perf fix mid-load (scripts/fetch_dera_nport.py):** per-accession CHECKPOINT was fine at Session-1 scale (21 accessions) but dominated cost at 13K accessions. Changed to CHECKPOINT every 2000 accessions + progress print every 500. Rate: 7.5 → 13 acc/s. Final CHECKPOINT at quarter end.

**Validation — 3 blockers surfaced:**
1. 5,921 of 14,046 staged series (42%) missing from `entity_identifiers` — index / bond / MM funds the legacy XML `classify_fund` path had filtered out. BLOCK per strict N-PORT entity gate.
2. 1,187 synthetic series_ids (`{cik}_{accession}` fallback) — subset of the above.
3. 8 cross-quarter amendment duplicate impact rows — `resolve_amendments` dedupes within a ZIP, not across. `stg_nport_holdings` was fine (DELETE+INSERT wins); only `ingestion_impacts` had dupes.

**Resolution (option B + D per user direction):**
- **D:** Deduped impacts in place — kept the row whose `manifest_id` matches the stg holdings authoritative link. 8 → 0.
- **B:** Split staged set into **resolved (8,125)** + **excluded (5,921)**. Excluded queued in `pending_entity_resolution` with `source_type='NPORT'`, `identifier_type='series_id'`, `resolution_status='pending'`.

**New file** `scripts/validate_nport_subset.py` — fast set-based validator for subset promotion. `validate_nport.py`'s O(N) per-series Python loops hung at 14K series (>45 min, killed); subset validator runs BLOCK + entity gate (EC + DM rollup) as SQL aggregates in < 1 min. Writes `logs/reports/nport_{run_id}.md` with `Promote-ready: YES`. Idempotent pending-queue insert.

**Promote** (commit pending — changes to fetch_dera_nport + new validator + .gitignore + docs):
- Invoked `promote_nport.py --exclude` with the 5,921 series CSV (86KB arg; well under ARG_MAX 1MB).
- ~12,300 tuples promoted (8,125 series × ~1.5 months avg).
- Holdings: **−913,898 / +3,836,260** (net +2,922,362).
- fund_universe upserts: 8,125.
- Snapshot refreshed at 7.65 GB.

**Prod state after promote:**
| | Before | After |
|---|---|---|
| fund_holdings_v2 rows | 6,393,206 | 9,315,568 |
| series | 6,674 | 8,453 |
| newest report_date | 2025-11-30 | 2026-01-31 |
| fund_universe | 6,677 | 8,459 |
| pending_entity_resolution (NPORT) | 0 | 5,921 |

**Follow-ups to track:**
1. Entity MDM expansion for the 5,921 queued series (index / bond / MM funds — Session 3 topic).
2. `validate_nport.py` performance — rewrite per-series loops (`_flag_top10_drift`, `_warn_holdings_count_delta`, `_flag_aum_delta`) as set-based SQL so the full validator scales past 10K series.
3. Cross-quarter amendment dedup in `resolve_amendments` — currently per-ZIP; Session 2 used a one-off manual dedup. Automate for future runs.

---

## Session Summary 2026-04-14 (N-PORT DERA ZIP Session 2) — fetch_nport_v2.py rewrite

Session 1 proved parity (commit 5cf3585); Session 2 integrates. This update covers the code rewrite; the staging run + validate + promote land in a separate update once the concurrent CUSIP OpenFIGI retry (also staging) releases the write lock.

- **Rewrite** `scripts/fetch_nport_v2.py` as 4-mode orchestrator:
  - **Mode 1 — DERA bulk (default):** `discover_missing_quarters()` seeds last 2 complete DERA quarters on first run, walks forward from last-loaded thereafter. `_already_loaded_quarters()` reads `ingestion_manifest` for `DERA_ZIP:YYYYQn` object_keys. Each ZIP gets one manifest row + one `quarter`-unit impact; per-(series, month) impacts written inside `dera_load_to_staging`.
  - **Mode 2 — `--monthly-topup` (XML):** `NPortXMLPipeline` (renamed from Session-1 `NPortPipeline`) scoped to today's calendar quarter; filters filings posted since last DERA ZIP quarter-end. Correct edgartools API — `get_filings(form='NPORT-P', year=Y, quarter=Q)` — no `limit=` kwarg.
  - **Mode 3 — `--test`:** delegates to `fetch_dera_nport.run_test_mode(zip_spec=...)`.
  - **Mode 4 — `--dry-run`:** shows plan; skips staging read-lock so it still works when another writer holds it.
- **New flag** `--zip PATH` on both scripts. PATH may be a single ZIP file or a directory containing `{YYYY}q{N}_nport.zip` files; matches skip the network download. Falls back to auto-download when no match found.
- **Circular-import fix:** `quarter_label_for_date/_for_month`, `_STG_HOLDINGS_DDL`, `_STG_UNIVERSE_DDL`, `_ensure_staging_schema` moved from `fetch_nport_v2` into `fetch_dera_nport`. Both scripts import from `fetch_dera_nport` now — no caller-side duplication.
- **Backward-compat alias:** `TEST_FUNDS = {name: cik for f in REFERENCE_FUNDS}` kept so any legacy import pattern continues to work; new code should prefer `REFERENCE_FUNDS` from `fetch_dera_nport`.
- **Dry-run verdict (2026-04-14):** missing quarters = `['2025Q4', '2026Q1']`, both resolved locally via `--zip data/nport_raw/dera`. 398MB + 442MB ZIPs already on disk.
- **Blocker:** `scripts/run_openfigi_retry.py --staging` (PID 49378 at session close) holds the staging write-lock. DERA staging load deferred until it finishes — DuckDB single-writer semantics. Same pattern as prod-promote vs `app.py`.

**Expected after staging load:** ~2-3M new rows across 3-5 new months (2025-12, 2026-01/02/03). Validate with `validate_nport.py --changes-only` then `--run-id`; promote on authorization.

**Next (Session 3+):** N-1A / N-CSR narrative strategy enrichment (populates the three `fund_universe.strategy_*` columns added by migration 002). Separate from N-PORT fetch.

---

## Session Summary 2026-04-14 (CUSIP v1.4 Session 2) — OpenFIGI retry scripts

Session 2 delivers the scripts that drain `cusip_retry_queue` via OpenFIGI v3 and port classification flags into `securities`. Full overnight retry will run outside this session; these scripts are committed now so they're ready to execute.

- **`scripts/run_openfigi_retry.py`** — standalone driver. Selects `status='pending' AND attempt_count < 3` from `cusip_retry_queue`, POSTs 10-CUSIP batches to `/v3/mapping` with 2.4s sleeps (25 req/min), upserts `_cache_openfigi`, updates `cusip_classifications` (ticker / figi / exchange / market_sector / confidence='high' / ticker_source='openfigi'), marks queue row `resolved`. FOREIGN CUSIPs whose OpenFIGI exchange is US get flipped to `is_priceable=TRUE` inline. Resume-safe via idempotent UPSERT. `--limit N` for batched partial runs. Live 100-CUSIP test: **92 resolved / 8 no_match / 0 errors in 30 seconds** on staging.
- **`scripts/normalize_securities.py`** — UPDATE `securities` from `cusip_classifications` JOIN. Populates 7 new columns + COALESCE-refreshes ticker/exchange/market_sector. Inserts rows for classification-only CUSIPs (13D/G-only). UPDATE + LEFT-JOIN INSERT; never DROP+CREATE.
- **`scripts/validate_classifications.py` extended** with 5 new checks: resolution rate ≥ 50% (WARN_MIN), unmappable rate ≤ 30% (WARN), `securities.canonical_type NULL` post-normalization (BLOCK_POST), `securities.is_equity NULL` post-normalization (BLOCK_POST), securities rows without classification match (BLOCK_POST). BLOCK_POST gates auto-SKIP until `normalize_securities.py` has run.
- **`scripts/pipeline/cusip_classifier.py`** — `US_PRICEABLE_EXCHANGES` extended with `'US'` (OpenFIGI v3 composite code). Without this, the FOREIGN→priceable re-flip in the retry script never fires because v3 returns `exchCode='US'` for US-listed composite queries, not specific codes like NMS/NYQ.
- **Pre-flight confirmed:** 132,618 classifications, 37,925 pending → 37,833 pending after test batch, `_cache_openfigi` now has 92 rows, securities 7 new columns still all-NULL (normalization defers to post-retry).

**Overnight retry command:**
```bash
python3 scripts/run_openfigi_retry.py --staging        # ~2.5h at 250 CUSIPs/min
python3 scripts/normalize_securities.py --staging
python3 scripts/validate_classifications.py --staging
```

**Prod promotion** deferred until post-overnight validation passes; requires explicit authorization before running Migration 003 + full pipeline against prod.

---

## Session Summary 2026-04-14 (N-PORT DERA ZIP Session 1) — parity test harness

Builds the DERA quarterly-ZIP fetch path as a parallel to `fetch_nport_v2.py` (per-accession XML). One ~470MB ZIP replaces thousands of individual HTTP requests. **Does not replace fetch_nport_v2.py** — Session 2 integrates DERA as the primary bulk path after this parity test passes.

- **New file** `scripts/fetch_dera_nport.py` (~1,000 lines): downloads SEC DERA `{YYYY}q{N}_nport.zip`, streams TSVs from the ZIP without extraction (zipfile.open + csv.DictReader — critical for the 988MB FUND_REPORTED_HOLDING.tsv), joins the 4 metadata tables (SUBMISSION / REGISTRANT / FUND_REPORTED_INFO / IDENTIFIERS) into stg_nport_holdings shape. Amendment resolution: latest accession per (series_id, report_month). CURRENCY_VALUE is already USD per SEC Rule C.2.c (readme confirmed). `--test` routes to a dedicated `data/13f_dera_parity.duckdb` file so it never contends with live staging runs.
- **New file** `scripts/migrations/002_fund_universe_strategy.py`: adds 3 nullable columns to `fund_universe` — `strategy_narrative`, `strategy_source`, `strategy_fetched_at`. Targets for future N-1A / N-CSR narrative enrichment (Session 3+). Idempotent via `ADD COLUMN IF NOT EXISTS`. Applied to staging; prod apply deferred until dev app releases write lock.
- **`scripts/validate_nport.py` `--changes-only` mode**: run-scoped per-(series, report_month) diff vs prod — classifies each tuple as NEW_SERIES / NEW_MONTH / AMENDMENT and reports prod series absent from the run as closures. Writes `logs/reports/nport_changes_{run_id}.md`.
- **Parity test results (2025Q3, 5 reference funds, 21 accessions, 12,633 holdings):**
  - row_count_delta: max |delta| = **0 rows** (threshold: ±1) ✓
  - cusip_coverage: min Jaccard = **100.00%** (threshold: ≥99%) ✓
  - series_id_mismatches: 0 ✓
  - report_month_mismatches: 0 ✓
  - group1_required_populated: 100% (fund_cik, series_id, report_month, report_date, market_value_usd, shares_or_principal) ✓
  - amendment_latest_wins: 0 tuples with >1 accession ✓
  - manifest_id_populated: 100% ✓
  - **Verdict: APPROVED FOR SESSION 2**
- **Key discoveries from DERA readme + real data:**
  - `SERIES_ID` present in FUND_REPORTED_INFO.tsv (A.2.b), 696 of 13,199 accessions (5.3%) missing — handled with synthetic fallback matching fetch_nport_v2 convention.
  - Monthly granularity confirmed: 10+ distinct REPORT_DATE values in one quarter ZIP.
  - 1 series per accession (13,199 accessions ↔ 13,199 FUND_REPORTED_INFO rows in 2025Q3).
  - `CURRENCY_VALUE` is USD despite the name (SEC N-PORT Rule C.2.c); EXCHANGE_RATE is supplementary. Not native currency.
  - `ISSUER_CUSIP='N/A'` is how prod stores CUSIP-less positions (832K rows in prod) — parity requires preserving 'N/A' literally, not normalising to NULL.
  - Two parity-metric bugs caught: `pandas .count('cusip')` excludes NULLs (false row-count delta); CUSIP 'N/A' normalisation diverged from prod.

**Session 2 plan (separate prompt):** Rewrite `fetch_nport_v2.py` to use DERA ZIP as primary bulk path for complete quarters, keep per-accession XML as Mode 2 for monthly top-up. Integrate manifest semantics end-to-end. Full promote path tested against amendment chains.

**Follow-up bookkeeping:**
- Prod migration 002 pending user app.py restart (cannot apply while write-lock held).
- Parity DB at `data/13f_dera_parity.duckdb` — regenerated from scratch on each `--test` run, safe to delete.

---

## Session Summary 2026-04-14 (CUSIP v1.4 Session 1) — classification layer

First of two sessions. Builds the CUSIP classification layer that gates which CUSIPs are priceable equities vs permanent non-equities (BOND/OPTION/CASH). Session 2 will run the OpenFIGI retry against the resulting queue.

- **Migration 003** (`scripts/migrations/003_cusip_classifications.py`): 3 new tables (`cusip_classifications`, `cusip_retry_queue`, `_cache_openfigi`) + `schema_versions` stamp + 7 new columns on `securities` (`canonical_type`, `canonical_type_source`, `is_equity`, `is_priceable`, `ticker_expected`, `is_active`, `figi`). `market_sector` already existed — not re-added. Rollback on failure, idempotent, applied to staging.
- **New classifier module** (`scripts/pipeline/cusip_classifier.py`): pure logic, no DB writes. `classify_cusip()` implements the 5-step order: derivative pre-check → market_sector map → combined seed → rule match → (new) equity-seed fallback. Corrected `ASSET_CATEGORY_SEED_MAP` after discovering plan v1.4 had `DE` wrongly mapped to 'debt' (it's Derivative-Equity) and missed `DBT`/`ABS-*`/`LON`/`STIV`/`DIR`/`DCO`/`DCR`/`DO`/`DFE`. Full vocabulary now covered.
- **`scripts/build_classifications.py`**: standalone driver. Reads 3-source CUSIP universe from prod, classifies all 132,618 rows in 2.5s (~53K rows/s), UPSERTs into `cusip_classifications` (staging), populates `cusip_retry_queue` with 37,925 pending entries. Applies manual overrides from `data/reference/ticker_overrides.csv` as Step 5.
- **`scripts/validate_classifications.py`**: 4 BLOCK + 1 WARN + 1 INFO gates. All BLOCK PASS on staging; OTHER = 0.14% (well under 5% WARN threshold).
- **`scripts/build_cusip.py` rewrite**: legacy moved to `scripts/retired/build_cusip_legacy.py`. New version is UPSERT-only (no DROP+CREATE), OpenFIGI v3 with 10-per-batch + 2.4s sleep (25 req/min), `save_figi_cache` persists all 7 response columns, `handle_unfetchable` logs orphan tickers to `logs/unfetchable_orphans.csv`, removed `UPDATE holdings` block (handled by Batch 3 enrichment).
- **`discover_market()` additive filter**: `LEFT JOIN cusip_classifications cc` with `has_table()` guard. Pre-migration: no-op. Post-migration: pre-filter 5,867 tickers → post-filter 5,031 (836 excluded — 1,709 OPTIONs, 148 BONDs, 50 FOREIGN, 40 CASH, etc.).
- **Staging validation numbers:**
  - Total CUSIPs classified: **132,618**
  - `canonical_type='OTHER'`: 185 (0.14%)
  - `cusip_retry_queue` pending: **37,925** (~2.5h at 250 CUSIPs/min in Session 2)
  - Top categories: BOND 71,328 / COM 30,340 / OPTION 18,730 / ETF 5,580 / CASH 1,882
  - `discover_market()` universe: 5,867 → 5,031 tickers (836 excluded, all legitimate non-equities)

**Next (Session 2):** OpenFIGI retry run against `cusip_retry_queue` (~10K equity CUSIPs at 250 req/min → ~40 min). Then port `cusip_classifications` flags into `securities` via `update_securities_from_classifications()`.

---

## Session Summary 2026-04-14 (Batch 2C) — N-PORT v2 SourcePipeline

Second SourcePipeline. Same structural pattern as 13D/G (Batch 2B) with stricter entity gate (BLOCK on unknown series_id, not FLAG) and dual staging tables (holdings + universe).

- **3 new scripts:** `scripts/fetch_nport_v2.py` (NPortPipeline; reuses parse_nport_xml + classify_fund from legacy fetch_nport.py; dynamic quarter labelling replaces the hardcoded MONTHLY_TARGETS dict; atomic per-series loads via BEGIN→DELETE→INSERT→COMMIT→CHECKPOINT), `scripts/validate_nport.py` (HARD entity gate; both rollup worldviews; lifecycle checks for new_series / top10_drift / AUM delta), `scripts/promote_nport.py` (Group 2 entity enrichment at promote via entity_current lookup; atomic per (series_id, report_month) tuple; UPSERT fund_universe; stamp freshness; refresh snapshot).
- **discover_nport() rewrite** in `scripts/pipeline/discover.py`: was a `return []` stub; now actually queries EDGAR per CIK (test mode) or per calendar quarter (full mode), coerces edgar lib's mixed string/date returns, anti-joins ingestion_manifest.
- **Validate scripts open prod read-only** — `validate_nport.py` and `validate_13dg.py` updated. The dev app holds a prod read lock; entity_gate_check's pending insert is wrapped in try/except so the gate logic still works.
- **5-fund test:** Fidelity Contrafund / Vanguard Wellington / T. Rowe Blue Chip / Dodge & Cox / Growth Fund of America. 15 accessions → 14 series → 10,503 holdings staged in 5.8s. Validate 0 BLOCK / 3 FLAG / 1 WARN. Promote: −3,006 / +10,503 holdings, +3 new series (6,677 → 6,680). New 2025-11 data live; AAPL now in Growth Fund of America 2025-11 ($7.55B / 2.22% NAV) with full Group 2 entity enrichment populated.
- **All 10 PROCESS_RULES blockers** for fetch_nport.py from `docs/pipeline_violations.md` cleared in v2 — see `docs/NEXT_SESSION_CONTEXT.md` for the per-blocker tracking.
- **Operational note:** dev app must be stopped for promote (DuckDB single-process write-lock contention). Tracked as a follow-up; long-term resolution is MT-1 (Gunicorn / hosted serving).

**Next session:** Batch 3 — `enrich_holdings.py` Group 3 DirectWritePipeline for `holdings_v2` (`ticker`, `security_type_inferred`, `market_value_live`, `pct_of_float`). Plus retire legacy `fetch_nport.py` + `fetch_13dg.py` after amendment-chain test runs.

---

## Session Summary 2026-04-14 (Batch 2B-13dg) — scoped 13D/G reference vertical

First full SourcePipeline proof — discover → fetch → parse →
load_to_staging → validate → promote end-to-end.

- **3 new scripts:** `scripts/fetch_13dg_v2.py` (SourcePipeline class `Dg13DgPipeline`, EDGAR efts full-text search by subject CIK, manifest+impact writes per accession, reuses proven parser from `fetch_13dg.py`), `scripts/validate_13dg.py` (BLOCK/FLAG/WARN + `entity_gate_check` with 13D/G relaxation: missing `entity_identifiers` → FLAG not BLOCK, queues to `pending_entity_resolution`), `scripts/promote_13dg.py` (DELETE+INSERT `beneficial_ownership_v2`, rebuild `beneficial_ownership_current`, stamp freshness, refresh `13f_readonly.duckdb` snapshot, mirror manifest+impacts staging→prod).
- **Scoped test (AR, OXY, EQT, NFLX):** 3 accessions returned by EDGAR (AR had no new filings since 2024-11-12). Staged → validated (0 BLOCK / 3 FLAG / 0 WARN; 3 filers queued in `pending_entity_resolution`) → promoted. `beneficial_ownership_current`: 24,753 → 24,756 rows. Backup taken beforehand.
- **CHECKPOINT GRANULARITY** policy comment on `fetch_13dg_v2.py` (unit = one accession) and `promote_13dg.py` (unit = one run_id).
- **Control plane live in prod for 13D/G:** 3 rows in `ingestion_manifest`, 3 in `ingestion_impacts` (all `promote_status='promoted'`), 3 in `pending_entity_resolution`, `data_freshness` stamped on both BO tables.
- **`fetch_13dg.py` stays intact** — retires to `scripts/retired/` after a second successful v2 run (amendment chain test).

**Next:** resolve pending_entity_resolution rows via INF1 workflow, then copy this SourcePipeline pattern to N-PORT (Batch 2C).

---

## Session Summary 2026-04-14 (Batch 2B-market) — market data quality fix

Hardening before full market refresh is ever authorized.

- **CUSIP-anchored `discover_market()`** (`scripts/pipeline/discover.py`): latest quarter + latest report_month + equity-only + $1M min position + `securities.ticker` present. 43,049 → 5,874 active universe, 5,628 stale. Est. fetch time 712 min → 94 min. New `con_write` arg lets staging runs check freshness against the write DB, not prod.
- **Cross-validation** (`_cross_validate_ticker`): fuzzy name match vs `securities` (token_sort_ratio < 60), market_cap sanity, known-exchange check, price divergence vs `holdings_v2` implied price. All WARN-level. Output: `logs/market_validation_{run_id}.csv`.
- **All-3-bucket attempt stamping** (`_stamp_batch_attempt`): stamps fetch_date + metadata_date + sec_date for every ticker in the batch, even ones Yahoo/SEC returned no data for. Fixes the restart-safety bug where Yahoo-unpriceable tickers (`1RG`, exotic OTC) kept being re-picked on every run because their metadata_date/sec_date stayed NULL.
- **`--test-size N` flag** on `--test` mode.
- **CHECKPOINT GRANULARITY POLICY** comment block added to `fetch_market.py`: unit = one batch (100 tickers).
- **Crash-recovery test:** six staged runs demonstrated the fix. Post-fix run 6 correctly skipped all 30 tickers from run 5 (stale count 5,562 → 5,532).

**Full market refresh remains unauthorized.** This session proved crash recovery on ~150 tickers across staging; prod remains untouched.

---

## Session Summary 2026-04-13 (Batch 2A) — fetch_market.py DirectWritePipeline

First real proof of the v1.2 pipeline framework against a canonical
table.

- **`scripts/fetch_market.py` rewrite (~750 lines):** implements `MarketDataPipeline(source_type="MARKET")` conforming to `DirectWritePipeline` structurally. `discover()` → `discover_market()`. `fetch()` writes one `ingestion_manifest` row per 100-ticker batch, calls `rate_limit('query1.finance.yahoo.com')` + `rate_limit('data.sec.gov')` on every HTTP call. `write_to_canonical()` upserts `market_data` with CHECKPOINT every 500 rows and writes one `ingestion_impacts` row per ticker. `validate_post_write()` runs a 85%/95% coverage gate (prod-only) plus sentinel gates (non-positive price/float, market_cap > 50T, stale rows). `stamp_freshness()` upserts `data_freshness`. `--dry-run` shows discovery, no writes. `--test` clips to 10 tickers and routes to staging.
- **Legacy holdings update removed:** the pre-rewrite `UPDATE holdings SET market_value_live/pct_of_float` block (targeting Stage-5-dropped `holdings`) is gone. Group 3 enrichment for `holdings_v2` is now a separate `enrich_holdings.py` planned for Batch 2B.
- **`scripts/pipeline/discover.py` bug fix:** `discover_market()` tripped on `if row.get("unfetchable"):` when the cell held `pd.NA`. Replaced with explicit `is True` check.
- **Test run (staging):** 10 tickers, 8.3s, 1 manifest + 10 impacts (8 loaded / 2 failed on exotic OTC tickers), 27.6 KB bytes, all promote_status=promoted, `data_freshness[market_data]` stamped.
- **Dry-run (prod):** 43,049-ticker universe, 6,424 stale prices / 382 stale metadata / 2,008 stale SEC, 428 batches, est. 12h at rate limits. No writes.
- **PROCESS_RULES violation list for fetch_market.py cleared:** §1, §2, §3, §4, §5, §6, §9 all addressed. `docs/pipeline_violations.md` row for `fetch_market.py` now stands as historical record of the pre-rewrite state.

**Next: Batch 2B** — authorized full market refresh (separate session, ~12h), then `enrich_holdings.py` for Group 3 post-promote.

---

## Session Summary 2026-04-13 (Batch 1) — Housekeeping + control plane to prod

Schema-only follow-up to the v1.2 framework foundation session.

- **T1 drop positions:** 18,682,708 rows removed from prod. `scripts/unify_positions.py` → `scripts/retired/`. EXPORT DATABASE backup taken first (2.1 GB at `data/backups/13f_backup_20260413_222950`).
- **T2 `build_summaries.py` DDL:** `summary_by_parent` CREATE updated from 9 → 13 columns with `PK (quarter, rollup_entity_id)` — matches prod exactly. Script not run. INSERT-side rewrite (swap `holdings` → `holdings_v2` + `fund_holdings_v2`) stays on the REWRITE list.
- **T3 migration 002 — skipped.** Direct PRAGMA inspection of prod showed `holdings_v2` / `fund_holdings_v2` / `beneficial_ownership_v2` already carry every column their owner scripts reference. Drift is owner-behind, not prod-missing; no ALTER TABLE migration can fix it. `docs/canonical_ddl.md` reclassified the three verdicts BROKEN → OWNER_BEHIND; `summary_by_parent` + `summary_by_ticker` BROKEN → ALIGNED after T2.
- **T4 migration 001 on prod:** 4 L0 tables live in prod (`ingestion_manifest`, `ingestion_impacts`, `pending_entity_resolution`, `data_freshness`) at 0 rows. `ingestion_manifest_current` VIEW present.
- **T5 docs reclassification:** see above. Added a Migration History section explaining why 002 was not created.
- **T6 `.gitignore`:** `PHASE*_PROMPT.md` and `data/*.csv` added. Two pre-existing untracked files (`PHASE1_PROMPT.md`, `data/uncategorized_institutions_funds.csv`) now correctly ignored.

Verification at close: 11 columns in each prod L3 fact table confirmed via PRAGMA; app healthy (6,511 tickers); smoke 8/8; pre-commit green.

---

## Session Summary 2026-04-13 — Pipeline framework foundation (v1.2)

Twelve-deliverable documentation + framework-scaffolding session. No
pipeline script ran; no canonical table was modified. Acceptance
criteria for rewrite work on 9 scripts that touch Stage-5-dropped
tables are now in place.

**Docs shipped:**
- `docs/data_layers.md` — complete table inventory classified into L0–L4 (every prod table assigned, 0 unclassified)
- `docs/canonical_ddl.md` — L3 + L4 DDL audit; 5 BROKEN total (3 L3: `holdings_v2`, `fund_holdings_v2`, `beneficial_ownership_v2`; 2 L4: `summary_by_parent`, `summary_by_ticker`) / 20 ALIGNED; blocks promote-script writing until each row's drift is resolved
- `docs/pipeline_inventory.md` — full script audit: REWRITE × 10, RETROFIT × 7, OK × 11, RETIRE × 13
- `docs/pipeline_violations.md` — per-script PROCESS_RULES detail with file:line for every violation

**Code shipped:**
- `scripts/migrations/001_pipeline_control_plane.py` — creates `ingestion_manifest`, `ingestion_impacts`, `pending_entity_resolution`, confirms `data_freshness`, view `ingestion_manifest_current`. Idempotent. Three separate sequences. `object_key` synthetic uniqueness. Sentinel pattern for partial-index gap in DuckDB 1.4.4.
- `scripts/pipeline/registry.py` — 52 DatasetSpec entries covering every prod table.
- `scripts/pipeline/protocol.py` — typing.Protocol definitions: SourcePipeline, DirectWritePipeline, DerivedPipeline + 6 supporting dataclasses.
- `scripts/pipeline/shared.py` — sec_fetch (5xx backoff + 429 60s pause), rate_limit (per-domain threading.Lock + monotonic token bucket), stamp_freshness, refresh_snapshot, entity_gate_check (3 hard blockers + 2 best-effort hints), write_manifest_row, write_impact_row, compute_object_key.
- `scripts/pipeline/manifest.py` — get_or_create_manifest_row, update_manifest_status, supersede_manifest, write_impact, update_impact_status, get_already_fetched, get_promotable_impacts.
- `scripts/pipeline/discover.py` — discover_13f (scrapes SEC form-13f landing page, no hardcoded ZIP URLs), discover_nport (accession-level, 75-day publication lag), discover_13dg (scoped to SCOPED_13DG_TEST_TICKERS = AR/OXY/EQT/NFLX), discover_market (staleness thresholds 7/30/90d).

**Bugs fixed (pre-commit green):**
- `scripts/api_market.py:201` — `has_table('fund_holdings')` → `has_table('fund_holdings_v2')` (Stage 5 follow-on).
- `scripts/build_benchmark_weights.py:16` — removed broken `from db import get_connection` import. Now uses inline `duckdb.connect(get_db_path())` at call site (the standard pattern used by every other script: `fetch_nport`, `fetch_13dg`, `fetch_market`, `fetch_ncen`, `fetch_finra_short`, `compute_flows`, `build_summaries`, `build_cusip`, `load_13f`, `build_shares_history`). `db.connect_write()` already exists for the named-function case — no new wrapper needed.

**Open decisions D5–D8** (documented in `docs/data_layers.md` §6, require operational data):
- D5 entity retro-enrichment path; D6 market_value_live refresh cadence; D7 snapshot retention; D8 L3 DDL migration framework.

**Critical finding:** 11 non-retired scripts still touch Stage-5-dropped
`holdings`/`fund_holdings`/`beneficial_ownership` tables (8 write, 3
read-only — see `docs/pipeline_inventory.md` cross-cutting finding #1
for the full list). They will fail on next run. Rewrite acceptance
criteria are the per-script violation lists in `docs/pipeline_violations.md`.

**Verification (all green):**
- Staging migration run clean — 0 rows in each new table on fresh install.
- Pre-commit (ruff + pylint + bandit) passes on all 7 new files and both bug-fix files.
- Smoke tests pass (8/8).
- App healthy: /api/v1/tickers returns 6,511 tickers.

---



## Session Summary 2026-04-10

**decision_maker_v1 rollup live:**
- 2,389 N-CEN sub-adviser routings applied (`ncen_sub_adviser` rule)
- 2,371 produce a different rollup than `economic_control_v1`
- All 541 unique sub-advisers resolved via CRD match (100% hit rate)

**DM8 intra-firm fix:**
- 621 routings collapsed back to brand parent (Fidelity HK/UK → Fidelity, DFA Australia → Dimensional, BlackRock Singapore → BlackRock, T. Rowe Price International → T. Rowe Price)
- Three-tier matching: shared economic rollup (485), entity_relationships parent (17), name-based brand matching (119)

**N-PORT coverage %:**
- `nport_coverage_pct`, `total_nport_aum`, `rollup_entity_id`, `rollup_name` columns added to summary_by_parent
- Q4 rebuilt from holdings_v2 + fund_holdings_v2 with rollup-based aggregation (8,417 rows)
- Top: Vanguard 100%, Fidelity 100%, Capital Group 100%, MFS 100%, Principal 100%
- BlackRock 61% structural (large institutional separate account AUM not captured in N-PORT — expected, not a data quality issue)
- Bottom: all hedge funds 0% (Citadel, DE Shaw, Jane Street, Susquehanna, Millennium, Point72)

**UI enhancements:**
- Global "Fund Sponsor / Voting" vs "Decision Maker" toggle in app header
- N-PORT Cov. badges on Register and Conviction tabs (≥80% green, 50-79% amber, 1-49% grey)
- L4 Fund Portfolio rollup context panel via new `/api/fund_rollup_context` endpoint

**L3 classification audit:**
- All 13 L3 entity_type categories reviewed, 900+ fixes applied
- wealth_management grew from 294 to 1,617 CIKs as hundreds of RIAs were correctly pulled out of active/mixed/PE
- L5 top 200 audited: Capital Group cleaned 46→8 CIKs (40 bogus "X Capital" entities extracted), Fidelity cleaned (FNF + Bancorp extracted)

---

## IN PROGRESS

| # | Item | Notes |
|---|------|-------|
| P1 | Full 13D/G fetch — `fetch_13dg.py` | Done | 60,135 filings parsed (99.8%). 112 remaining are HTTP 404s or regex-hang filings — not recoverable |
| P2 | `fetch_market.py` batch rewrite | Done | Batch yf.download(), persistent cache (7-day), upsert-only. --metadata-only mode, price-only fallback. Production tested |
| P3 | `compute_flows.py` set-based rewrite | Done | Single INSERT per period via window functions. 9.4M rows in 10s. Production tested |

---

## PIPELINE 1 — 13D/G Beneficial Ownership

| # | Item | Priority | Notes |
|---|------|----------|-------|
| 1 | Amendment tracking | Done | `rebuild_current` uses ROW_NUMBER by filing_date DESC + amendment_count column |
| 3 | Crossed 5% threshold flag | Done | `crossed_5pct` boolean in beneficial_ownership_current via first_13g CTE |
| 4 | Intent change tracking | Done | `prior_intent` column via LAG() window function |
| 5 | `pct_owned` null improvement — tune regex for null cases | Done | Added 4 new patterns for 13D cover page format (Row 11, multi-line). 13G: 95.7% hit rate. 13D: structural gap (cover page layout) — new patterns improve coverage |

---

## PIPELINE 8 — Short Interest (FINRA)

| # | Item | Priority | Notes |
|---|------|----------|-------|
| 9 | Short vs long comparison — same manager long 13F and short N-PORT | Done | `get_short_long_comparison()` in queries.py, `/api/short_long` endpoint. Matches 13F long parents to N-PORT short fund advisers |
| 10 | Short squeeze signal — high crowding + high short interest flag | Done | `get_short_squeeze_candidates()` in queries.py, `/api/short_squeeze` endpoint. Flags tickers with ≥15% short + high institutional ownership. Squeeze score = short_pct × inst/float |

---

## APP — Flow Analysis Tab

| # | Item | Priority | Notes |
|---|------|----------|-------|
| 11 | Chart.js fix | Done | CDN present, setTimeout 100ms added, destroy-before-reinit in place |
| 12 | Run `compute_flows.py` after full fetch completes | Done | 9.4M investor_flows rows, 19K ticker_flow_stats. 4 periods (4Q/3Q/2Q/1Q) |
| 13 | Refresh readonly snapshot | Done | Auto-refreshed after each pipeline run |

---

## APP — New Features (unlocked by short interest data)

| # | Item | Priority | Notes |
|---|------|----------|-------|
| 14 | Conviction tab — short interest column | Done | `short_pct` added to query3 results from short_interest table |
| 15 | Crowding tab — short crowding overlay | Done | `/api/crowding` endpoint, top holders + short history table |
| 16 | Smart Money tab — net exposure view | Done | `/api/smart_money` endpoint, long by type + N-PORT shorts |
| 17 | Activist tab — short interest context | Done | `short_interest` section added to query6 return data |
| 18 | New/Exits tab — short interest available | Done | Short data accessible via Crowding/Smart Money tabs |

---

## INFRASTRUCTURE — Performance & Reliability

Structure: open work appears in `### Open items` below. Historical
closed rows live in `### Closed items (log)` further down. New
follow-ups land in `### Open items` and move to the log when they
close.

**Allocation policy.** INF## IDs are allocated sequentially. The gap
at **INF19 / INF20 / INF21** is preserved intentionally — IDs jumped
from INF18 to INF22 during the 2026-04-16 session close (git history
shows no rows at those IDs on any branch). Treat as reserved / not
available for reuse; future confusion is avoided by leaving the gap.
Next free ID at allocation time is `max(in use) + 1`; verify via
`grep -oE 'INF[0-9]+' ROADMAP.md | sort -u` before claiming.

### Open items

| # | Item | Priority | Notes |
|---|------|----------|-------|
| INF25 | BLOCK-DENORM-RETIREMENT sequencing — retire denormalized v2 enrichment columns | Deferred to Phase 2 (int-09 2026-04-22) | **Decision: defer Step 4 to Phase 2.** Steps 1–3 done (BLOCK-TICKER-BACKFILL `3299a9f`; BLOCK-3 `0dc0d5d`; Batch 3 REWRITE queue closed 2026-04-19 — `d7ba1c2` / `87ee955` / `7e68cf9` / `223b4d9`). Forward hooks (int-06) bound drift on every `securities` write; urgency case is gone. Step 4 deferred because the read-site footprint in `scripts/queries.py` (405 `ticker` + 69 `entity_id` + 6 `rollup_entity_id` refs) is a dedicated project rather than a remediation-window task, and `rollup_entity_id` retirement requires an explicit dual-graph resolution decision. Exit criteria (mig-12 complete, read-site audit tool per mig-07/INF41, join pattern proven, dual-graph strategy chosen, drift gate stable ≥2 quarters, rename-sweep discipline applied) in `docs/findings/int-09-p0-findings.md §4`. Principle + sequence in `docs/data_layers.md §7`. Scope: `ticker`, `entity_id`, `rollup_entity_id`, `lei` on v2 fact tables — columns answering "what is the current mapping for this key" become joins, not stamps. |
| BLOCK-PCT-OF-FLOAT-PERIOD-ACCURACY | **CLOSED 2026-04-19** — Restore period-accurate denominator in `enrich_holdings.py` Pass B | Done | Shipped as the **pct-of-so** workstream (merged `8925347` + follow-on `12e172b`). Migration 008 renamed `holdings_v2.pct_of_float → pct_of_so`; added `pct_of_so_source VARCHAR` audit column; three-tier ASOF denominator in Pass B (`soh_period_accurate` → `market_data_so_latest` → `market_data_float_latest`). True float-adjusted denominator (public-float-based, distinct from shares-outstanding) tracked separately as **INF38 / BLOCK-FLOAT-HISTORY**. Context: `docs/REWRITE_PCT_OF_SO_PERIOD_ACCURACY_FINDINGS.md`. |
| INF34 | `queries.py` `rollup_type` filter missing on `summary_by_parent` / `investor_flows` reads | Low | **CLEARED 2026-04-19 (`62ad0eb`).** Explicit `WHERE rollup_type = 'economic_control_v1'` filters added to the EC-only read sites per Rewrite2 Phase 0 follow-up. Full log preserved in `### Closed items (log)`. |
| INF38 | BLOCK-FLOAT-HISTORY — true float-adjusted `pct_of_float` denominator | Low-Medium | New column distinct from `pct_of_so`: denominator is public float (excl. insider/affiliate holdings) rather than shares outstanding. Out of scope for the pct-of-so workstream that just shipped; captured for future. Context: `docs/REWRITE_PCT_OF_SO_PERIOD_ACCURACY_FINDINGS.md §14.10`. |
| INF39 | BLOCK-STAGING-PROD-SCHEMA-DIVERGENCE — pre-flight schema diff | Medium | **CLEARED 2026-04-19 (`f22312e`).** `scripts/pipeline/validate_schema_parity.py` + `config/schema_parity_accept.yaml` + `make schema-parity-check` pre-flight gate shipped. Context: `docs/BLOCK_SCHEMA_DIFF_FINDINGS.md`. Full log preserved in `### Closed items (log)`. |
| INF37 | `backfill_manager_types` residual — 9 entities / 14,368 rows without `manager_type` | Low / ongoing curation | After Rewrite5 Phase 4 prod apply (`7747af2`), 14,368 `holdings_v2` rows spread across 9 entities have `manager_type=NULL`. These entities are not in `data/reference/categorized_institutions_funds_v2.csv` and are not covered by ADV (`managers.strategy_type`). **Workflow:** when new institutions appear in `holdings_v2` without `manager_type`, add them to `categorized_institutions_funds_v2.csv` and re-run `backfill_manager_types.py` — that is the closure path. Not a regression; track as standing curation work that gets chipped at opportunistically (e.g., alongside L4/L5 audits). |
| INF27 | CUSIP coverage — residual-coverage tracking tier | STANDING / data quality | **Tracking tier documented in `docs/data_layers.md §11` (int-11-p1, 2026-04-22).** Original 6.2M-CUSIP gap closed by BLOCK-SECURITIES-DATA-AUDIT Phase 3 (132K→430K; current universe **430,149** CUSIPs on prod). Residual = (a) ~81 malformed CUSIPs from upstream ingest artifacts (upstream data-quality issue, not classifier bug; no AUM impact), (b) ongoing new-CUSIP flow from each quarterly 13F + monthly N-PORT ingest. **Pipeline handles automatically** via `build_classifications.py` + `run_openfigi_retry.py` — `cusip_retry_queue` is the monitoring surface; `status` distribution tracks resolution progress. No manual intervention required per cycle. Trigger condition for revisiting: net-increase in `pending` rows across two consecutive runs. Cross-refs: `docs/data_layers.md §11`, `docs/BLOCK_TICKER_BACKFILL_FINDINGS.md §10.1`, `docs/BLOCK_SECURITIES_DATA_AUDIT_FINDINGS.md`. Sibling item: **INF26** (`_update_error()` permanent-pending hygiene). |
| INF9 | Persist 2026-04-10 session fixes to entity_overrides_persistent | **Required before next `build_entities.py --reset`** | Manual fixes from the 2026-04-10 session live as direct rows in `entity_rollup_history` / `entity_relationships` / `entity_classification_history`, NOT in `entity_overrides_persistent`. A future `build_entities.py --reset` would wipe every fix. Replay gaps tracked across INF9a–INF9e (all closed 2026-04-12 as individual schema extensions). Remaining question is whether the full Route A row set has been written to `entity_overrides_persistent` in prod. Full history: see closed-items log below (originally filed pre-decomposition). |
| INF16 | INF7 follow-up — recompute `managers.aum_total` for two Soros CIKs | Low | Recompute `managers.aum_total` for CIK `0001029160` (currently holds VSS's $977M) and CIK `0001748240` (holds Orion's $257M). CRD links already broken so no downstream impact, but numbers are wrong. Same pattern as INF7 steps (g)/(h)/(h-bis). May be subsumed by INF17 batch cleanup. |
| INF2 | Monthly maintenance checklist | Recurring | First of each month: `validate_entities.py` (prod health check — expect 1 non-structural FAIL on `wellington_sub_advisory` until INF3), review `manual_routing_review` gate output for overdue routings, `diff_staging.py` to confirm staging matches production. Backups are NOT scheduled — see INF1 backup protocol. |
| 43e | Family office classification | Medium | Currently family offices are split between hedge_fund (Soros, Moore, ICONIQ — active style) and strategic (Longview, Briar Hall, Consulta, Allen — concentrated). A separate `family_office` manager_type would improve accuracy. Requires: curated list, ADV cross-reference (many register as exempt reporting advisers). |
| 43g | Drop redundant type columns — migrate queries | Medium | Two redundant columns to deprecate after full classification chain cleanup: (1) `fund_universe.is_actively_managed` → replace with `fund_strategy` (14 query refs); (2) `holdings_v2.manager_type` → replace with `entity_type` (65 remaining refs in SELECT/GROUP BY/display). Also update `build_managers.py` to write `entity_type` instead of `manager_type` on new data loads. Single pass after L1-L5 audit complete. |
| 43b | app.py remaining hardening | Low | B110: 8 try-except patterns now log but still swallow (review if any should surface errors). B603/B607: subprocess calls in admin endpoints — consider allowlist validation. B104: Flask bind `0.0.0.0` — restrict to localhost for production. |
| 48 | Phase 3.5 deferred items | Partially done | D1 ✅ (accuracy audit), D7 ✅ (oversized pass). Remaining: D2 (pymupdf recall 88→95%), D4 (match quality benchmark), D5 (IAPD API), D10 (admin UI), D11 (auto-promoter), D12 (ADV Section 7.B sub-adviser extraction for decision_maker_v1 coverage, +10–15% series), D13 (N-PORT sub-adviser metadata extraction from N-PORT XML). |
| 56 | Decision maker and voting rollup worldviews | Not started | DM1-DM7 — see `## DECISION MAKER & VOTING ROLLUP WORLDVIEWS` section below. Addresses sub-adviser policy issue surfaced in L5 audit (item 43i). |

### Closed items (log)

| # | Item | Priority | Notes |
|---|------|----------|-------|
| INF26 | OpenFIGI retry hygiene — `_update_error()` permanent-pending bug | **Done 2026-04-22** (int-10) | `scripts/run_openfigi_retry.py:185-208` `_update_error()` now flips `status='unmappable'` when `attempt_count >= MAX_ATTEMPTS` on hard errors. Staging sweep applied against existing `cusip_retry_queue`. PRs closed as part of conv-11. |
| INF28 | Schema constraint hygiene — PK/UNIQUE on `securities.cusip` + VALIDATOR_MAP | **Done 2026-04-22** (int-12, PR #95) | Migration 011 `securities_cusip_pk.py` applied prod + staging. Phase 0 findings at `docs/findings/int-12-p0-findings.md`. `VALIDATOR_MAP` placeholder resolved. |
| INF29 | Priceability refinement — OTC grey-market rows flagged `is_priceable=TRUE` | **Done 2026-04-22** (int-13, PR #97) | Migration 012 `securities_is_otc.py` added `is_otc VARCHAR` classifier + backfill script. Phase 0 findings at `docs/findings/int-13-p0-findings.md`. |
| INF30 | BLOCK-MERGE-UPSERT-MODE — NULL-only merge mode in `merge_staging.py` | **Done 2026-04-22** (int-14) | `scripts/merge_staging.py` gained NULL-only / column-scoped merge mode. Companion to `PROMOTE_KIND` split at promote_staging layer. |
| INF31 | BLOCK-MARKET-DATA-WRITER-CONVENTION — `fetch_date` / `last_write_ts` discipline | **Done 2026-04-22** (int-15, PR #90) | `scripts/refetch_missing_sectors.py:254` stamps `fetch_date` + `metadata_date` on UPDATE. Convention applied across every writer that mutates `market_data`. |
| INF32 | BLOCK-QUARTERLY-UPDATE-INTEGRITY — `quarterly-update` target missing 13F load step | **Done 2026-04-22** (obs-10) | `Makefile` `quarterly-update` target now invokes `load_13f.py`; retired-script references pruned from `scripts/update.py`. |
| INF33 | BLOCK-CI-ACTIONS-NODE20-DEPRECATION — GitHub Actions Node 24 migration | **Done 2026-04-22** (obs-12) | `.github/workflows/smoke.yml` + `lint.yml` upgraded to latest `actions/checkout`, `actions/setup-python`, `actions/cache` on Node 24 runtime. Pre-emptive of June 2026 forced cutover. |
| INF35 | f-string interpolation refinement in `build_summaries.py` | **Done 2026-04-22** (int-16) | `.format()` patterns converted to f-strings. Cosmetic hardening shipped. |
| INF36 | NULL `top10_*` placeholders in `summary_by_parent` | **Done 2026-04-22** (int-17, PR #99) | Migration 013 `drop_top10_columns.py` dropped 30 unused `top10_*` columns from `summary_by_parent`. Implicit-NULL convention eliminated by column removal. |
| INF40 | BLOCK-L3-SURROGATE-ROW-ID — stable surrogate PK for rollback replay | **Done 2026-04-22** (mig-06, PRs #103 #104) | Phase 0 findings (PR #103) scoped approach; migration 014 `surrogate_row_id.py` shipped `row_id BIGINT PK` on `holdings_v2`, `fund_holdings_v2`, `beneficial_ownership_v2`. Applied prod + staging in conv-11 data-ops-batch-2. |
| INF41 | BLOCK-READ-SITE-INVENTORY-DISCIPLINE — mechanically exhaustive rename sweep | **Done 2026-04-22** (mig-07, PR #101) | `scripts/audit_read_sites.py` shipped — Mode 1 on-demand terminal scanner for codebase read-sites. Discipline applied on every rename going forward. |
| INF42 | BLOCK-DERIVED-ARTIFACT-HYGIENE — stale dist/fixture detection | **Done 2026-04-22** (mig-08, PR #86) | `scripts/build_fixture.py` writes `_fixture_metadata` provenance row; CI staleness gate blocks stale React `dist/` or fixture DBs from merging. |
| INF45 | BLOCK-SCHEMA-DIFF-L4-EXTENSION — extend parity check to L4 tables + `entity_current` VIEW | **Done 2026-04-22** (mig-09) | `scripts/pipeline/validate_schema_parity.py` extended to L4 derived + `entity_current` VIEW. `make schema-parity-check --layer all` runs full coverage. |
| INF46 | BLOCK-SCHEMA-DIFF-L0-EXTENSION — extend parity check to L0 control-plane tables | **Done 2026-04-22** (mig-10) | Parity check extended to `ingestion_manifest`, `ingestion_impacts`, `pending_entity_resolution`, `data_freshness`, `schema_versions`. |
| INF47 | BLOCK-SCHEMA-DIFF-CI-WIRING — `validate_schema_parity.py --json` in smoke CI | **Done 2026-04-22** (mig-11) | CI pytest scope widened; parity check wired into smoke workflow. |
| INF3 | Fix wellington_sub_advisory validation gate | Done 2026-04-10 | Scoped check (a) of `gate_wellington_sub_advisory` to `rollup_type='economic_control_v1'` only. Under decision_maker_v1, routing fund series to Wellington is correct DM1 behavior. Pre-fix: 122 fund_rollups + 78 "without ncen adviser role" issues / FAIL. Post-fix: PASS. Code-only change, no data mutation. Commit 76da81d. |
| INF22 | `entity_overrides_persistent` override_id sequence + NOT NULL migration | **Done 2026-04-17** (`ffccb92`) | Schema-level fix for the NULL-PK bug that `_heal_override_ids` was patching at runtime. `scripts/migrations/006_override_id_sequence.py` (new, ~200 lines) creates `override_id_seq` starting from `MAX(override_id)+1`, adds `DEFAULT nextval('override_id_seq')` + `NOT NULL` on `entity_overrides_persistent.override_id`. Idempotent — probes for sequence + column state before mutating. Ran on staging + prod. **Prod data-side** (58 NULL rows → 25-82) already healed 2026-04-16 via DM14 promote side-effect (`d684e4e`). Full prod state after both fixes: 90 overrides, 0 NULL, DEFAULT/NOT NULL enforced. Runtime MAX+1 helpers in `promote_staging.py` / `admin_bp.py` / `scripts/dm14_layer1_apply.py` are now redundant backstops — left in place as belt-and-suspenders, harmless. |
| INF24 | Market refresh operational close | **Done 2026-04-17** | `fetch_market.py` (PID 78767, launched 2026-04-16) completed overnight on the full 81-batch run. `_next_id` (commit `11a35e9`) held through the full pipeline — no PK collisions. **Prod state:** `market_data` 10,064 rows, `last_loaded_at` 2026-04-16 23:27 UTC. `enrich_holdings.py --fund-holdings` re-ran against fresh prices: `holdings_v2.market_value_live` **+445K rows**, `holdings_v2.pct_of_float` **+127K rows**, `fund_holdings_v2.ticker` **+488K rows**. `build_summaries.py --rebuild` re-ran with the fresh mvl. `make freshness` now **PASS** — all 7 critical tables OK (`holdings_v2`, `fund_holdings_v2`, `investor_flows`, `ticker_flow_stats`, `summary_by_parent`, `beneficial_ownership_current`, `market_data`). No outstanding follow-ups. |
| INF5 | Fix `_verify_adv_relationship()` parent-not-in-ADV silent skip | Done 2026-04-10 | Discovered 2026-04-10 during L5 batch 1 audit. Tier 1 (AUM sanity) + Tier 2 (state match) in `entity_sync._verify_adv_relationship()` only fired when both parent and child had ADV rows. When the parent had no `adv_managers` row at all (legacy managers-feed entities never registered as advisers), both tiers silently skipped and the function returned PASS. Test: 5 of 6 known-bad ADV mis-matches in L5 batch 1 (AGF→Ally Bridge, Cairn→CI Investments, Americana→&Partners, Edgewood→GoodHaven, Huntington→Linscomb) passed. Fix: new Tier 1a check — if `_lookup_adv_row(parent)` returns None, return `(False, 'low', 'tier1_parent_not_in_adv_managers')`. Legit non-adviser parents (individuals, trusts, pure holdcos) now route to `entity_relationships_staging` for manual review rather than silently inserting. Smoke-tested against prod: eid=162 (registered adviser) passes, eid=342 (no ADV row) fails with expected reason. Code-only change; no data mutation. |
| L4-1 | Mixed classification review — bank holdcos vs pure asset managers | Done 2026-04-12 | _Staging workflow required (INF1)._ Discovered 2026-04-10 during Section 3 L4 audit. The `mixed` classification (1,037 entities) is legitimately used for diversified financial holding companies filing 13F at the consolidated holdco level (JPMorgan Chase, Bank of America, Goldman Sachs Group, UBS, RBC, BNY Mellon Corp, Wells Fargo, Barclays, Deutsche Bank, BMO, Citigroup, BNP Paribas — all top-20 by AUM are major global banks). However, **3 entities in the top 20 are pure asset managers mis-classified as mixed and should be `active`: Franklin Templeton ($408B), PIMCO (~$26B in 13F + N-PORT), Nomura (Japanese bank but the AM arm is 100% active per N-PORT)**. Review the full 1,037-entity population for similar cases — likely 10-30 more pure asset managers hiding under `mixed` because they were mass-imported with `confidence='fuzzy_match'` source='managers'. Of 1,037 mixed entities, only 11 have ≥3 N-PORT series children — those 11 are the highest-priority review targets (start with the ones whose N-PORT series are >90% active, indicating they're really active managers not bank holdcos). |
| L4-2 | N-PORT cross-check classification mismatches | Done 2026-04-12 | _Staging workflow required (INF1)._ Discovered 2026-04-10 during Section 3 sub-section 3d cross-check. 3 parents with classification that contradicts their N-PORT series children's active/passive split: **(1) Invesco ($352.5B, classified `passive` but 96.7% of N-PORT series AUM is active across 89 series)** — Invesco has some passive ETFs (legacy PowerShares) but the vast majority of AUM is active management. Should be `mixed` or `active`. **(2) Nuveen / TIAA ($292.2B, classified `passive` but 90.1% active across 28 series)** — Nuveen is primarily an active manager (TIAA-CREF asset management). Should be `mixed` or `active`. **(3) Security Investors LLC ($6.9B, classified `active` but 80.9% passive across 84 series)** — should be `passive` or `mixed`. Fix all 3 classifications. |
| INF9a | Extend replay_persistent_overrides for `is_activist` flag | Done 2026-04-12 | _Follow-up to INF9 Route A._ `replay_persistent_overrides()` in `build_entities.py:748-761` hard-codes `is_activist=FALSE` when replaying a `reclassify` action. The 2 Section 3 L4 activist flag fixes from Apr 10 (Mantle Ridge LP `is_activist=FALSE → TRUE`, Triangle Securities Wealth `TRUE → FALSE`) cannot be persisted through the current replay contract. Options: (a) extend the `reclassify` action to read an optional `is_activist` value from a new column on the override row; (b) add a new action type `set_activist` with a BOOLEAN `new_value`. Schema change required on `entity_overrides_persistent`. Until this lands, these 2 flag fixes live in `entity_classification_history` and would be reverted by `--reset`. Affected entities are small by AUM but the activist flag is a user-visible badge, so a `--reset` must not silently reset them. |
| INF9b | Extend entity_overrides_persistent with `rollup_type` for decision_maker_v1 targets | Done 2026-04-12 | _Follow-up to INF9 Route A._ `replay_persistent_overrides()` hard-codes `rollup_type='economic_control_v1'` when replaying a `merge` action (line 794). The Apr 10 DM12 narrow routing fixes (Securian/Sterling false ADV match deletion + HC Capital Trust 7 sub-adviser routings + CRI 5 sub-adviser routings = 13 actions) re-routed fund series under `decision_maker_v1`, not `economic_control_v1`. A replayed `merge` row would write to the wrong worldview. Schema change: add `rollup_type VARCHAR DEFAULT 'economic_control_v1'` to `entity_overrides_persistent`, and update the replay to read it. Until this lands, DM12 fixes live in `entity_rollup_history` rows and would be wiped by `--reset`. |
| INF9c | Suppress/delete bad parent_bridge fuzzy-match relationships across `--reset` | Done 2026-04-12 | _Follow-up to INF9 Route A._ Of the 28 relationship deletions from L5 batches 1-6 (Apr 10), a subset was ADV-sourced and is now prevented from re-entering the graph by INF5's Tier 1a verifier (commit `3e2a9fc`). The remainder was `parent_bridge` fuzzy-match sourced; the `parent_bridge` loader in `build_entities.py` has no equivalent verifier, so those bad edges would be silently re-wired by the fuzzy matcher on every `--reset`. Options: (a) add a `delete_relationship` / `suppress_edge` action to `replay_persistent_overrides()` that takes `(parent_cik, child_cik, relationship_type)` and leaves a tombstone, and have the `parent_bridge` loader check the tombstone before inserting; (b) port the ADV-style Tier 1 (AUM sanity) + Tier 2 (state match) verifier to the `parent_bridge` loader. Option (b) is structurally cleaner but requires teaching the fuzzy matcher to respect the same AUM ratio. Until this lands, the pre-INF5 `parent_bridge` subset of the 28 L5 deletions would be re-inserted on `--reset`. INF9 Route A needs to identify how many of the 28 are in this parent_bridge subset — that count becomes INF9c's scope. **Code framework shipped 2026-04-12 (`a0d6685`):** `suppress_relationship` action + `relationship_context` JSON column are ready. **Data population status:** the original L5 deletion list is NOT recoverable from the current DB state — the Apr-10 closures were either hard-DELETEd or overwritten by the 100+ entity merges in INF4c/INF4d/INF6/INF8 batch work (2026-04-12). Populating the `suppress_relationship` override rows requires a **fresh L5 parent_bridge audit** to identify which fuzzy-match relationships are still bad in the current entity graph. The INF5 Tier 1a verifier (`3e2a9fc`) already backstops the ADV-sourced subset; only the parent_bridge subset remains exposed. **Data populated 2026-04-12:** fresh L5 audit found 496 non-self parent_bridge relationships, 29 SUSPECT (zero brand-token overlap), 6 CONFIRMED_BAD (PIMCO→Mountain Pacific, Capital Group→Potrero, Capital Group→Barington, Point72→SoundStone, Parametric→Parametrica ×2). All 6 closed via INF1 workflow + 6 `suppress_relationship` override rows written to staging+prod. Commit `b543030`, snapshot `20260412_110029`. **Known limitation:** the 6 `suppress_relationship` override rows have NULL `parent_cik` because the parent entities (PIMCO, Capital Group, Point72, Parametric) are PARENT_SEEDS brand ghosts with no CIK in `entity_identifiers`. The `suppress_relationship` replay in `replay_persistent_overrides()` uses `parent_cik` to resolve the parent edge and will **skip these 6 rows on `--reset`**. Primary protection is the direct relationship closure in prod. On next `--reset`, these 6 edges may be re-created by the `parent_bridge` fuzzy matcher. **Follow-up shipped 2026-04-12:** `suppress_relationship` replay extended with entity_id fallback — priority ladder: (1) parent_cik+child_cik via entity_identifiers, (2) `parent_entity_id`+`child_entity_id` from relationship_context JSON (for PARENT_SEEDS ghosts). 6 existing override rows backfilled with entity_ids (PIMCO eid=30, Capital Group eid=12 ×2, Point72 eid=39, Parametric eid=90 ×2). entity_ids for PARENT_SEEDS are deterministic in practice (processed in seed list order) but not guaranteed by contract — fallback is best-effort across full `--reset`. |
| INF9d | CIK-less entities can't be reached by `replay_persistent_overrides()` | Done 2026-04-12 (closed as won't fix for orphans) | _Discovered 2026-04-11 during INF9 Route A row survey._ Four Apr-10 Section 3 L4 classification fixes target entities with NO `entity_identifiers` row of type `cik`. The replay function resolves overrides by CIK (line 737-740), so any override row for these entities would be logged as `"override N: CIK (none) not found, skipping"`. Affected entities: **(1) Pacific Life Insurance Company (eid=20194, insurance → pension_insurance)** — no feeder backing, only `entity_aliases` row with source=`adv_manual`; **(2) Stowers Institute for Medical Research (eid=20196, foundation → endowment_foundation)** — same; **(3) Stonegate Global Financial (eid=20201, holding_company → strategic)** — same; **(4) International Assets Advisory, LLC (eid=20203, holding_company → wealth_management)** — has `adv_managers` row via CRD 10645 (no CIK), would be re-created on `--reset` with a new entity_id via the ADV CRD path. Entities (1)(2)(3) are orphan: they exist only because someone manually created them during the Apr 10 session and will be dropped wholesale by `--reset` — no override row can rescue them. Entity (4) needs either a new `entity_crd` column on `entity_overrides_persistent` OR an identifier_type parameter on the override row so the replay can resolve via CRD instead of CIK. Options: (a) extend the override schema with `identifier_type VARCHAR NOT NULL DEFAULT 'cik'` and `identifier_value VARCHAR NOT NULL` (replacing `entity_cik`), update the replay to walk `entity_identifiers` with the stored type/value pair; (b) for the 3 orphan entities, add them to a curated `manual_entities_preserve` list that `build_entities.py` re-creates after `--reset` before replaying overrides; (c) accept the limitation and track the 4 affected classifications out-of-band until the next `--reset` requires them. Until this lands, 4 of the 28 replayable-in-principle rows are not actually persistable via the current schema. **Schema fix shipped 2026-04-12 (`a0d6685`):** `identifier_type` + `identifier_value` columns added — entity (4) International Assets Advisory (CRD-only) is now resolvable via `identifier_type='crd'`. **Orphan entities closed as won't fix:** Verified 2026-04-12 — 0 identifiers, 0 holdings_v2/fund_holdings_v2 refs. No analytical impact on `--reset`. `manual_entities_preserve` mechanism not needed. **Correction 2026-04-13:** Stage 5 pre-delete verification found these are NOT orphans — each has 1 brand alias, 2 rollup_history rows (self-root EC+DM), fresh manual_l4 classification edits from 2026-04-10, and 1 outgoing `wholly_owned` ADV_SCHEDULE_MANUAL relationship to a real child entity (20194→1685 Pacific Life Fund Advisors, 20196→8544 American Century, 20201→9990 Catalyst Capital, 20203→2196 International Assets Investment Mgmt). Re-closed as won't-fix for deletion — these are live PARENT_SEEDS brand shells with ADV lineage. Removed from Stage 5 cleanup list. |
| INF9e | Extend staging workflow to cover `entity_overrides_persistent` | Done 2026-04-12 | _Discovered 2026-04-11 after applying INF9 Route A._ Extend staging workflow to cover `entity_overrides_persistent`: (1) create table in prod via DDL migration from `entity_schema.sql`, (2) extend `diff_staging.py` to diff the overrides table, (3) extend `promote_staging.py` to sync it. Required before the 24 INF9 Route A override rows can reach prod. Staging holds the rows safely in the interim. |
| INF10 | `merge_staging.py --all` silently full-replaces non-whitelisted prod tables | Done 2026-04-10 | _Code-only change, no staging workflow needed._ Discovered 2026-04-10 via external review. Pre-fix, `run_pipeline.sh` ended with `merge_staging.py --all --drop-staging`, which resolved `--all` to every table currently in staging — including reference tables (`holdings`, `managers`, `fund_holdings`, `market_data`, `adv_managers`, `filings`, `securities`, `parent_bridge`) seeded from prod, plus leftover entity tables from any prior `sync_staging.py` run. For tables not in `TABLE_KEYS`, the merge path did `DROP TABLE IF EXISTS` + `CREATE TABLE AS SELECT`, silently reverting any concurrent prod writes between seed-time and merge-time and promoting in-progress entity staging edits to prod bypassing the INF1 workflow. Additionally `TABLE_KEYS["fund_holdings"] = None` had a comment claiming "append new rows" which was false — `pk_cols=None` also hits the drop+recreate branch. Fix: (1) `run_pipeline.sh` now names the six 13D/G output tables explicitly (`beneficial_ownership`, `beneficial_ownership_current`, `fetched_tickers_13dg`, `listed_filings_13dg`, `short_interest`, `ncen_adviser_map`) with a comment warning against reverting to `--all`; (2) `merge_staging.py --all` now refuses to run unless `--i-really-mean-all` is also passed (bypassed only by `--dry-run` for inspection); (3) fixed the `fund_holdings` / `ncen_adviser_map` / `beneficial_ownership_current` TABLE_KEYS comments to match actual behavior + added a header comment clarifying that `pk_cols=None` means full-replacement; (4) updated module docstring with an IMPORTANT note steering callers to `--tables`. Smoke-tested all four argparse branches: bare `--all` errors cleanly, `--all --dry-run` bypasses, `--tables X --dry-run` parses and runs, `--all --i-really-mean-all --dry-run` runs. |
| INF11 | `sync_staging.py --tables` drops all entity tables unconditionally | Done 2026-04-10 | _Code-only change._ Discovered 2026-04-10 via external review. Pre-fix, `sync_staging.py` unconditionally iterated `reversed(db.ENTITY_TABLES)` and dropped every entity table before the CTAS loop rebuilt only the subset named in `--tables`. An operator running `sync_staging.py --tables entity_aliases` would silently destroy `entity_relationships`, `entity_classification_history`, `entity_rollup_history`, and the staging review queues. Fix: (a) drop only the tables that are about to be re-CTAS'd (`for table in reversed(tables):`) — FK drop-order is irrelevant because CTAS-created staging tables have no foreign-key constraints; (b) emit an explicit "PARTIAL SYNC" log entry listing dropped vs untouched tables so the operator sees the scope. Smoke-tested: inserted a marker row in `entity_relationships`, ran `sync_staging.py --tables entity_aliases`, confirmed the marker row survived (pre-fix it would have been wiped). |
| INF12 | Unauthenticated mutating/admin endpoints on deployable Flask app | Done 2026-04-11 | `d51db60`. New `scripts/admin_bp.py` Flask Blueprint mounted at `/api/admin/` with a `@admin_bp.before_request` token guard: 503 "Admin disabled" when `ENABLE_ADMIN != 1` or `ADMIN_TOKEN` is unset, 403 "Forbidden" when `X-Admin-Token` header is missing or wrong (`hmac.compare_digest` for timing-safe compare). Moved 15 routes onto admin_bp: `add_ticker`, `stats`, `progress`, `errors`, `run_script`, `manager_changes`, `ticker_changes`, `parent_mapping_health`, `stale_data`, `merger_signals`, `new_companies`, `data_quality`, `staging_preview`, `running`, `entity_override`. `POST /api/add_ticker` renamed to `POST /api/admin/add_ticker` (only caller was `admin.html`, updated in same commit). `run_pipeline.sh` and `merge_staging.py` dropped from the `/api/admin/run_script` allow-list — never web-triggerable, run manually only. **Exception:** `/api/admin/quarter_config` stays on the main app because the public main UI (`app.js` QuarterSelector + `QuarterSelector.tsx`) fetches it on every page load to populate the quarter dropdown — gating would break the public site. `admin.html` gained an `adminFetch()` helper that prompts for the token on first load, stores it in `localStorage` under `admin_token`, and injects `X-Admin-Token` on every admin call (clears on 403, surfaces 503 as "Admin disabled on server"). Bind unchanged (`0.0.0.0` for Render); auth happens at the Blueprint layer. INF12 comment added near `app = Flask(...)` documenting the env-var contract. Smoke-tested two local servers: port 8001 with no env → 503 on `/api/admin/stats`; port 8002 with `ENABLE_ADMIN=1 ADMIN_TOKEN=test123` → 403 without header, 200 with correct header, 200 on ungated `/api/admin/quarter_config`. `pre-commit run pylint bandit` on `app.py` + `admin_bp.py`: clean. Dropped the proposed "bind to 127.0.0.1 by default" step — not needed since auth is enforced at Blueprint level and Render deployment requires 0.0.0.0. |
| INF13 | `shutil.copy2` on live DuckDB file in snapshot fallback | Done 2026-04-12 | _Code-only change._ Discovered 2026-04-10 via external review. `scripts/app.py:99` in `_resolve_db_path()` executes `shutil.copy2(DB_PATH, tmp_path)` + `os.replace()` when `duckdb.connect(DB_PATH, read_only=True)` raises (i.e. a writer holds an exclusive lock). The rename is atomic but the file read happens while the writer is active; DuckDB uses page-level writes with a WAL, so the file can be internally inconsistent (torn pages, header pointing at unwritten checkpoints, WAL out of sync). Exposure is narrow — the fallback only triggers when the read-only connect raises, and the switchback monitor replaces the snapshot as soon as the writer releases — but the bad-snapshot window is real and, by construction, opens precisely when the main DB is under concurrent write. Fix: use DuckDB `EXPORT DATABASE` via an already-open writer connection (or via a separate read connection with a `CHECKPOINT` first), or refuse to serve if no pre-validated snapshot from `scripts/refresh_snapshot.sh` exists rather than creating one in the hot path. **Verified 2026-04-12:** `_resolve_db_path()` at lines 86-120 already has fail-fast RuntimeError when main DB is locked and no snapshot exists. The `shutil.copy2` was removed in a prior session (folded into the INF14 commit `f272570` cleanup). Current code: try read-only connect → if locked, check snapshot → if exists return it, if not raise RuntimeError. No code change needed. |
| INF14 | Pay down pre-commit pylint/bandit wall on app.py + queries.py | Done | f272570. 39 pylint + 13 pylint + 33 bandit issues → 0. W1203 logger f-strings, W1514 open encoding, W1510 subprocess check=False, W0107 unnecessary pass, W0603/W0404/W0212/E0401 inline disable comments, B110/B112/B404/B603/B607/B104/B113 nosec annotations. 74 line-for-line replacements, zero logic changes. Pre-commit runs clean. Note: bandit 1.8.3 (pre-commit pin) requires bare # nosec on multi-code B603+B607 lines — revert to explicit codes when pin advances to 1.8.6+. Preexisting breakage flagged: /api/admin/manager_changes HTTP 500 at HEAD pre-INF14, unrelated, tracked separately. |
| INF15 | Fix /api/admin/manager_changes HTTP 500 | Done | 794c81e. Root cause: # nosec B608 placed on con.execute(f""" line, injecting comment into SQL string. DuckDB errors on # character. Same bug as 26 other endpoints. Fixed in same commit as autocomplete root cause. Closes as duplicate of B608 nosec placement fix. |
| BUG | Fund Portfolio broken — queries.py:1815 query7 WHERE clause missing f-prefix on f-string, {LQ} never substitutes, all calls return 404. One-char fix (add f before the quote). | Done 2026-04-12 | _Discovered 2026-04-11 during Phase 2 endpoint type audit._ `/api/query7` (the Fund Portfolio drilldown) returns `"No holdings found for CIK ..."` 404 for every valid CIK. Root cause: `where = "h.cik = ? AND h.quarter = '{LQ}'"` at `queries.py:1815` is a plain string, not an f-string, so `{LQ}` is passed to DuckDB as a literal and matches no quarter. One-character fix: add `f` prefix. The outer SQL is already an f-string, so the substituted `{where}` carries the unsubstituted `{LQ}` through untouched. Verify the fix by hitting `/api/query7?ticker=EQT&cik=0000902219` and confirming a non-empty `positions` array. |
| INF17 | Systematic managers-table CRD pollution cleanup | **All phases Done 2026-04-12** | _Discovered 2026-04-11 via post-INF7 audit._ INF7 was not a one-off. Audit found **127 manager rows (8.4% of CRD-linked rows) = 114 unique (cik, crd) pairs after dedup** where `managers.crd_number` points to a different real-world firm than `managers.manager_name`. **All 114 also have `managers.aum_total` exactly equal to the wrong `adv_managers.adv_5f_raum` (100% overlap with the INF7 fingerprint) — confirms a mechanical upstream bug.** **Root cause located:** `scripts/build_managers.py` → `link_cik_to_crd()` (lines 270–292) uses `rapidfuzz.fuzz.token_sort_ratio` with `score_cutoff=85`. Token-sort-ratio gives high scores to pairs sharing corporate-suffix tokens ("Financial LLC", "Capital Management LLC") even when distinguishing brand words differ completely — same failure mode as pg_trgm `similarity()` and `jaro_winkler`. Phase 2 of `build_managers_table()` (line 377) then copies `a.adv_5f_raum as aum_total` via a LEFT JOIN on the bad CRD, stamping the wrong firm's RAUM onto the row. Secondary contaminator: `fetch_ncen.py:328` uses the same `>=85` token_sort_ratio threshold for the `adviser_cik` column (staging-only, not in prod, but a live hazard). **Downstream impact confirmed:** all 114 bad CRDs landed on `entity_identifiers` with `source='managers'` (100% propagation), and **16 entities have active non-self rollups** driven by the bad CRD (11 via `wholly_owned`/`ADV_SCHEDULE_A`, 3 via `fund_sponsor`/`N-CEN`, 2 via `orphan_scan`, 3 via `parent_bridge_sync`/`manual`). **BUT — spot-checks of 4 entities (Geode, Carillon, Nikko Japan, Nikko Europe) revealed all 4 had coincidentally-correct or legitimately-correct rollup targets.** Geode→brand ghost eid=89 accidentally correct (real Geode CIK 0001214717 also rolls there carrying the real $5.89T book via PARENT_SEEDS); Carillon→Raymond James Financial structurally correct (RJF is the real legal parent — Carillon was spun up as a RJF subsidiary in 2016); Nikko Japan→Sumitomo Mitsui Trust structurally correct (Sumitomo acquired Nikko AM in 2009 and remains majority owner); Nikko Europe→Nikko Japan plausible intra-group routing. **All 4 were 13F-NT filers with zero holdings** in `holdings_v2`. Step 2 audit's "$180.71B at risk" estimate is likely dramatically overstated; needs confirmation via batch verification of the remaining 12 entities. Four CIKs are `manually_verified=True` in the managers table despite carrying pollution — flag is unreliable and should be reset during Phase 1. **Revised four-phase plan:** **Phase 1** — scrub bad CRDs from `managers.crd_number` + `managers.aum_total` for all ~114 affected CIKs (reference-table only, no entity-layer touch). Safe pass that removes upstream pollution without disturbing rollups that may be coincidentally correct. Flows through `merge_staging.py --tables managers`, same as INF7 step (g)/(h)/(h-bis). **Phase 2** — per-entity verification of the 16 entities with active ADV rollups. **COMPLETED 2026-04-11** via 4 spot-checks (Geode, Carillon, Nikko Japan, Nikko Europe) + batch verification of the remaining 12. **Fix APPLIED 2026-04-11 — snapshot `20260411_214440`.** 5 entities self-rooted + 5 bad CRDs closed via INF1 staging workflow (sync → edit → diff → validate --staging (9/0/7) → promote --approved → validate --prod (9/0/7)). Rollback path: `python3 scripts/promote_staging.py --rollback 20260411_214440`. Transaction row counts: 10 rollups closed + 10 self-root rollups inserted + 5 CRD identifiers closed = 25 row operations. diff_staging.py line-level changes: 10 rollup retargets + 5 identifier deletes = 15. Original fix list (all applied): (i) **eid=2195 7G Capital Management LLC** ($161M Q4, 13F-HR, bad CRD 285013=AIGH Capital, currently rolling to ARS Investment Partners via `wholly_owned/ADV_SCHEDULE_A` — unrelated firms); (ii) **eid=6285 Capital Insight Partners LLC** ($473M Q4, 13F-HR, bad CRD 133738=Indus Capital Partners, currently rolling to Indus Capital Partners LLC via `wholly_owned/ADV_SCHEDULE_A` — unrelated Dallas vs NY firms); (iii) **eid=7606 Compton Financial Group LLC** ($442M Q4, 13F-HR, bad CRD 166912=Red Oak Financial, currently rolling to Calton & Associates via `wholly_owned/ADV_SCHEDULE_A` — no real-world basis); (iv) **eid=10514 IFS Advisors LLC** ($192M Q4, 13F-HR, bad CRD 147518=Insight Financial Services, currently rolling to Parker Financial LLC via `wholly_owned/ADV_SCHEDULE_A` — unrelated); (v) **eid=6267 Scion Capital Group LLC** ($0 Q4, 13F-NT, bad CRD 157798=Siris Capital Group, currently rolling to Capital Group / American Funds via `fund_sponsor/N-CEN` — Scion is Michael Burry's firm, rollup landed at Capital Group via substring match on "Capital Group" — visible data quality embarrassment in the entity graph). **Excluded from INF17:** (a) **eid=3559 IMA Advisory Services Inc.** — CRD 112091 is legitimately IMA's own CRD (adv_managers firm_name matches); the Step 2 downstream query over-flagged it by joining on CRD alone instead of `(CIK, CRD)` pair. Its rollup to AJ Advisors is from a real ADV Schedule A walk — separate data quality question whether AJ Advisors is IMA's real parent, but not an INF17 case. (b) **eid=830 + eid=4248 Amundi US + Amundi Asset Management** — both roll to `eid=752 Amundi Taiwan Ltd.` via `parent_bridge_sync/manual`, which is a structurally odd target (Amundi Taiwan is a regional sub, not a global parent). Not CRD-driven — the manual rollups would survive Phase 1 CRD scrub unchanged. Flag as a separate review item, possibly L4-classification work. **Leave alone (rollups are correct independent of CRD pollution, Phase 1 CRD scrub sufficient — no Phase 2 action):** eid=821 AEW Capital Management (orphan_scan fragmentation collapse under AEW L.P.), eid=2995 Martin Currie IM (wholly_owned to Martin Currie Ltd — real corporate parent chain), eid=5040 Centre Asset Management (orphan_scan fragmentation collapse), eid=7719 Baird Financial Corp (accidentally-correct N-CEN match to the real Baird). **Plus spot-checked legitimately-correct cases from earlier this session:** eid=152 Carillon Tower Advisers → Raymond James Financial (real parent), eid=8478 Nikko Japan → Sumitomo Mitsui Trust (real parent), eid=7579 Nikko Europe → Nikko Japan (plausible intra-group), eid=9840 Geode Capital Holdings → brand ghost eid=89 (accidentally correct co-grouping with real Geode). **Confirmed AUM at risk: ~$1.27B** (7G $161M + Capital Insight $473M + Compton $442M + IFS $192M + Scion $0) — the Step 2 "$180.71B" estimate was inflated by two factors: most affected entities are 13F-NT notice filers with zero direct holdings, and most non-self rollups driven by bad CRDs happened to land at correct or fragmentation-collapsed targets by coincidence. **Phase 3** — upstream fix: replace `fuzz.token_sort_ratio` + `score_cutoff=85` in `build_managers.py:276` with a stopword-aware brand-token matcher. **DONE 2026-04-11 (commit `0634682`).** Added module-level `_BRAND_STOPWORDS` frozenset (21 words: llc lp inc ltd co corp fund capital management advisors partners holdings group the and of asset investment financial services wealth), `_brand_tokens()` tokenizer (lowercase, split on non-alphanumeric, drop stopwords + tokens < 3 chars), `_brand_tokens_overlap()` (Jaccard > 0 check), and `_city_state_compatible()` (accept on missing, reject only on populated disagreement). Rewrote the fuzzy loop in `link_cik_to_crd()` to apply Gate (a) brand-token overlap and Gate (b) city+state compatibility between `process.extractOne()` and `fuzzy_matches.append()`. Rejected candidates are logged to `logs/build_managers_rejected_crds.csv` with columns `(manager_cik, manager_name, candidate_crd, candidate_firm, score, reason)` where reason ∈ `{brand_token_mismatch, city_state_mismatch}`. Dropped dead `batch_size = 500` variable. Added pre-existing `# nosec B110` annotation on the unrelated `ALTER TABLE holdings...except: pass` block (not INF17-related, just needed to keep pre-commit clean). Pre-commit ruff + pylint + bandit all clean. **Pipeline not re-run.** Without a run, existing `cik_crd_links` table still contains the 114 pre-fix bad matches — Phase 1's blanket scrub is still needed. The fix only prevents FUTURE pollutions on the next `--reset`. **Secondary contaminator still needed:** `fetch_ncen.py:328` has the same bug for `adviser_cik` column — tracked as **INF17b** (new follow-up, not in this commit). Without Phase 3, any future `build_entities.py --reset` re-introduces the entire population of bad CRDs. **Phase 4** — preserve coincidentally-correct rollups via `parent_bridge_sync` manual writes BEFORE scrubbing their CRD identifiers (else the ADV Schedule A chain that currently lands at the right parent breaks on `--reset` and the legitimate rollup is lost). Requires a `preserve_relationship` action in `replay_persistent_overrides()` — same schema gap as INF9c, so these two items should land together. Also needed: sweep the 20 "unknown" rows (empty brand tokens after stopword removal) that the audit heuristic couldn't evaluate. **Note:** all 4 spot-checked entities filed 13F-NT with zero holdings. Confirm in Phase 2 batch verification whether this generalizes — if most/all 16 are 13F-NT, the true holdings-level AUM at risk is near zero and INF17's urgency is primarily about preventing recurrence (Phase 3), not about active misattribution. **Phase 1 also needs to address `parent_name` column pollution:** two managers rows with `parent_name='Trian Fund Management'` are fuzzy_match mis-parenting — CIK `0001541353` Triangle Securities Wealth Management (Raleigh NC, $1.29B) and CIK `0001779763` Iron Triangle Partners LP (Cos Cob CT, $0.87B). Neither is a Trian entity. Discovered during INF8 merge investigation (2026-04-12). Fix: `UPDATE managers SET parent_name=NULL WHERE cik IN ('0001541353','0001779763')` in the same Phase 1 blanket scrub pass. **Additional INF17 Phase 1 CRD pollution pairs discovered during INF4c batch merge (2026-04-12):** 11 pairs where both entities have holdings (safety-gated out of the batch merge) and are confirmed to be DIFFERENT firms sharing a polluted CRD: CRD 112753 (GoldenTree/EdenTree), CRD 145167 (SGL/Wetzel), CRD 154718 (Uniplan/Portland), CRD 284064 (FJ/FWG), CRD 296943 (Epacria/Electric Capital), CRD 301071 (Puff/QP), CRD 326808 (Samjo/Sabby), CRD 310053 (Northcape/Orca), CRD 161230 (Bandera/Andera), CRD 106450 (InTrack/Intact), CRD 110900 (Hunter Associates/Investment Mgmt Associates). For each: scrub the bad CRD from the entity that doesn't legitimately own it (requires per-pair investigation of which entity's CRD is real vs fuzzy-matched). **4 additional CRD pollutions from INF4e (2026-04-12):** CRD 104702 (WCM/WIM — scrub eid=11228), CRD 122997 (LongView pairs — scrub eid=1865), CRD 168777 (Thrive/Thryve — scrub eid=11205), CRD 309790 (WMP/PWM — scrub eid=11225). **Total scrub list now ~129 CIK rows** (114 original + 11 INF4c + 4 INF4e). Blocks any future `build_entities.py --reset` that relies on `managers` as a trusted source. |
| INF4f | NorthStar CRD fragmentation merge | Done 2026-04-12 | _Discovered 2026-04-12 during 43i NorthStar investigation._ eid=6693 ("NORTHSTAR ASSET MANAGEMENT INC", Boston MA, CIK `0000869304`, padded CRD `000107138`) and eid=7693 ("NORTHSTAR ASSET MANAGEMENT Co LLC", CIK `0001132708`, unpadded CRD `107138`) are the same real-world firm (NORTHSTAR ASSET MANAGEMENT, Boston MA, CRD 107138 in adv_managers). Both have non-zero Q4 holdings — safety-gated out of INF4c batch. Standard INF4-pattern CRD fragmentation merge, but needs the "both have CIK" handling from gotcha q (transfer CIK from merge source to survivor, not just close). Fix 1 (wrong parent closure: eid=861 Wisconsin North Star was incorrectly parent of both via orphan_scan) already applied in 43i follow-up session. These two entities are now self-rooted for EC but remain as separate fragmented entities. |
| INF18 | Financial Partners Group children investigation | Done 2026-04-12 | _Discovered 2026-04-12 during 43i investigation._ Three entities roll under Financial Partners Group, Inc for EC via `orphan_scan`: Cassaday & Co Wealth Management LLC, LVW Advisors LLC, Quadrant Private Wealth Management LLC. Unclear if these are legitimate subsidiaries (Financial Partners Group is an aggregator that acquires RIA practices) or sub-advisers (in which case they should self-root for EC). Verify corporate structure before fixing. If subsidiaries: leave. If sub-advisers: self-root for EC (same fix as the 4 Baird sub-advisers in 43i). **Investigated 2026-04-12:** Not a bug — all three children (Cassaday eid=9864 $5.98B RAUM McLean VA, LVW Advisors eid=5166 $2.28B Pittsford NY, Quadrant Private Wealth eid=7058 $1.81B Center Valley PA) have `wholly_owned/ADV_SCHEDULE_A` relationships from Financial Partners Group, Inc. (eid=1600). FPG is a real RIA aggregator that acquires and owns ~25 wealth management practices — ADV Schedule A confirms ownership. EC rollup is correct. No data change. Side note: FPG has its own fragmentation (eid=1600 "Inc" vs eid=9722 "LLC" with circular orphan_scan) — minor cleanup candidate, not an INF18 issue. |
| INF17b | Fix CRD normalization + fuzzy-match gate in `fetch_ncen.py:328` (adviser_cik) | Done 2026-04-12 | _Discovered 2026-04-11 during INF17 Phase 3 code review._ Two independent bugs in `fetch_ncen.py:328-333` (the `adviser_cik` assignment loop): **(1) CRD format normalization — DONE.** Commit `d89e663` adds `str(adv_crd).lstrip("0") or "0"` before the `UPDATE managers SET adviser_cik = ?` so padded CRDs from `ncen_adviser_map` are stored in canonical unpadded format. Same fix as INF4b but applied to the adviser_cik column. **(2) Brand-token overlap gate — REMAINING.** The fuzzy match still uses `rapidfuzz.fuzz.token_sort_ratio` with `score_cutoff=85` and no brand-token overlap check. Same bug as INF17 Phase 3 in `build_managers.py` (which was fixed in `0634682`). Needs the same `_brand_tokens_overlap()` + `_city_state_compatible()` gates applied to the `fetch_ncen.py` adviser matching loop. These are independent fixes — CRD normalization prevents format-mismatch pollution, the brand-token gate prevents same-suffix name-collision pollution. The `adviser_cik` column is staging-only (silently dropped by `merge_staging.py` on promote due to schema drift), so the pollution hasn't reached prod, but it's a live hazard if the column ever gets promoted. |
| INF8 | Trian Fund Management entity fragmentation | Done 2026-04-12 | _Staging workflow required (INF1)._ Discovered 2026-04-10 during L5 batch 6 (final batch). Three entity records for the same firm: eid=107 ("Trian Fund Management", canonical, the rollup target with $5.33B holdings_v2), eid=1090 ("TRIAN FUND MANAGEMENT, L.P."), eid=9075 ("Trian Fund Management GP, LLC"). Relationships 48 (107→1090) and 452 (107→9075) currently use rule_applied=`fund_sponsor` (which is wrong for institution-to-institution) and source=`parent_bridge` `fuzzy_match` (legacy keyword path). The 1090/9075 entries are correct in spirit (they ARE Trian-related entities) but should be properly consolidated into eid=107 with all identifiers re-attached. Same INF4/INF6/INF7 fragmentation pattern. |
| INF7 | eid 2218 entity identity confusion (Soros vs VSS) | Done 2026-04-11 | Promotion snapshot `20260411_180047`. Investigation found the root cause: the upstream `managers` table had two independent fuzzy-match pollutions that both cascaded into the entity layer. **(1) eid=2218 (real Soros Fund Management):** CIK `0001029160` is legitimate — it files 13F-HR every quarter under "SOROS FUND MANAGEMENT LLC" with `crd_number=None` in the filing header (consistent with Soros's 2011 family-office conversion and ADV deregistration). But someone had fuzzy-matched CRD `156600` into the same `managers` row; CRD `156600` is actually `VSS FUND MANAGEMENT LLC` ($977M NY firm in adv_managers, `aum_total` on the polluted row matched VSS's RAUM to the dollar). The CRD then flowed into `entity_identifiers` via `source='managers'`. The Apr 7 session's rollup builder walked CRD `156600` through VSS's ADV Schedule A, found Verus Capital Partners as a control parent, and briefly wired eid=2218 under eid=1630 (`wholly_owned`) until Apr 10 self-root broke the chain. The CRD was left behind on `entity_identifiers` and the `managers` row until this INF7 pass. **(2) eid=8443 (Peter Soros — SOROS CAPITAL MANAGEMENT LLC, Coronado CA):** discovered during INF7 precondition checks, same pattern. CRD `119428` is `ORION CAPITAL MANAGEMENT LLC` in Coronado CA with RAUM $256,676,559 matching `managers.aum_total` to the dollar. Same city + "Capital Management LLC" name similarity drove the mispair. **(3) eid=92:** a PARENT_SEEDS brand-parent ghost with zero identifiers, acting as a fuzzy_match rollup target for three Soros-named CIKs (eid=2218 real Soros, eid=8443 Peter Soros, eid=10228 George Soros the individual). No verified corporate meaning — just a shared-name brand bucket. **Option A fix applied via INF1 staging workflow, 9 edits:** (a) closed CRD 156600 on eid=2218 in `entity_identifiers`; (b) closed relationship_id=418 (92→8443) in `entity_relationships`; (c) self-rooted eid=8443 in both rollup_types in `entity_rollup_history`; (d) closed relationship_id=507 (92→10228); (e) self-rooted eid=10228 in both rollup_types; (f) closed both active aliases on eid=92 ("Soros Fund Management" brand + "SOROS" filing) — entity row preserved per spec; (g) `UPDATE managers SET crd_number=NULL WHERE cik='0001029160'` to scrub the VSS CRD at source; (h) `UPDATE managers SET parent_name=NULL WHERE cik='0001748240'` to kill the fuzzy_match path; (h-bis) `UPDATE managers SET crd_number=NULL WHERE cik='0001748240'` to scrub the Orion CRD at source (added during precondition review after discovering the second pollution). Full transaction applied to staging in one pass. diff_staging.py: 9 line-level changes (2 deleted relationships, 4 retargeted rollups, 2 deleted aliases, 1 deleted identifier). validate_entities.py --staging: 9 PASS / 0 FAIL / 7 MANUAL (exact baseline match, no regression). promote_staging.py --approved: snapshot `20260411_180047`, 13 row-level changes applied to prod, post-promotion prod validation 9/0/7. merge_staging.py --tables managers: DROP+CREATE rebuild of prod.managers (12,005 rows, 15 cols preserved); verified post-merge that both target CIKs show the expected NULL values. Final prod validate_entities.py: 9/0/7 — same as baseline. Two follow-up items deferred: (i) `managers.aum_total` for CIKs 0001029160 and 0001748240 still holds VSS's $977M and Orion's $257M respectively — those are wrong numbers, but with the CRD links broken they no longer feed downstream lookups, flag as separate cleanup; (ii) staging `managers` has an extra `adviser_cik` column not in prod, silently dropped by merge — schema drift, pre-existing, unrelated to INF7, separate follow-up. INF7 close-out note: the real Soros Fund Management ($8.6B Q4 book, 839 positions) is now cleanly attributed to eid=2218 with CIK `0001029160` as its only identifier; Peter Soros's SOROS CAPITAL MANAGEMENT LLC is self-rooted at eid=8443; George Soros the individual is self-rooted at eid=10228; the eid=92 ghost is retired (row preserved, aliases closed, children unrooted). |
| INF6 | Tortoise Capital Advisors fragmentation | Done 2026-04-12 | _Staging workflow required (INF1)._ Discovered 2026-04-10 during L5 batch 2 audit. Tortoise Capital Advisors exists at two entity_ids: eid=2273 ("TORTOISE CAPITAL ADVISORS, L.L.C.", canonical, $14.8B holdings_v2) and eid=20187 ("Tortoise Capital Advisors, LLC", duplicate currently rolling to 2273 via `circle_fix` rule). Same firm, two entity records — INF4-style consolidation candidate. Merge eid=20187 into eid=2273 (or vice versa) and update any references. |
| INF4e | Investigate 4 borderline pairs from INF4c safety gate | Done 2026-04-12 | _Discovered 2026-04-12 during INF4c batch merge dry run._ All 4 pairs investigated and excluded as **CRD pollutions (not real fragmentations)**. All 4 added to INF17 Phase 1 scrub list. **(1) CRD 104702** WCM INVESTMENT MANAGEMENT (eid=9232, CIK `0001061186`, $48.57B) / WIM INVESTMENT MANAGEMENT Ltd (eid=11228, CIK `0002042565`, $0.39B) — different firms (US vs UK, "Ltd" = British legal form), both file 13F-HR independently with different CIKs. ADV confirms CRD belongs to WCM (CIK `1061186`, Laguna Beach CA). WIM got the CRD from fuzzy match (1-letter difference: WCM→WIM). **(2) CRD 122997** LONGVIEW ASSET MANAGEMENT, LLC (eid=803, CIK `0001086477`, $9.42B, 3 concentrated positions) / LongView Asset Management LLC (eid=1865, CIK `0002053960`, $0.14B, 51 diversified positions) — different firms sharing the same name, different CIKs, radically different portfolio profiles. ADV CIK is NULL but firm_name + city (Santa Fe NM) match eid=803. **(3) CRD 168777** Thrive Wealth Management, LLC (eid=1022, CIK `0001847610`, $0.68B) / THRYVE WEALTH MANAGEMENT LLC (eid=11205, CIK `0002113283`, $0.20B) — different firms (1-letter spelling variant: Thrive vs Thryve). ADV CIK `1847610` confirms CRD belongs to Thrive. Thryve has only 1 filing (vs 4 for Thrive). **(4) CRD 309790** Wealth Management Partners, LLC (eid=2365, CIK `0001843294`, $0.35B) / PARTNERS WEALTH MANAGEMENT, LLC (eid=11225, CIK `0002054714`, $0.10B) — different firms, word-order swap. ADV CIK `1843294` confirms CRD belongs to WMP. token_sort_ratio is order-insensitive, so "partners wealth management" = "wealth management partners" after sorting → classic false positive. INF4c batch fully closed. |
| INF4c | Batch merge of 115 CRD-format fragmented entity pairs | Done 2026-04-12 | _Discovered 2026-04-12 via INF4b audit query._ Code fix (INF4b) prevents new fragmentation; these 115 existing pairs predate the fix. Top priority: Boston Partners ($96.6B, eid=9143 vs eid=17933). Full list available from the INF4b audit query (`entity_identifiers` self-join on `LTRIM(identifier_value, '0')` for `identifier_type='crd'`, filtered to pairs not already linked via `entity_rollup_history`). **Exclude 3 confirmed INF17 pollution pairs before merging:** CRD 110901 (Ostrum/Trillium — different firms, same polluted CRD), CRD 157402 (SCGGF/SCGE), CRD 144316 (US Financial/Pure Financial). Run same INF1 workflow as INF4/INF6/INF8. Start with top 10 by AUM, then batch the remainder. |
| INF4d | Boston Partners priority merge ($96.6B) | Done 2026-04-12 | _First item in INF4c batch._ eid=9143 ("BOSTON PARTNERS", canonical, 13F filer) + eid=17933 ("Boston Partners Global Investors, Inc", N-CEN duplicate). Same CRD 124982, CRD format mismatch root cause (padded `000124982` vs unpadded `124982`). $96.6B combined Q4 2025 AUM — largest single fragmented pair in the 115-pair INF4c batch. Investigate and merge via INF1 workflow (same pattern as INF4 Loomis Sayles). |
| INF4b | CRD string-format fragmentation in entity resolver | Done 2026-04-12 | _Discovered 2026-04-11 during INF4 investigation._ Entity resolver compares CRDs as raw strings without normalizing leading zeros. `managers` stores unpadded CRDs (e.g. `105377`); `ncen_adviser_map` stores padded CRDs (e.g. `000105377`). String mismatch causes N-CEN adviser rows to create new entities instead of attaching to existing ones. Root cause of INF4 Loomis Sayles fragmentation (eid=7650 vs eid=17973) and likely affects other firms. Fix: normalize CRDs to canonical format (strip leading zeros OR standardize to 6-digit zero-padded) in `entity_identifiers` lookup before string comparison. Audit `entity_identifiers` for CRD values with/without leading zeros to measure full scope before fixing. Without this, any `build_entities.py --reset` re-creates the merged-away duplicate from the N-CEN feed. |
| INF4 | Loomis Sayles entity fragmentation | Done 2026-04-11 | _Staging workflow required (INF1)._ Discovered 2026-04-10 during Section 1c CRI fix. eid=17973 ("Loomis, Sayles & Company, L.P.") is the canonical DM rollup target for 25 fund series, but the actual 13F filer with $246.9B AUM and 4,487 holdings_v2 rows is eid=7650 ("LOOMIS SAYLES & CO L P"). Same firm, two entity IDs from different feeders. Sub-advised fund routings and the parent's own 13F book are split across both entity_ids, so any join through entity_id fragments Loomis's true book. DM13 consolidation candidate — merge eid=17973 into eid=7650 (or vice-versa, whichever has the canonical N-CEN registration), update all 25 DM routings + econ rollups + holdings_v2 references in one staged pass. |
| INF1 | Staging workflow framework | Done 2026-04-10 | `sync_staging.py` (CTAS mirror prod → staging entity tables, 8 tables, sequence reset), `diff_staging.py` (human-readable name-not-id diffs by table, current-state SCD comparison), `promote_staging.py` (intra-DB snapshot, diff-based DELETE+INSERT, validate_entities --prod, auto-rollback on rc=2 structural FAIL only), `rollback_promotion.py` (--list / --restore SNAPSHOT_ID), `backup_db.py` (full DuckDB EXPORT DATABASE under data/backups/). All entity changes (DM12-DM15, L4/L5 audits, ADV pre-insert verification fallouts, manual fixes) MUST go through this workflow before production promotion. validate_entities.py default flipped from staging to prod with explicit --staging / --prod flags. End-to-end tested 2026-04-10. See ENTITY_ARCHITECTURE.md → Operational Procedures and MAINTENANCE.md. |
| 19 | Centralize quarter config | Done | `config.py` created, imported by fetch_13f, load_13f, fetch_market, build_summaries, compute_flows. app.py SQL quarters need dedicated pass (60+ queries) |
| 20 | Incremental market data refresh | Done | `get_stale_tickers()` skips tickers fetched <7 days. `save_market_data()` upserts, never drops |
| 21 | Materialized summary tables | Done | `build_summaries.py` creates `summary_by_ticker` + `summary_by_parent`. --rebuild flag |
| 22 | Logged exceptions in app.py | Done | All bare `except Exception:` replaced with `as e` + `app.logger.error()` in endpoint handlers |
| 23 | Incremental `load_13f.py` — append + rebuild latest quarter only | Done | `--quarter 2025Q4` flag deletes+reloads single quarter from staging tables, then rebuilds filings+holdings from all data. Also added `--staging` and crash handler |
| 24 | OpenFIGI + yfinance persistent cache in `build_cusip.py` | Done | `_cache_openfigi` and `_cache_yfinance` tables, 30-day yfinance freshness |
| 25 | Readonly snapshot auto-refresh | Done | `scripts/refresh_snapshot.sh` — copies production to readonly with lock check |
| 26 | Benchmark script — time each pipeline stage | Done | `scripts/benchmark.py` with --run flag, stage timing, dry-run check |
| 27 | app.py quarter centralization — convert SQL strings to use config.py vars | Done | All 6 hardcoded quarter strings replaced with `{LQ}` / `{PQ}` variables. Zero hardcoded quarters remain in app.py |
| 28 | Pyflakes cleanup — unused imports removed from 17 scripts | Done | 31 unused imports fixed |
| 29 | SHOW TABLES removed from Flask endpoints — cached via `has_table()` | Done | 6 per-request queries replaced with startup-time cache |
| 30 | Snapshot switchback monitor — background thread auto-recovers from snapshot | Done | 60s polling, logs switchover |
| 31 | `queries.py` service layer — extract SQL from app.py route handlers | Done | 34 functions, 1,789 lines. app.py 2,362→618 lines |
| 32 | `export.py` — extract Excel export logic from app.py | Done | `build_excel()` + style constants, 83 lines |
| 33 | `run_pipeline.sh` hardened — process guard, timestamps, macOS notification | Done | pgrep check, phase start/end, osascript notify |
| 34 | Global SEC rate limiter — 2 req/s across all workers, Retry-After parsing | Done | `_rate_limit()` with shared lock, Phase 2 workers reduced to 2 |
| 35 | Phase 2 stall fix — reduced retry timeouts, per-future timeout | Done | Retry 3→2, sleep 10→5s, HTTP timeout 15→10s, fut.result(timeout=60) |
| 36 | `--phase1-only`, `--phase2-only`, `--phase3-only` CLI flags | Done | Each phase independently runnable and resumable |
| 37 | Crash handler on all fetch scripts | Done | db.crash_handler() writes traceback to logs/<script>_crash.log |
| 38 | Test isolation — `--test` uses `13f_test.duckdb`, write guard | Done | `db.py` centralized, assert_write_safe() |
| 39 | Bug 6 — fetch_market.py upsert, never drops table | Done | Unique index on ticker, delete-then-insert per batch |
| 40 | Bug 7 — app.py snapshot switchback monitor | Done | Background thread, 60s poll, auto-switch + log |
| 41 | Chart.js fix — setTimeout 100ms before chart init | Done | Lets DOM render canvas before Chart.js init |
| 42 | Entity MDM — Phases 1-3 complete on staging, see ENTITY_ARCHITECTURE.md | Staging complete, awaiting Phase 4 merge | Phase 3: 5,293 long-tail CIKs resolved via SEC EDGAR (100% retrieval), 153 parent matched, 104 SIC classified, 181 aliases added. resolve_long_tail.py + entity_sync extensions. 13 validation gates (8 PASS, 5 MANUAL, 0 FAIL). |
| 43 | Fix pre-existing app.py lint debt | Done | flake8: 116→0. bandit B608: 28→0 (`.bandit` config). 17 bare `except Exception:` → `as e` + `app.logger.debug()`. `setup.cfg` added. Pre-commit unblocked. |
| 43c | Review LOW + MEDIUM confidence classifications 1-by-1 | Done | L3 entity_type: all 13 categories audited, 900+ entity fixes. passive 59→49, quantitative 68→43, hedge_fund cleaned (24 active managers removed), PE 168→77, strategic cleaned (J.Stern $275B), SWF fixed (Norges Bank + Nordic pensions), mixed 1,423→707 (565 wealth + 124 bank trusts + 4 SWF + 2 pension extracted), active 4,719→4,164 (565 wealth + 3 HF + 3 endowment extracted). L4 synced. L5 top 10 parents audited: Vanguard Capital Wealth Advisors unwired from Vanguard Group, Capital Group cleaned (46→8 CIKs, 40 bogus "X Capital" entities extracted), Fidelity cleaned (FNF + Bancorp extracted), PIMCO verified (no 13F filer — bond manager below equity threshold). Nomura verified (Macquarie US Public Investments legit acquisition 2024). Kimelman & Baird unwired from Baird. |
| 43i | L5 sub-adviser rollup policy review | Done 2026-04-12 | 360 non-fund entities wired to parents via transitive_flatten/orphan_scan/phantom_merge where child name has no overlap with parent. Most are legitimate brand subsidiaries (DWS→Deutsche, SPDR→SSGA, FlexShares→Northern Trust, Putnam→Franklin, Columbia→Ameriprise) but some are sub-advisers incorrectly consolidated under fund sponsor instead of operating AM (e.g., GAMMA/Strategas/Reinhart sub-advising Baird Funds Trust, Wellington sub-advising Vanguard funds). Needs case-by-case review with fund_sponsor vs operating AM rule clarification. Top affected: State Street (76), ProShare (29), Nomura (27), Fidelity (15), Franklin Templeton (13). **Investigated 2026-04-12:** actual zero-overlap institution count is 28 (not 360 — the original estimate included fund series which are legitimate). Of the 28: 24 are legitimate brand subsidiaries with different names (Raymond James→Carillon/ClariVest/Eagle/Scout, Franklin→Benefit Street/Lexington/O'Shaughnessy/Royce, AMG→Boston Common/Tweedy Browne, PGIM→Jennison/Prudential, Fidelity→Strategic Advisers, NY Life→Ausbil/NYL, DFA→DFA Australia, North Star fragmentation). **4 confirmed bad cases fixed:** GAMMA Investing LLC, Greenhouse Funds LLLP, REINHART PARTNERS LLC, Strategas Asset Management LLC — all sub-advisers to Baird Funds Trust incorrectly rolled under Baird (eid=73) for EC via transitive_flatten. Self-rooted for economic_control_v1, DM unchanged. Wellington/Vanguard EC already correct (Wellington self-grouped under eid=11220). Snapshot `20260412_103422`. **Follow-up: 3 Financial Partners Group children** (Cassaday, LVW Advisors, Quadrant Private Wealth) unclear if subsidiaries or sub-advisers — tracked as INF18. |
| 43f | Review parent_seeds for passive misclassifications | Done | Moved 63 pension funds ($1.2T), 41 insurance cos ($0.2T), 2 endowments ($36B) from passive to correct categories. Passive now 45 entities = pure index/ETF providers. | parent_seeds contains pension funds, insurance companies, and endowments mixed in with index providers. Pension funds (CalPERS, HOOPP, state retirement systems) and insurance (State Farm, Zurich, Cincinnati) are classified as passive because their parent_seeds strategy_type is passive — but they're not passive index managers. Need to separate: (a) index providers/ETF issuers (Vanguard, BlackRock, SSGA, Geode) = passive, (b) pension/insurance = pension_insurance, (c) endowments = endowment_foundation. Also review fund-level: many pension/insurance funds hold index ETFs (correctly passive at fund level) but the manager should be pension_insurance. |
| 43d | Fund-level classification cleanup | Done | L1 fund_strategy: 895 NULL series classified (231 passive, 744 active via keyword + fund_universe fallback). 295 conflicts resolved: 55 active→passive (ProShares leveraged ETFs, MML index), 240 passive→active (AQR, AB, Avantis — false keyword matches). entity_classification_history synced. Zero NULLs, zero conflicts with fund_universe. Richer manager types (hedge_fund, quantitative) structurally absent from N-PORT — only registered funds file. |
| 43h | CIK-level type differentiation | Done | `entity_type` column on holdings_v2: per-entity classification from entity_classification_history + ADV strategy enrichment. 1,245 entities reclassified ($11.7T AUM). Key moves: $3.3T active→hedge_fund, $2.6T mixed→active, $1.5T wealth→active. Active Only filter now uses entity_type instead of parent-inherited manager_type. |
| 44 | Entity MDM Phase 3.5 — ADV ownership | Done | 3,585 CRDs parsed (98.2%), 26,822 rows, 1,059 relationships. Dual-parser: pymupdf primary (88.3% recall, 99.5% accuracy, 20 min) + pdfplumber fallback. `--refresh` mode for updates (4 workers). QC report, manual adds, interactive review HTML. See ENTITY_ARCHITECTURE.md. |
| 45 | PyMuPDF parser for ADV | Done | 100-400x faster than pdfplumber. 88.3% recall, 99.5% accuracy, +1,151 net entities. Handles all oversized PDFs. Used as primary in `--refresh` mode. pdfplumber retained as fallback. |
| 46 | ADV pipeline hardening | Done | Atomic SCD, deterministic matching, duplicate staging guard, PDF validation, explicit checkpoint, alias cache reuse, evidence resolution policy, scan-based ownership code extraction, expanded match universe (all aliases). |
| 47 | Interactive entity review tool | Done | `data/reference/adv_entity_review.html` — 1,926 unresolved CRDs, 20,416 searchable aliases, pre-populated recommendations, export to `adv_manual_adds.csv`. |
| 49 | D1 accuracy audit | Done | 90 PDFs, 6 strata. 75% parser agreement, zero false positives. Pymupdf recall gap = Schedule B indirect owners. |
| 50 | Entity review complete | Done | 1,926 CRDs triaged: 99 wired to parents, 1,827 confirmed independent. 12 new entities created. 174 orphan subsidiaries consolidated. DBA/legal name check added to QC. |
| 51 | Phase 4 — Migration | Done 2026-04-13 (Stage 5 closed) | Cutover 2026-04-09. App running on entity-backed v2 tables. 100% entity/rollup coverage, 0.00% AUM diff, 8/8 parity gates pass, zero 500 errors. Shadow log clean. **Stage 5 cleanup 2026-04-13:** 3 legacy pre-v2 tables dropped from staging + prod — `holdings` (12,270,984 rows), `fund_holdings` (6,385,709 rows), `beneficial_ownership` (51,905 rows prod / 0 staging). Backup `data/backups/13f_backup_20260413_184518` (2.7GB). Zero code references to legacy tables (grep confirmed — all reads/writes are on `*_v2` variants). Zero view references. `validate_entities.py --prod` 9 PASS / 0 FAIL / 7 MANUAL unchanged. 8/8 smoke tests green. **4 INF9d ghost entities NOT deleted — closed as won't fix.** Pre-delete verification found eid=20194/20196/20201/20203 are live PARENT_SEEDS brand shells: each has 1 brand alias, 2 rollup_history rows (self-root EC+DM), 2-3 classification_history rows including fresh manual_l4 edits from 2026-04-10, and 1 outgoing `wholly_owned` ADV_SCHEDULE_MANUAL relationship to a real child entity (20194→1685 Pacific Life Fund Advisors, 20196→8544 American Century, 20201→9990 Catalyst Capital, 20203→2196 International Assets Investment Mgmt). ROADMAP claim "no aliases, no holdings, safe to delete" was inaccurate — 0 holdings_v2 refs is correct but ADV lineage is load-bearing. |
| 52 | Phase 4 post-cutover: brand 13F consolidation | Done | T. Rowe Price: 3 fragments (eid=3616, 13, 17924) consolidated under eid=3616 ($1,013B + 32K N-PORT). MFS: fund entities flattened from eid=5047→eid=17 ($287B + 15K N-PORT). Neuberger Berman: was rolling to Man Group (wrong), fixed to eid=55 ($129B + 1.8K N-PORT). HSBC: no fragmentation found. |
| 53 | Post-Phase 4: 13D/G entity coverage | Done | Added 51 CIK identifiers for 13D/G filers matched by name to existing entities. Coverage: 53.7% → 77.1% (+23.4pp). Remaining unmatched are individuals, fund-level CIKs, and small filers without entity entries. |
| 54 | Post-Phase 4: Voya rollup wiring fix | Done | Voya (eid=1591) set to self-rollup. Was rolling to VVR Holdings (eid=2110). Display name cleaned to "Voya Investment Management". |
| 55 | Post-Phase 4: 13D/G exit validation | Done | 381 exits with pct=0% but shares>0 fixed (shares set to 0). beneficial_ownership_current rebuilt: 13,799 with prior_intent, 166 intent changes detected (92 passive→activist, 74 activist→passive). crossed_5pct flag validated: 22,349 crossed, 6,810 subsequently reduced below 5%. Exit validation confirmed correct for direct filer CIKs (4/4 verified against EDGAR). Filing agent CIK gap (902664, 1213900, 1341004) cannot be verified via standard EDGAR index path — requires filer CIK lookup. Deferred to beneficial_ownership entity coverage expansion item (#53). |

---

## Regulatory Watch

| Item | Status | Details |
|---|---|---|
| SEC N-PORT proposed rule change | Monitoring | Feb 18, 2026 proposal would limit public N-PORT data to month-3 of each quarter only, with 60-day lag after quarter end. If adopted, months 1 and 2 disappear from future DERA ZIPs. Action: monitor comment deadline and final rule. If adoption looks likely, archive all available month-1/month-2 data before cutoff. |
| Q1 2026 13F availability | May 16 target | Early filers appearing (e.g. First Merchants Corp filed Apr 6). Bulk of institutional filings expected May 1-15. Run fetch_13f.py after May 15 deadline. |
| DERA 2026Q1 N-PORT ZIP | ~Late June | Not yet available. Standard 60-90 day lag after quarter close. Continue using fetch_nport_v2.py --monthly-topup for interim data. |

---

## ARCHITECTURE BACKLOG — from ARCHITECTURE_REVIEW.md (2026-04-12)

Source: `ARCHITECTURE_REVIEW.md` (repo root). Prerequisites and phase
sequencing documented there — do not reorder without consulting the gate
criteria in each phase. **Status (2026-04-13): all near-term architecture
work complete — Phase 0 (CI) through Phase 4+ Batch 4-C (Flask → FastAPI)
+ 4C+ (openapi-typescript regen) all shipped.** Phase 5 / 6 parked as
Medium Term (MT-1 through MT-6) — triggered on external user / team /
productization milestones, not calendar. Next near-term work is data-layer
(N-PORT refresh; Stage 5 cleanup closed 2026-04-13); architecture side has Phase 3+
(portfolio_context precompute) and Phase 3++ (build_analytics.py) both
trigger-based. See `docs/NEXT_SESSION_CONTEXT.md` for the session queue.

### Phase 0 — Prerequisite (promoted from BL-1, split into 0-A + 0-B)

| # | Item | Batch | Notes |
|---|------|-------|-------|
| ARCH-0A | Lint + static analysis CI | 0-A | ✅ **DONE 2026-04-13** (commit `e201885`). `.github/workflows/lint.yml` runs pre-commit (ruff + pylint + bandit) on push + PR. Green on main. See COMPLETED section for details + BL-8 for suppressed-rule cleanup backlog. |
| ARCH-0B | Runtime smoke test CI | 0-B | ✅ **DONE 2026-04-13**. Phase 0-B1 design doc landed (`7f62b7d`, Option 2). Phase 0-B2 implementation shipped in the same session: `scripts/build_fixture.py` + `tests/fixtures/13f_fixture.duckdb` (~11 MB) + `tests/smoke/` (endpoint-OK + response-equality tests) + `.github/workflows/smoke.yml`. 4 monitored endpoints: `/api/tickers`, `/api/query1?ticker=AAPL`, `/api/summary?ticker=AAPL`, `/api/entity_graph?entity_id=2`. Gate cleared: Phase 4 Batch 4-A (Blueprint split) now unblocked. See COMPLETED. |

### Phase 1 — Contract Stabilization (freeze endpoint semantics)

| # | Item | Batch | Notes |
|---|------|-------|-------|
| ARCH-1A | Routing hygiene | 1-A | ✅ **DONE 2026-04-13** (commit `a8dd77a`). Dual-mount helper aliases 38 public `/api/*` routes at `/api/v1/*`. New public `/api/config/quarters` route with legacy `/api/admin/quarter_config` retained for vanilla-JS. Rollup audit: 2 gaps fixed on `/api/short_analysis` + `/api/short_long`. Input guards via single `before_request` hook. Ticker regex corrected to `^[A-Z]{1,6}(\.[A-Z])?$` to match stated intent (spec literal did not accept `BRK.B`). See COMPLETED. |
| ARCH-1B1 | Endpoint classification + export parity | 1-B1 | ✅ **DONE 2026-04-13** (commit `d3a2fcb`). Full 39-route classification comment block committed to `app.py`. `api_export` now mirrors `api_query` rollup_type handling via shared `_RT_AWARE_QUERIES` set. q1/q16 export unblocked by scoped data-shape extraction extension. q6/q10/q11/q15 exports still 500 — tracked as BL-10. See COMPLETED. |
| ARCH-1B2 | Error envelope + Pydantic schemas + React error boundaries | 1-B2 | ✅ **DONE 2026-04-13**. Vanilla-JS retirement unblocked the gate. Infra commit lands Envelope[T]/ErrorShape/MetaShape + respond() helper + useFetchEnvelope hook + ErrorBoundary. Rollout commit wraps 6 priority endpoints: `/api/v1/tickers`, `/api/v1/query1` (Register), `/api/v1/portfolio_context` (Conviction), `/api/v1/flow_analysis`, `/api/v1/ownership_trend_summary`, `/api/v1/entity_graph`. React tabs + 3 ticker dropdown consumers updated. AppShell wraps every tab in `<ErrorBoundary key={activeTab}>`. Smoke snapshots regenerated for 3 of 4 monitored endpoints (envelope shape); `_counts` helper recurses into `data` for enveloped endpoints. 8/8 smoke tests green. See COMPLETED. |

### Phase 2 — Correctness Fixes

| # | Item | Batch | Notes |
|---|------|-------|-------|
| ARCH-2A.1 | `get_nport_children` N+1 loop fix | 2-A | ✅ **DONE 2026-04-13** (commit `700bcdb`). New `get_nport_children_batch()` in `queries.py` single-round-trips 25 parents via VALUES+ILIKE+QUALIFY. 17.8ms vs 297ms legacy (14× speedup, well under ≤50ms target). End-to-end query1 AAPL 748→211ms. Call sites: query1 (Register), portfolio_context (Conviction). |
| ARCH-2A.2 | `summary_by_parent` path check | 2-A | ✅ **DONE 2026-04-13** (commit `700bcdb`). Confirmed read-only on every request path — 3 consumers at queries.py lines 775/1442/4304 are all pure SELECT. Writes only in `build_summaries.py` (T4 per-quarter atomic). No code change needed. |
| ARCH-2A.3 | Write-path consistency audit (non-entity) | 2-A | ✅ **DONE 2026-04-13** (commit `700bcdb`). New `docs/write_path_risk_map.md` — 22 scripts grouped into 6 risk tiers. Key finding: no audited script uses explicit BEGIN/COMMIT/ROLLBACK. T2 (DROP→CREATE→INSERT without txn) is the natural BL-3 target. Audit only. |

### Phase 3 — Precompute + Data Layer

| # | Item | Batch | Notes |
|---|------|-------|-------|
| ARCH-3A.1 | `FAMILY_MAP` → `fund_family_patterns` DB table | 3-A | ✅ **DONE 2026-04-13** (commit `731f4a0`). New `fund_family_patterns(pattern, inst_parent_name, PK(inst_parent_name,pattern))` — 83 rows across 50 groups, seeded from the dict via `scripts/migrate_batch_3a.py`. `get_nport_family_patterns()` reads from DB, memoized; `_FAMILY_PATTERNS_FALLBACK` preserved verbatim for deploy safety. |
| ARCH-3A.2 | `data_freshness` table + `/api/freshness` | 3-A | ✅ **DONE 2026-04-13** (commit `731f4a0`). New `data_freshness(table_name PK, last_computed_at, row_count)` — empty on initial migration. New `/api/freshness` route (auto-dual-mounted at `/api/v1/freshness`), returns `{data: []}` gracefully if table missing. React footer + per-table pipeline write hooks are follow-on work. |
| ARCH-3B | `portfolio_context` quarterly precomputed artifact | 3-B | New `portfolio_context_cache` table + `scripts/compute_portfolio_context.py`. Thin endpoint `SELECT` — no pandas. Target ≤50ms. Keep legacy fallback until one full pipeline run validates. |
| ARCH-3PP | `build_analytics.py` quarterly precompute | 3++ | New pipeline step after `compute_flows.py`. Precomputes `register_cache`, `conviction_cache`, `ownership_trend_cache`, `cross_ownership_cache` per ticker×quarter×rollup_type. Eliminates on-demand complex SQL for stable analytical views. Same pattern as `investor_flows`. See `ARCHITECTURE_REVIEW.md` Phase 3++. |

### Phase 4 — Backend Modularization

| # | Item | Batch | Notes |
|---|------|-------|-------|
| ARCH-4A | Runtime surface split | 4-A | ✅ **DONE 2026-04-13**. Split `scripts/app.py` (1,788 lines post-Phase-1-B2) into 9 modules: `app.py` (thin entry, 103 lines), `app_db.py`, `api_common.py`, `api_config.py`, `api_register.py`, `api_fund.py`, `api_flows.py`, `api_entities.py`, `api_market.py`, `api_cross.py`. All domain files ≤400 lines. Endpoint classification moved to `docs/endpoint_classification.md`. `app_legacy.py` retained for 1-week rollback. Fast-forward merged from `phase4-blueprint-split` after 8/8 smoke + 11/11 Playwright green. See COMPLETED. |
| ARCH-4B | Service layer split | 4-B | ✅ **DONE 2026-04-13**. Split `scripts/queries.py` (5,703 → 5,523 lines) — extracted `cache.py` (43 lines: CACHE_TTL, `CACHE_KEY_SUMMARY` template, `cached()` + `_cached` alias) and `serializers.py` (211 lines: `_clean_val`, `clean_for_json`, `df_to_records`, `resolve_filer_to_parent` + supporting CIK/name sets, `resolve_filer_names_in_records`, `_13f_entity_footnote` + `_13F_ENTITY_NOTES`, `get_subadviser_note` + `SUBADVISER_NOTES`). `queries.py` re-exports `clean_for_json` + `df_to_records` so handler imports stay green (Option B pragmatic split, not spec's strict Option A — rollback is "module merge" per spec). `get_summary` migrated to `cached(CACHE_KEY_SUMMARY.format(ticker=ticker), ...)`. 0 `jsonify()` calls in `queries.py`, all response shaping in `serializers.py`. 8/8 smoke tests green, pre-commit clean. See COMPLETED. |
| ARCH-4C | Flask → FastAPI migration | 4-C | ✅ **DONE 2026-04-13**. All 8 domain Blueprints → FastAPI APIRouters (config/register/fund/flows/entities/market/cross/admin). `scripts/app.py` now a FastAPI entry with `uvicorn` `__main__`, `StaticFiles` mount, `Jinja2Templates`, `lifespan` handler. `admin_bp.py` token auth moved from `@before_request` to `Depends(require_admin_token)` via `Header(alias='X-Admin-Token')`. All routes kept as `def` (not `async def`) to preserve thread-local `get_db()` cache under FastAPI's threadpool. Every `jsonify()` call replaced with `JSONResponse` or bare dict return. Excel export uses `StreamingResponse`. Smoke tests swap `flask.test_client()` for `fastapi.testclient.TestClient`. `/docs` + `/redoc` OpenAPI surface added. Flask removed from requirements.txt + smoke.yml. 8/8 smoke + 11/11 Playwright green on uvicorn. Fast-forward merged from `phase4-fastapi`. **Follow-up:** React api.ts regeneration from `/openapi.json` via openapi-typescript (not gating — wire format unchanged). See COMPLETED. |

### Medium Term — External User / Team / Productization Triggered

_Explicitly parked. Not being planned. Pulled from the near-term queue
2026-04-13 after Batch 4-C landed. Each item is triggered by a business
milestone — calendar cadence is the wrong lens._

| # | Item | Notes / Trigger |
|---|------|-----------------|
| MT-1 | Gunicorn + Nginx serving | Replace the `uvicorn` dev entry with `gunicorn -k uvicorn.workers.UvicornWorker` behind Nginx. Trigger: first external user or `uvicorn` dev-server limits surface. |
| MT-2 | Authentication | Flask-JWT or Auth0 or FastAPI OAuth2 — picks up per-user access control on top of the existing INF12 admin token. Trigger: same moment as Gunicorn. |
| MT-3 | Pipeline scheduling | APScheduler inside the FastAPI process (smaller footprint) or Airflow (multi-team). Trigger: moving from solo-operator to team cadence. |
| MT-4 | Cloud deployment | Railway + Vercel, or a combined EC2 box, or Render — choose when productizing. Render `render.yaml` already sketched (`web/README_deploy.md`). |
| MT-5 | PostgreSQL migration for entity MDM | Only if DuckDB write concurrency becomes a problem. Fixed-size analytical tables stay on DuckDB. Trigger: concurrent writer contention. |
| MT-6 | Repo reshape (Phase 6 in ARCHITECTURE_REVIEW.md) | `src/{api,queries,pipeline,entity,db}/` layout. Gate: concrete pain point (second operator onboarding, independent pipeline deploy, test isolation) — not aesthetic preference. |

### Phase-independent backlog

_BL-1 (GitHub Actions CI) promoted to Phase 0 — see ARCH-0A above._

| # | Item | Notes |
|---|------|-------|
| BL-2 | Pipeline dependency enforcement | Makefile or DAG. Prevents out-of-order runs. |
| BL-3 | Write-path consistency (non-entity) — implementation | Follow-on to ARCH-2A.3 audit. Extend staging/validation to flow recompute + market data upsert. |
| BL-4 | Three snapshot roles documented | Serving (`13f_readonly.duckdb`) / promotion rollback (intra-DB tables) / cold archive (`EXPORT DATABASE`). Distinct retention rules. |
| BL-5 | Zustand scope enforcement | Document rule: global store = `ticker / quarter / rollupType / company` only. Tab-local state stays in components. |
| BL-6 | Loading state standardization | Shared skeleton + empty state components across all 11 React tabs. |
| BL-7 | DB-universe ticker validation | Route-layer check against the `tickers` table (or cached set). Catches typos that the Batch 1-A regex passes. Follow-on to ARCH-1A. Load the ticker set at app startup or cache with short TTL to keep route layer decoupled from a per-request DB query. |
| BL-10 | `/api/export/query<N>` 500s for q6 / q10 / q11 / q15 | ✅ **DONE 2026-04-13** (commit `9ea3557`). New `build_excel_multisheet()` in `scripts/export.py`; `api_export` extractor in `scripts/api_register.py` routes dict-of-lists and list[1]-with-nested-structures to multi-sheet output. Verified 8/8 exports return 200 with valid xlsx: q6→5 sheets, q10/q11→2 sheets each, q15→3 sheets (Overview+quarters+coverage). q1/q2/q7/q16 single-sheet path unchanged. See COMPLETED. |
| BL-9 | Fix `/api/short_long` 500 — `KeyError 'long_value_k'` | ✅ **DONE 2026-04-13** (commit `9572844`). Two bugs in `get_short_long_comparison` (`scripts/queries.py:3864`): (1) `_has_table('fund_holdings')` short-circuited the endpoint to an empty response after Stage 5 cleanup dropped the legacy table — fixed to check `fund_holdings_v2` which the SQL actually reads; (2) the KeyError itself — SELECT aliased as `long_value`, not `long_value_k`; renamed the output dict key to match for parity with the parallel `short_value` field. See COMPLETED. |
| ARCH-4C-followup | Two-step React type migration (replace hand-written `src/types/api.ts` with `api-generated.ts`) | **Discovered 2026-04-13 during migration attempt.** Current state: `api-generated.ts` has 7 named schemas vs ~55 in `api.ts`, and 5 of the 7 generated schemas are `{[key: string]: unknown}` opaque because the backend Pydantic models in `scripts/schemas.py` only name top-level envelope shape without field types. Mechanical tab-by-tab migration as originally queued would be a quality regression — e.g. `RegisterRow` in `api.ts` has 17 typed fields, but generated `RegisterRow` types only `institution`. **Plan, two steps in order: (1) Expand `scripts/schemas.py`** — author Pydantic models covering the field-level shape of all ~55 response types currently in `api.ts` (Conviction, Cohort, FundPortfolio, CrossOwnership, TwoCompany, Crowding, ShortAnalysis, SectorFlows, PeerRotation, etc.). Estimate: 4-6 hours of schema authoring + test-per-endpoint to confirm no drift from actual responses. **(2) Regenerate `api-generated.ts`** (`npx openapi-typescript http://localhost:8001/openapi.json -o src/types/api-generated.ts`) **and migrate React tabs tab-by-tab**, deleting `api.ts` once full coverage is verified. **Do not attempt step 2 until step 1 is complete** — mechanical migration before schemas.py is expanded trades ~900 lines of compile-time field types for `unknown` casts. |
| BL-8 | Pre-commit cleanup — re-enable suppressed rules | Follow-on to ARCH-0A. **Progress updates:** ✅ W0212 **Done 2026-04-17** (`a7ca962`) — 3 intentional internal accesses inline-suppressed with documented reasons; ✅ W0603 **Done 2026-04-17** (`67e10ba`) — 8 module-level-cache globals inline-suppressed with documented reasons; ✅ W0613 **Done 2026-04-17** (`f886efd`) — 19 unused-args fixed (renames to `_arg`, one removal, inline-suppressions for kwarg callers). All three rules now globally enforced. Prior: ✅ ruff E702/E731, pylint W0611/W0702/E0611 (category a, 2026-04-16); ✅ ruff F541, pylint W0621/W0622 (category b, 2026-04-16 extended). **Still pending:** category (b) **pylint W0108 (2), W0640 (1), W1309 (9), W1510 (7), I1101 (2), E0606 (2), E0401 (2); bandit B101 (4), B108 (1), B310 (1), B404 (9), B607 (7)**. Category (c) bulk-fix required: **E501 (908), E402 (21), W0718 (163), B608 (239)** plus E701 (17), F841 (17), W0612 (31), W1514 (28), B110 (21), B603 (11). Ship rule-by-rule as small commits. Not a Phase 1 gate. |

---

## DECISION MAKER & VOTING ROLLUP WORLDVIEWS

New rollup types to coexist alongside economic_control_v1 in entity_rollup_history.
No schema changes required — new rows with different rollup_type values.

### decision_maker_v1
Purpose: Roll fund series up to the entity making active investment decisions,
not the fund sponsor/brand. Enables accurate active/passive classification,
flow analysis by actual decision-maker, and conviction scoring.

Example: Vanguard Wellington Fund → Wellington Management (not Vanguard)
Example: Hartford Growth Fund → Wellington Management (not Hartford)
Example: Polen Capital via Gotham Trust → Polen Capital (not Gotham)
Exception: Purely passive funds (index, ETF) with no sub-adviser → roll to
brand/sponsor as before (economic_control_v1 = decision_maker_v1 for passive)

UI: Global toggle on all tabs switching between economic_control_v1 and
decision_maker_v1. Affects Register, Conviction, Flow Analysis, Ownership Trend,
Cross-Ownership, Peer Matrix.

Data sources (in priority order):
Phase 1 — N-CEN sub-adviser relationships (ncen_adviser_map, already in pipeline)
Phase 2 — ADV Schedule D Section 7.B (already parsed in adv_schedules.csv,
           needs extraction pass — no new PDF parsing required)
Phase 3 — N-PORT fund metadata sub-adviser fields (already in pipeline,
           needs extraction pass)
Deferred — Fund prospectus/SAI (N-1A) PDF parsing — comprehensive but requires
            new PDF parsing pipeline similar to ADV work

Coverage estimate:
N-CEN only: ~60-70% of sub-advised AUM
N-CEN + ADV Schedule D: ~85%
N-CEN + ADV + N-PORT: ~90%+

### voting_control_v1 — CLOSED 2026-04-10
economic_control_v1 serves as proxy for voting authority. Fund sponsor votes
shares in >95% of cases. Separate voting rollup adds false precision without
analytical value for ownership/flow analysis. UI toggle will label
economic_control_v1 as 'Fund Sponsor / Voting' to make this explicit.

### Implementation sequence
| # | Item | Priority | Notes |
|---|------|----------|-------|
| DM1 | Build decision_maker_v1 rollup — Phase 1 (N-CEN) | Done | Built 2026-04-10. 2,389 N-CEN sub-adviser routings inserted into entity_rollup_history with rule_applied='ncen_sub_adviser'. 2,371 (99.2%) produced a different rollup from economic_control_v1. Passive funds and entities without sub-advisers copied from economic_control_v1. Total decision_maker_v1 rows: 20,205 (matches economic_control_v1). All 541 unique sub-advisers resolved via CRD match. |
| DM4 | Global UI toggle — Fund Sponsor/Voting vs Decision Maker | Done | Built 2026-04-10. Header dropdown in index.html. window.fetch override in app.js auto-appends rollup_type to all /api/ URLs. app.py _get_rollup_type() helper validates and passes to 12+ parameterized endpoints. Default: economic_control_v1. Invalid values fall back safely. Fund-level NVDA comparison confirmed: Fidelity $261B splits to FMR core $113B + Fidelity HK $86B + FMR UK $34B under decision_maker_v1. 674 NVDA fund positions ($247B) route differently. |
| DM7 | Coverage report and validation | Done | 2,389 N-CEN sub-adviser routings, 100% CRD match rate. Validation cases confirmed: Hartford Growth → Schroder, Alger Small Cap Focus → Redwood Investments, Vanguard Wellington Fund → Wellington Management, Vanguard 500 Index → Vanguard (passive unchanged). Fund-level Fidelity fragmentation flagged as DM8 for future refinement. |
| DM2 | Build decision_maker_v1 rollup — Phase 2 (ADV Schedule D) | Deferred | Audit 2026-04-10: adv_schedules.csv contains only Schedule A/B (ownership) — no Section 7.B (Financial Industry Affiliations / sub-adviser) data. Requires targeted ADV Section 7.B re-parse against existing PDF cache (3,654 PDFs already on disk, no new downloads). Parser extension needed to extract sub-adviser relationships from Section 7.B. Deferred to Phase 3.5b ADV pipeline work (item D12). |
| DM3 | Build decision_maker_v1 rollup — Phase 3 (N-PORT metadata) | Deferred | Audit 2026-04-10: fund_holdings_v2 has no sub-adviser metadata fields — not extracted during N-PORT fetch. Requires fetch_nport.py extension to capture sub-adviser fields from N-PORT XML where present. Deferred to pipeline work (item D13). |
| DM4 | Global UI toggle — Fund Sponsor / Voting vs Decision Maker | High | Add rollup_type selector to app.py config. Pass rollup_type parameter to all 34 query functions in queries.py. Toggle affects Register, Conviction, Flow Analysis, Ownership Trend, Cross-Ownership, Peer Matrix simultaneously. Two options only: 'Fund Sponsor / Voting' (economic_control_v1, default) and 'Decision Maker' (decision_maker_v1). |
| DM5 | Build voting_control_v1 rollup | CLOSED | economic_control_v1 serves as proxy for voting authority. Fund sponsor votes shares in >95% of cases. Separate voting rollup adds false precision without analytical value. UI toggle labels economic_control_v1 as 'Fund Sponsor / Voting' to make this explicit. |
| DM6 | N-1A prospectus/SAI parsing pipeline | Low | Comprehensive sub-adviser coverage via fund prospectus. Requires new PDF parsing pipeline similar to ADV work. Deferred until DM1-DM3 coverage assessed. Add to pipeline backlog after Phase 3.5b. |
| DM7 | Coverage report and validation | High | After DM1-DM3: report coverage % by rollup type, AUM under each rollup, top 20 decision-maker re-routings with before/after. Validate Wellington/Vanguard, Polen/Gotham, Hartford/Wellington cases explicitly. |
| DM8 | Intra-firm decision_maker_v1 routing refinement | Done | 2026-04-10: 621 intra-firm cases collapsed via three-tier matching: (1) shared economic_control_v1 rollup (485 cases: Fidelity Contrafund→FMR HK collapsed to Fidelity/FMR); (2) entity_relationships parent link (17 cases); (3) name-based brand matching for self-rollup subsidiaries (119 cases: DFA Australia, T. Rowe Price International, BlackRock Singapore collapsed to parent brand). Top 15 now shows only cross-firm routings: BlueCove ($178B from Fidelity), GQG Partners ($107B from Goldman), Franklin Advisers ($83B from Putnam), T. Rowe Price ($88B from Strategic Advisers Fidelity), Jennison ($58B from Harbor), Sands Capital ($52B from Bessemer), Wellington ($36B from Hartford). |
| DM9 | nport_coverage_pct on summary_by_parent | Done | 2026-04-10: Added `nport_coverage_pct`, `total_nport_aum`, `rollup_entity_id`, `rollup_name` columns to summary_by_parent. Rebuilt Q4 from holdings_v2 + fund_holdings_v2 with rollup-based aggregation (8,417 rows). Top by coverage: Vanguard 100%, Fidelity 100%, Capital Group 100%, MFS 100%, Principal 100%. BlackRock 61% (structural — large institutional separate account AUM not captured in N-PORT; expected behavior, not a data quality issue). State Street 26% (similar). Bottom: Citadel/DE Shaw/Jane Street/Susquehanna/Millennium all 0% (hedge funds don't file N-PORT). Zero parents >100%. |
| DM10 | N-PORT Cov. badges on Register + Conviction | Done | 2026-04-10: `nport_cov` field added to query1 (Register) and query3 (Conviction) responses. Frontend renders color-coded badges: ≥80% green, 50-79% amber, 1-49% grey, 0/null no badge. Column header tooltip explains semantics. CSS badges in Oxford Blue scheme. |
| DM11 | L4 Fund Portfolio rollup context panel | Done | 2026-04-10: New `/api/fund_rollup_context` endpoint accepts `cik` or `series_id`, returns both economic_control_v1 and decision_maker_v1 rollup names plus `same` flag. renderQuery7 fetches context and renders info panel above holdings table. Panel shows single row if rollups match, two rows if they differ. Oxford Blue left border, compact Arial. Hidden if entity not found. Tested with Strategic Advisers Fidelity US Total Stock (Fidelity/FMR → BlueCove Limited, same=false). |
| DM12 | Umbrella fund trust routing cleanup | Done 2026-04-10 | _Completed via staging workflow (INF1)._ 2026-04-10: N-CEN "adviser" field lists ONE firm as primary adviser for entire umbrella fund trusts that host many unrelated managers. This routes 200+ series incorrectly to a single firm. AICF (Advisors' Inner Circle Fund) fixed: 24 series manually routed to actual managers (Acadian, Cambiar, LSV, Rice Hall James, Sands Capital, CIBC Private Wealth, Hamlin, Sarofim, Edgewood, Haverford). 2026-04-10 DM12 investigation pass: of 17 flagged trusts, 14 already correctly routed (Glenmede, Manning & Napier, Innealta, Muzinich, Sterling Capital, Kennedy Capital, Bretton, Counterpoint, Tuttle, Polaris, Vontobel, etc.); 4 are false positives from keyword match (Heard Capital, AmFam, Quantum, Inspire — all private-fund/insurance arms with no N-CEN presence, correctly self-rollup); 3 trusts have specific issues but are symptoms of broader problems now tracked as DM13/DM14/DM15. DM12 narrow fixes scoped to: (1a) Securian/Sterling false ADV match deletion, (1b) HC Capital Trust 5 sub-adviser routings, (1c) Catholic Responsible Investments 4 sub-adviser routings. |
| DM13 | ADV_SCHEDULE_A relationship quality audit | **BlueCove sweep Done 2026-04-16** (`ef3f302`) — full audit remaining | _Staging workflow required (INF1)._ 2026-04-10 discovered via Securian/Sterling root-cause. 410 suspicious wholly_owned/parent_brand/mutual_structure relationships where parent and child names share zero word overlap after stopword removal. **First pass complete 2026-04-16:** BlueCove Limited N-CEN false-match cluster closed — all 20 `sub_adviser` relationships with parent=18338 closed, 17 `decision_maker_v1` rollups retargeted to correct EC parents (3 Strategic Advisers Fidelity → FMR LLC shifting $235.48B+ back; 9 Boston Partners + 2 WPG → Boston Partners; 3 Matson Free Market → Matson Money; 2 Altair), 15 sub-adviser parents classified unknown→active (Voya IM, BlackRock IM, JPM IM, T. Rowe Price ×3, DFA ×2, Allspring, Goldman, Schroders, BlackRock Intl, Milliman, BlueCove, Research Affiliates); 35 `entity_overrides_persistent` rows written (20 suppress_relationship + 15 reclassify); compute_flows + build_summaries rebuilt. Broader 410-row audit of non-BlueCove false matches remains pending. Root cause: ADV parser matched entity names without external verification. Example: Sterling Financial Group ($16M RIA, Pasadena CA) incorrectly wired as parent of Securian Asset Management ($43.8B, St Paul MN). Other examples: BGL BNP Paribas → AXA, Blue Owl → Whitebox, Engaged Capital → Harbor Investment Advisory, CNA Financial → Cabana, NYL Investors → Millennium, Cannae Holdings → JANA, Soroban → 1492. Some are legit (Natixis → AEW, Natixis → Mirova, Perpetual → JOHCM). Six-tier verification pipeline: (1) AUM sanity — parent ADV AUM ≥ 50% of child; (2) State/city match via adv_managers; (3) EDGAR full-text search for parent in child's ADV; (4) CRD cross-reference from Schedule A PDF; (5) OpenCorporates state-of-incorporation; (6) Web search for ambiguous residuals. Pre-insert Tier 1+2 check now live in entity_sync.insert_adv_ownership() — failing relationships routed to entity_identifiers_staging with conflict_reason='adv_ownership_verification_failed'. |
| DM14 | DM8 extension for unlabeled intra-firm sub-advisers | **Layer 1 Done 2026-04-16** (`d684e4e`) — Layer 2 (DM14b) pending | _Staging workflow required (INF1)._ 2026-04-10: discovered during sub-adviser gap scope scan. Chain-walk audit 2026-04-16 of 1,487 `sub_adviser` rels currently DM-routed to the sub-adviser (combined fund AUM $2,118.45B) found 8 relationships / 4 sub-advisers / **$12.15B AUM** with bilateral `wholly_owned`/`parent_brand` chains supporting automatic collapse today. Applied via `scripts/dm14_layer1_apply.py` + INF1 staging workflow (snapshot `20260416_212955`): 8 `entity_rollup_history(decision_maker_v1)` rows SCD-retargeted (`ncen_sub_adviser` → `manual_override`) + 8 `entity_overrides_persistent` rows (IDs 83-90, rollup_type=`decision_maker_v1`, identifier_type=`series_id`). Retargets: 4 AMG Yacktman ($9.56B) + 1 AMG Frontier ($0.09B) → AMG (eid=8968); 2 Vaughan Nelson Natixis ($2.50B) → Natixis IM (eid=8386); 1 Calvert EM ($0.01B) → MORGAN STANLEY (eid=2920). **Side-effect:** INF22 prod override_id heal (58 NULL → 25-82 via `_heal_override_ids`) landed atomically in the same promote. compute_flows + build_summaries --rebuild rebuilt; validate_entities.py prod 8 PASS / 2 FAIL / 6 MANUAL preserved. Bigger ROADMAP-named candidates (Manulife IM / FIAM / Principal RE / Voya / Davis / PGIM Ltd / Cohen & Steers Asia — ~$170B AUM) deferred as **DM14b** below because the entity graph lacks the required `wholly_owned` / `parent_brand` edges. |
| DM14b | Graph-completion for ROADMAP-named intra-firm collapses | **Done 2026-04-17** | _Staging workflow required (INF1)._ 2026-04-16: DM14 chain-walk audit discovered that 7 high-AUM candidate clusters (**~$170B combined AUM, 123 funds**) cannot be collapsed today because `entity_relationships` is missing the connecting `wholly_owned` / `parent_brand` edges. Once each edge is added (ADV Schedule A/S corroborated per firm), the existing DM14 Layer 1 chain walk picks them up with no code changes. **Missing edges:** (1) MANULIFE IM (US) LLC 10538 ↔ MANUFACTURERS LIFE INSURANCE 8179 — 48 funds / $70.84B (10538 already rolls to 8994 Manulife Financial Corp, need 8179→8994 or 8179→10538); (2) FIAM LLC 9910 → FMR LLC 10443 — 3 funds / $43.13B (FIAM has no upward edges); (3) PRINCIPAL REAL ESTATE 8652 ↔ Principal Financial seed 54 — 4 funds / $33.28B (8652 rolls to 7316/6700, need 54 wired into tree); (4) Voya IM Co 17915 → VOYA INVESTMENTS 4071 — 49 funds / $21.81B (neither side has edges); (5) Davis Selected Advisers NY 17975 → Davis Selected Advisers 3703 — 11 funds / $14.36B; (6) PGIM Limited 18190 → PGIM, Inc. 1589 — 3 funds / $5.55B; (7) Cohen & Steers Asia 18044 → COHEN & STEERS CAPITAL MANAGEMENT 142 — 5 funds / $4.92B. Work order: one entity at a time, ADV Schedule A verification before insertion. Re-run `scripts/dm14_layer1_apply.py` (with refreshed CANDIDATES list) after edges land. |
| DM15 | External sub-adviser coverage pass | **Done 2026-04-17** | _Staging workflow required (INF1)._ 2026-04-10: discovered during sub-adviser gap scope scan. ~300-500 series with real external sub-adviser gaps (non intra-firm). Total AUM affected ~$549.7B in latest quarter. Top gap trusts by count: Eq Advisors Trust (50 series), JNL Series Trust (47), Nationwide Variable Insurance Trust (34), Lincoln Variable Insurance Products Trust (29), Voya Partners (27), Transamerica Series Trust (27), Nationwide Mutual Funds (21), WisdomTree Trust (20), Voya Variable Portfolios (17). Top individual gap series by AUM: Victory Portfolios III S000012915 ($28.2B) → Loomis Sayles; Principal Funds S000007016 ($20.3B) → Hotchkis & Wiley; etc. Fix: for each gap series, route decision_maker_v1 to external sub-adviser entity from ncen_adviser_map. routing_confidence='high', source='N-CEN'. Dependency: complete DM13 + DM14 first. **2026-04-10 L5 batch 1 (parents 201-300) candidate added: FundVantage Trust / Gotham Asset Management umbrella — N-CEN registrant `FundVantage Trust` lists Gotham as adviser for 19 series, but the funds are externally managed by Polen Capital (Polen International Growth, Polen Emerging Markets, Polen Global Growth, Polen Growth, Polen U.S. Small Company Growth, Polen U.S. SMID, Polen Global SMID), EIC (EIC Value Fund), and Private Capital Management (Private Capital Management Value Fund). Plus Quality Dividend Fund and ~9 more. Same AICF pattern. Re-route decision_maker_v1 to actual sub-advisers per series.** **2026-04-10 L5 batch 2 (parents 301-400) candidates added: (1) ALPS Advisors Inc umbrella — ALPS is a fund administrator (DST/SS&C subsidiary), ~24 fund children include multiple external sub-advisers (Emerald Insights, Emerald Growth, Emerald Finance & Banking Innovation, Seafarer Overseas Growth, Seafarer Overseas Value, Disciplined Growth Investors, Vulcan Value Partners Small Cap, Vulcan Value Partners). Same FundVantage/Gotham pattern. Re-route decision_maker_v1 to actual sub-advisers. (2) NEOS Investment Management → FIS Christian Stock Fund — single mis-route; NEOS is an ETF brand, FIS is unrelated. Check for broader pattern in NEOS's other 17 fund children. (3) Brandes Investment Partners → Stewart Investors Worldwide Leaders Fund — single mis-route; Stewart Investors is a sub-brand of First Sentier Investors (MUFG), not Brandes. Check for broader pattern.** **2026-04-10 L5 batch 3 (parents 401-500) candidates added: (1) Mairs & Power Inc umbrella (~11 series) — N-CEN lists Mairs & Power as adviser for Jensen Quality Mid Cap, Jensen Global Quality Growth, Column Mid Cap Select, Column Small Cap Select, Column Mid Cap, Column Small Cap, Rockefeller Climate Solutions, Rockefeller US Small Cap Core, plus 3 more. Same FundVantage/Gotham pattern — Trust for Professional Managers or similar umbrella. Re-route decision_maker_v1 to actual sub-advisers. (2) Champlain Investment Partners umbrella — N-CEN lists Champlain as adviser for Kopernik Global All-Cap Fund and Vontobel Global Environmental Change. Same pattern, ~2+ series. (3) Frontier Capital Management → Vanguard Explorer Value Fund — single mis-route in econ_control_v1. Vanguard Explorer Value is a Vanguard multi-manager fund where Frontier is a sub-adviser. Under econ_control_v1 should be Vanguard (fund sponsor); under decision_maker_v1, Frontier as decision maker is correct. Fix only the econ_control_v1 routing.** **2026-04-10 L5 batch 4 (parents 501-600) candidates added: (1) Focus Partners Advisor Solutions → ~10 SA-prefixed funds (SA International Small Company, SA Emerging Markets Value, SA U.S. Core Market, SA U.S. Value, SA Worldwide Moderate Growth, SA International Value, SA U.S. Small Company, SA Real Estate Securities) — likely Loring Ward / Buckingham Strategic Wealth funds (Focus Partners owns Buckingham). Same FundVantage/Gotham pattern. (2) Valmark Advisers → BTS Tactical Fixed Income VIT Fund — single mis-route. (3) Horizon Investments → Centre American Select Equity, Centre Global Infrastructure Fund — Centre Asset Management is separate, ~2 series. (4) **Bridges Investment Management → Akre Focus Fund, Boston Common ESG Impact, The Osterweis Fund, Congress Mid Cap Growth, Congress Small Cap Growth — Bridges is a small Omaha NE manager hosting major external sub-advisers in an umbrella trust. Notable: Akre Focus is a major fund. ~5 series.** (5) Manning & Napier → Callodine Equity Income Series — single (M&N otherwise hosts its own funds). (6) First Pacific Advisors → Manteio Multialternative Strategy Fund — single (FPA otherwise hosts its own funds). (7) Segall Bryant Hamill → Barrett Opportunity Fund — single (SBH otherwise hosts its own funds).** **2026-04-10 L5 batch 5 (parents 601-700) candidates added: (1) Capital Advisors Inc /OK umbrella — ~22 series (O'Shaughnessy Market Leaders Value, First Sentier Global Listed Infrastructure, First Sentier American Listed Infrastructure, Huber Mid Cap Value, Huber Select Large Cap Value, Huber Large Cap Value, Huber Small Cap Value, plus 14+ more). Trust for Professional Managers pattern. (2) Sterling Capital Management → Capitol Series Trust — ~14+ series (Nightview, Reynders McVeigh, Alta Quality, Guardian Capital Fundamental Global Equity, Guardian Capital Dividend Growth, FullerThaler Behavioral Unconstrained Equity, FullerThaler Behavioral Mid-Cap Equity, FullerThaler Behavioral Small-Mid Core Equity, plus more). Sterling Capital Funds (own funds) is clean from Section 1; Capitol Series Trust is the umbrella with external sub-advisers. (3) Crawford Investment Counsel → Dean Equity Income, Dean Small Cap Value, Absolute Capital Opportunities, FIIG Stock Fund (~4 series, Trust for Professional Managers pattern). (4) AmFam/AAM — partially fixed in batch 5 (11 funds re-routed to AAM with sub-adviser DM routing). Remaining AAM-sponsored funds with sub-advisers not yet in current DM15 scope.** **2026-04-10 L5 batch 6 (final, parents 701-720) candidate added: Donald Smith & Co → Vanguard Selected Value Fund — single econ_control_v1 mis-route. Vanguard Selected Value is a Vanguard multi-manager fund where Donald Smith is one of the sub-advisers; same Frontier/Vanguard Explorer pattern from L5 batch 3. Fix only econ_control_v1 to point to Vanguard; under decision_maker_v1, Donald Smith as decision maker is correct.** **2026-04-10 Section 3 L4 audit candidate added: Stonegate Global Fund Services (eid=20201) — fund administrator/wrapper for alternative assets (crypto, DeFi, VC, PE). Same FundVantage/ALPS umbrella pattern. Per operating-AM rollup policy, individual sub-advisers are the decision makers; Stonegate is just the administrator. Re-route decision_maker_v1 to sub-advisers listed in N-CEN Part C. Currently classified `strategic` as interim placeholder (was `holding_company` before Section 3 cleanup).** **L5 PARENTS 201-720 AUDIT COMPLETE 2026-04-10: 520 parents scanned across 6 batches. Cumulative: 28 ADV/parent_bridge mis-match relationships fixed (5.4% fix rate), 19 DM15 umbrella-trust candidates flagged, 4 entity fragmentation candidates flagged (INF5/6/7/8), ~$700B in holdings_v2 re-stamped. All 6 batches: 9 PASS / 0 FAIL / 7 MANUAL throughout. Pattern observation: ADV mis-match rate decreases and umbrella-trust pattern rate increases as parent AUM decreases — mid-tier managers more likely to host funds via umbrella trusts (FundVantage, Trust for Professional Managers, Capitol Series Trust, Northern Lights, etc.) rather than register their own ADV with explicit ownership chains.** **L4 CLASSIFICATION AUDIT COMPLETE 2026-04-10: 30 entity_classification_history rows changed across 6 sub-sections. (3a) Added new `market_maker` classification — 23 entities reclassified (Susquehanna, Jane Street, IMC, Optiver, CTC, Citadel Securities, Virtu, HRT, Two Sigma Securities, Flow Traders, DRW Securities — pulled out of `hedge_fund`, `quantitative`, `mixed`, `unknown`). ~$2.5T current-quarter AUM moved out of hedge_fund + quantitative. (3b) Singleton outlier classifications removed: `insurance` (1 entity → pension_insurance), `foundation` (1 → endowment_foundation), `holding_company` (2 → strategic + wealth_management). Active classification count: 17 → 15 (-3 singletons + 1 new market_maker). (3c) Activist flag inconsistencies fixed: Mantle Ridge LP (is_activist FALSE → TRUE), Triangle Securities Wealth (is_activist TRUE → FALSE). (3d) N-PORT cross-check (read-only) — 3 mismatches flagged as L4-2 follow-up. (3e) Mixed classification reviewed (no changes; bank holdcos legitimate; flagged Franklin Templeton/PIMCO/Nomura mis-classifications as L4-1). (3f) Strategic classification reviewed; SC US (TTGP) — Sequoia Capital US — moved from strategic → venture_capital. Stonegate Global Financial flagged as DM15 candidate (fund administrator umbrella). All 9 PASS / 0 FAIL / 7 MANUAL throughout.** |
| DM15b | N-CEN coverage expansion — 10 gap registrant trusts | **Done 2026-04-17** (`9ce5b17`) | _Scoped `fetch_ncen.py --ciks` (new flag) bypasses fund_universe + processed filter._ 10 fund trust CIKs confirmed on EDGAR with 1-9 N-CEN filings each; silent-fetch-failures in prior runs explained the gap. Scoped fetch delivered **+103 adviser-series rows** (0 errors). `ncen_adviser_map` 11,106→11,209. 81/84 fund_universe series now covered. 22 sub-adviser rows identified across 17 series / $8.95B AUM — unblocked DM15 Layer 2. **5 Securian SFT series ($1.26B AUM) remain uncovered** — no N-CEN sub-adviser rows despite N-PORT presence; accepted residual gap, out of DM15 scope. |
| DM15 Layer 2 | External sub-adviser DM retargets from DM15b ingestion | **Done 2026-04-17** (`938e435`) | _Staging workflow (INF1)._ 17 sub-adviser DM retargets / **$8.95B** AUM / 7 sub-advisers (all already in MDM): Voya IM Co 17915 (4 series), MacKay Shields 6290 (6), Spectrum AM 10034 (2), Winslow 5046 (2), Principal Real Estate 8652 (1), Dynamic Beta 19093 (1), CBRE IM 11166 (1). CBRE tiebreak: chose eid=11166 (CIK+CRD, $6.81B) over eid=18645 shell — latter **flagged as merge candidate**. Source retargets: 9 NYLI IM + 3 Principal Global + 1 SEI IM (fund_sponsor→sub-adviser), 4 Voya Financial Inc DM14c (manual_override→Voya IM Co). Override IDs 205-221 (5 NULL-CIK per INF9d/DM15 L1 precedent; migration 007 handles — audit correction from 4; see `docs/SYSTEM_AUDIT_2026_04_17.md` §6.1 R-02). |
| DM15c | Amundi SA global parent correction | **Done 2026-04-17** (`9160030`) | _Staging workflow (INF1)._ Corrected `parent_bridge_sync` Taiwan-as-global-parent artifact. **eid=2214** (CIK 0001330387, CRD 334151, Paris HQ, already in MDM) renamed "Amundi"→"Amundi SA", reclassified passive→active; global rollup target on both worldviews. **12 Amundi geo entities rerouted** (24 rollup retargets + 24 override rows IDs 222-245): Austria 1318, Hong Kong 1414, Italy SGR 3217, Singapore 3975, France AM 4248, Germany 4667, Japan 5403, Czech AM 6006, Czech Investicni 7079, UK 8338, Ireland 10266 (was misrouted to US Ameraudi eid=4294), Taiwan 752 itself (also reclassified unknown→active). All targets carry CIK — no replay gaps. |
| DM15 umbrella-trusts audit | Deferred (different scope from DM15b N-CEN coverage) | Blocked on D13 / DM6 | **~132 series / ~$105B AUM across 10 umbrella trusts** (Sterling Capital / Gotham / NEOS / Segall Bryant / Mairs & Power / Brandes / Crawford / Bridges / Champlain / FPA). Originally tagged as part of the L5 batches 1-6 DM15 scope below. Blocked: `ncen_adviser_map` has no `subadviser` rows for these trusts (they report single adviser in N-CEN; sub-advisers only surface in N-PORT XML item C.10/C.11 or N-1A prospectus). Requires D13 (N-PORT XML sub-adviser extraction) or DM6 (N-1A parsing). |
| DM15 | External sub-adviser coverage pass (umbrella entry, superseded by Layer 1 / Layer 2 / DM15b / DM15c / umbrella-trusts rows above) | **Done 2026-04-17** | _Staging workflow required (INF1)._ 2026-04-10: discovered during sub-adviser gap scope scan. ~300-500 series with real external sub-adviser gaps (non intra-firm). Total AUM affected ~$549.7B in latest quarter. Top gap trusts by count: Eq Advisors Trust (50 series), JNL Series Trust (47), Nationwide Variable Insurance Trust (34), Lincoln Variable Insurance Products Trust (29), Voya Partners (27), Transamerica Series Trust (27), Nationwide Mutual Funds (21), WisdomTree Trust (20), Voya Variable Portfolios (17). Top individual gap series by AUM: Victory Portfolios III S000012915 ($28.2B) → Loomis Sayles; Principal Funds S000007016 ($20.3B) → Hotchkis & Wiley; etc. Fix: for each gap series, route decision_maker_v1 to external sub-adviser entity from ncen_adviser_map. routing_confidence='high', source='N-CEN'. Dependency: complete DM13 + DM14 first. **2026-04-10 L5 batch 1 (parents 201-300) candidate added: FundVantage Trust / Gotham Asset Management umbrella — N-CEN registrant `FundVantage Trust` lists Gotham as adviser for 19 series, but the funds are externally managed by Polen Capital (Polen International Growth, Polen Emerging Markets, Polen Global Growth, Polen Growth, Polen U.S. Small Company Growth, Polen U.S. SMID, Polen Global SMID), EIC (EIC Value Fund), and Private Capital Management (Private Capital Management Value Fund). Plus Quality Dividend Fund and ~9 more. Same AICF pattern. Re-route decision_maker_v1 to actual sub-advisers per series.** **2026-04-10 L5 batch 2 (parents 301-400) candidates added: (1) ALPS Advisors Inc umbrella — ALPS is a fund administrator (DST/SS&C subsidiary), ~24 fund children include multiple external sub-advisers (Emerald Insights, Emerald Growth, Emerald Finance & Banking Innovation, Seafarer Overseas Growth, Seafarer Overseas Value, Disciplined Growth Investors, Vulcan Value Partners Small Cap, Vulcan Value Partners). Same FundVantage/Gotham pattern. Re-route decision_maker_v1 to actual sub-advisers. (2) NEOS Investment Management → FIS Christian Stock Fund — single mis-route; NEOS is an ETF brand, FIS is unrelated. Check for broader pattern in NEOS's other 17 fund children. (3) Brandes Investment Partners → Stewart Investors Worldwide Leaders Fund — single mis-route; Stewart Investors is a sub-brand of First Sentier Investors (MUFG), not Brandes. Check for broader pattern.** **2026-04-10 L5 batch 3 (parents 401-500) candidates added: (1) Mairs & Power Inc umbrella (~11 series) — N-CEN lists Mairs & Power as adviser for Jensen Quality Mid Cap, Jensen Global Quality Growth, Column Mid Cap Select, Column Small Cap Select, Column Mid Cap, Column Small Cap, Rockefeller Climate Solutions, Rockefeller US Small Cap Core, plus 3 more. Same FundVantage/Gotham pattern — Trust for Professional Managers or similar umbrella. Re-route decision_maker_v1 to actual sub-advisers. (2) Champlain Investment Partners umbrella — N-CEN lists Champlain as adviser for Kopernik Global All-Cap Fund and Vontobel Global Environmental Change. Same pattern, ~2+ series. (3) Frontier Capital Management → Vanguard Explorer Value Fund — single mis-route in econ_control_v1. Vanguard Explorer Value is a Vanguard multi-manager fund where Frontier is a sub-adviser. Under econ_control_v1 should be Vanguard (fund sponsor); under decision_maker_v1, Frontier as decision maker is correct. Fix only the econ_control_v1 routing.** **2026-04-10 L5 batch 4 (parents 501-600) candidates added: (1) Focus Partners Advisor Solutions → ~10 SA-prefixed funds (SA International Small Company, SA Emerging Markets Value, SA U.S. Core Market, SA U.S. Value, SA Worldwide Moderate Growth, SA International Value, SA U.S. Small Company, SA Real Estate Securities) — likely Loring Ward / Buckingham Strategic Wealth funds (Focus Partners owns Buckingham). Same FundVantage/Gotham pattern. (2) Valmark Advisers → BTS Tactical Fixed Income VIT Fund — single mis-route. (3) Horizon Investments → Centre American Select Equity, Centre Global Infrastructure Fund — Centre Asset Management is separate, ~2 series. (4) **Bridges Investment Management → Akre Focus Fund, Boston Common ESG Impact, The Osterweis Fund, Congress Mid Cap Growth, Congress Small Cap Growth — Bridges is a small Omaha NE manager hosting major external sub-advisers in an umbrella trust. Notable: Akre Focus is a major fund. ~5 series.** (5) Manning & Napier → Callodine Equity Income Series — single (M&N otherwise hosts its own funds). (6) First Pacific Advisors → Manteio Multialternative Strategy Fund — single (FPA otherwise hosts its own funds). (7) Segall Bryant Hamill → Barrett Opportunity Fund — single (SBH otherwise hosts its own funds).** **2026-04-10 L5 batch 5 (parents 601-700) candidates added: (1) Capital Advisors Inc /OK umbrella — ~22 series (O'Shaughnessy Market Leaders Value, First Sentier Global Listed Infrastructure, First Sentier American Listed Infrastructure, Huber Mid Cap Value, Huber Select Large Cap Value, Huber Large Cap Value, Huber Small Cap Value, plus 14+ more). Trust for Professional Managers pattern. (2) Sterling Capital Management → Capitol Series Trust — ~14+ series (Nightview, Reynders McVeigh, Alta Quality, Guardian Capital Fundamental Global Equity, Guardian Capital Dividend Growth, FullerThaler Behavioral Unconstrained Equity, FullerThaler Behavioral Mid-Cap Equity, FullerThaler Behavioral Small-Mid Core Equity, plus more). Sterling Capital Funds (own funds) is clean from Section 1; Capitol Series Trust is the umbrella with external sub-advisers. (3) Crawford Investment Counsel → Dean Equity Income, Dean Small Cap Value, Absolute Capital Opportunities, FIIG Stock Fund (~4 series, Trust for Professional Managers pattern). (4) AmFam/AAM — partially fixed in batch 5 (11 funds re-routed to AAM with sub-adviser DM routing). Remaining AAM-sponsored funds with sub-advisers not yet in current DM15 scope.** **2026-04-10 L5 batch 6 (final, parents 701-720) candidate added: Donald Smith & Co → Vanguard Selected Value Fund — single econ_control_v1 mis-route. Vanguard Selected Value is a Vanguard multi-manager fund where Donald Smith is one of the sub-advisers; same Frontier/Vanguard Explorer pattern from L5 batch 3. Fix only econ_control_v1 to point to Vanguard; under decision_maker_v1, Donald Smith as decision maker is correct.** **2026-04-10 Section 3 L4 audit candidate added: Stonegate Global Fund Services (eid=20201) — fund administrator/wrapper for alternative assets (crypto, DeFi, VC, PE). Same FundVantage/ALPS umbrella pattern. Per operating-AM rollup policy, individual sub-advisers are the decision makers; Stonegate is just the administrator. Re-route decision_maker_v1 to sub-advisers listed in N-CEN Part C. Currently classified `strategic` as interim placeholder (was `holding_company` before Section 3 cleanup).** **L5 PARENTS 201-720 AUDIT COMPLETE 2026-04-10: 520 parents scanned across 6 batches. Cumulative: 28 ADV/parent_bridge mis-match relationships fixed (5.4% fix rate), 19 DM15 umbrella-trust candidates flagged, 4 entity fragmentation candidates flagged (INF5/6/7/8), ~$700B in holdings_v2 re-stamped. All 6 batches: 9 PASS / 0 FAIL / 7 MANUAL throughout. Pattern observation: ADV mis-match rate decreases and umbrella-trust pattern rate increases as parent AUM decreases — mid-tier managers more likely to host funds via umbrella trusts (FundVantage, Trust for Professional Managers, Capitol Series Trust, Northern Lights, etc.) rather than register their own ADV with explicit ownership chains.** **L4 CLASSIFICATION AUDIT COMPLETE 2026-04-10: 30 entity_classification_history rows changed across 6 sub-sections. (3a) Added new `market_maker` classification — 23 entities reclassified (Susquehanna, Jane Street, IMC, Optiver, CTC, Citadel Securities, Virtu, HRT, Two Sigma Securities, Flow Traders, DRW Securities — pulled out of `hedge_fund`, `quantitative`, `mixed`, `unknown`). ~$2.5T current-quarter AUM moved out of hedge_fund + quantitative. (3b) Singleton outlier classifications removed: `insurance` (1 entity → pension_insurance), `foundation` (1 → endowment_foundation), `holding_company` (2 → strategic + wealth_management). Active classification count: 17 → 15 (-3 singletons + 1 new market_maker). (3c) Activist flag inconsistencies fixed: Mantle Ridge LP (is_activist FALSE → TRUE), Triangle Securities Wealth (is_activist TRUE → FALSE). (3d) N-PORT cross-check (read-only) — 3 mismatches flagged as L4-2 follow-up. (3e) Mixed classification reviewed (no changes; bank holdcos legitimate; flagged Franklin Templeton/PIMCO/Nomura mis-classifications as L4-1). (3f) Strategic classification reviewed; SC US (TTGP) — Sequoia Capital US — moved from strategic → venture_capital. Stonegate Global Financial flagged as DM15 candidate (fund administrator umbrella). All 9 PASS / 0 FAIL / 7 MANUAL throughout.** |

---

## COMPLETED

| Date | Item | Details |
|------|------|---------|
| 2026-04-21 | **obs-08 MINOR-16 O-05 backup-gap investigation — CLOSED (docs-only)** | Investigation of Atlas O-05 "Backup gap 3d12h; Apr 14 snapshots 1.6 G vs surrounding 2.6-2.7 G." **Finding: no infrastructure gap.** Backup infra already in place: `scripts/backup_db.py` (144 LOC EXPORT DATABASE helper with `--no-confirm`, `--list`, `--staging`), `Makefile` target `backup-db` (Makefile:136), and `quarterly-update` step 8 (`$(MAKE) backup-db`, Makefile:92). 12 snapshots on disk (`data/backups/`) spanning 2026-04-10 → 2026-04-19, ~31 G total. **Apr-14 size drop explained:** diff of parquet file lists shows `positions.parquet` present Apr 13, removed Apr 14 — corresponds to `DROP TABLE positions` in commit `d50b602` ("Batch 1 — drop positions, fix DDL drift, control plane to prod"). Schema rebound to 2.6 G by Apr 17 as DERA N-PORT S2, CUSIP v1.4 classifications, and manifest/impact tables landed (176 → 318 parquet files). **3d 12h gap Apr 14 → Apr 17 explained:** dev-intensive window (commits `7770f87`, `e4e6468`, `c31ffcb`, `d8a6a01`, `39d5e95`) with no `quarterly-update` invocation and no risky DB mutation; `promote_staging.py` intra-DB snapshots covered the interval per MAINTENANCE.md policy. **Remediation:** docs-only — fixed MAINTENANCE.md contradiction ("never on a schedule" vs `quarterly-update` step 8), added retention note (keep current-quarter + current-month; flag >50 G), refreshed last-updated to 2026-04-21. No script or Makefile changes. Full findings in `docs/findings/obs-08-p1-findings.md`. REMEDIATION_PLAN + REMEDIATION_CHECKLIST flipped to CLOSED. |
| 2026-04-17 | **DM audit close — DM13 / DM14 Layer 1 / DM14b / DM15 Layer 1 + INF23 + migrations 006/007 shipped** | Cumulative DM corrections this session: **134 fund series / ~$440B AUM** across 4 workstreams. **DM15 Layer 1** (`7bb68f5`, `scripts/dm15_layer1_apply.py`) — 15 external sub-adviser DM retargets via `ncen_adviser_map` role=`subadviser` (ALPS 7 / Valmark 6 / Focus Partners 1 / Manning & Napier 1 = ~$10.3B). Ships **migration 007** (`007_override_new_value_nullable.py`) — `ALTER TABLE entity_overrides_persistent ALTER COLUMN new_value DROP NOT NULL` to unblock NULL-CIK targets (Smith Capital + Milliman); INF9d precedent on the target side. Override IDs 91-105. **INF23** (`53d6e7b`, `scripts/inf23_apply.py`) — entity fragmentation cleanup: Morningstar IM 19596 merged into canonical 10513 (CIK `0001673385`), DFA 18096 merged into canonical 5026 (CIK `0000354204`), Milliman 18304 CIK `0001547927` backfilled from `adv_managers`, 6 DM15 NULL-CIK rows (98-103) repaired. 171 relationships re-pointed, 43 rollup targets re-pointed, 2 new `crd→cik` merge overrides (IDs 106-107). Smith Capital CIK deferred (no internal source). **DM14b** (`3c99365`, `scripts/dm14b_apply.py`) — 6 name-inferred `wholly_owned`/`parent_brand` edges (Manulife 8994→8179, FMR 10443→9910 FIAM, Principal 7316→54 seed, Davis 3703→17975 NY, PGIM 1589→18190 Ltd, Cohen & Steers 4595→18044 Asia) + chain-walk collapse of **91 fund series / $183.40B AUM** (Manulife 49 / $71.48B, FIAM 14 / $48.27B, Principal 5 / $36.66B, Davis 11 / $13.37B, PGIM 5 / $7.38B, C&S 7 / $6.24B). 91 override rows (IDs 108-198) all replay-clean. Single-script pattern — does **not** extend `dm14_layer1_apply.py`. Voya (17915↔4071) deferred as **DM14c** (49 funds / ~$21.81B, needs new `Voya Financial Inc` seed). **migration 006** (`ffccb92`) — `override_id_seq` + DEFAULT nextval + NOT NULL on `entity_overrides_persistent.override_id`. **Market refresh** — `fetch_market.py` delivered 10,064 tickers stamped 2026-04-16 23:27; `enrich_holdings.py --fund-holdings` +445K `market_value_live` / +127K `pct_of_float` / +488K `fund_holdings_v2.ticker`; `make freshness` PASS all 7 critical tables. **pending_entity_resolution status fix** — 279 rows flipped `pending`→`resolved` (entity records already existed, status never updated). True residual: 337 Tier C real + 1,186 deferred synthetics. Prod state end-of-session: `entity_overrides_persistent` 47→198, `entity_relationships` +6, `schema_versions` stamped 006+007, `validate_entities.py` 8 PASS / 1 FAIL (`wellington_sub_advisory` baseline) / 7 MANUAL preserved throughout. |
| 2026-04-17 | **BL-8 — W0212 / W0603 / W0613 globally enforced** | Three pylint rules moved from pre-commit global disable list to permanent-enforced. **W0212 protected-access** (`a7ca962`): 3 intentional internal accesses inline-suppressed with permanent-reason comments — `scripts/rollback_promotion.py:44,46` (sibling module sharing `_log` sink) and `scripts/sec_shares_client.py:319` (CLI self-test on own class). **W0603 global-statement** (`67e10ba`): 8 module-level-cache globals inline-suppressed — `scripts/db.py:23,28` (test/staging mode toggles), `scripts/fetch_nport.py:825` (`_include_index` CLI flag), `scripts/reparse_13d.py:41` + `scripts/reparse_all_nulls.py:36` + `scripts/resolve_bo_agents.py:64,73` (SEC/EFTS rate-limit timestamps), `scripts/fetch_13dg.py:41` (lazy edgar library import). **W0613 unused-argument** (`f886efd`): 19 fixes — renamed to `_arg` where the signature is part of a fixed protocol (`fetch_13dg_v2` discover/load_to_staging run_id, `validate_entities.gate_phase3_resolution_rate(con)`, `fetch_market.write_to_canonical`/`stamp_freshness` run_id, `fetch_dera_nport._prod_rows_for_ciks` quarter_label, `fetch_nport.download_xml` quarter_label, `fetch_13dg._retry_edgar` label, `entity_sync._stage_adv_verification_failure` reason + `insert_adv_ownership` ownership_code, `build_entities.step4_populate_relationships` series_to_entity, `queries.build_entity_graph` depth, `db.assert_write_safe` con); one removal (`queries.get_peer_rotation` unused quarter); four inline-suppressions where kwarg callers still pass the value (`queries.query6/query15`, `fetch_13dg.run_phase2` test_mode, `entity_sync._verify_adv_relationship` owner_name); file-level `# pylint: disable=W0613` on `scripts/retired/build_cusip_legacy.py` (not maintained). **Pre-commit disable list reduced** to `ruff: E501,E402,E701,F841` + `pylint: C,R,W0108,W0612,W0640,W0718,W1309,W1510,W1514,I1101,E0401,E0606`. Ruff + pylint + bandit green on all three commits. |
| 2026-04-16 | **Batch 3 close — `compute_flows.py` + `build_summaries.py` + migration 004 shipped** | Three-part delivery completes Batch 3 (legacy `holdings` retirement). **(1) Migration 004** (`scripts/migrations/004_summary_by_parent_rollup_type.py`, ~150 lines): adds `rollup_type VARCHAR` to `summary_by_parent`, changes PK from `(quarter, rollup_entity_id)` to `(quarter, rollup_type, rollup_entity_id)`. Idempotent rename+recreate+INSERT pattern (DuckDB can't ALTER PK in place). 8,417 existing rows preserved with `rollup_type='economic_control_v1'` stamp. Staging-first, then prod. **(2) `compute_flows.py` rewrite** (~370 lines): swapped from dropped legacy `holdings` to `holdings_v2`. Two new columns on `investor_flows` and `ticker_flow_stats` (`rollup_type`, `rollup_entity_id`); each per-period INSERT runs twice (EC + DM). `inst_parent_name` retained as back-compat = `rollup_name`. Value column = `market_value_usd` (Group 1, 100% complete) preserving legacy filing-date semantics. CLI: `--staging --dry-run`. Final row counts: `investor_flows` 9,380,507 → **17,396,524** (8,698,262 EC + 8,698,262 DM — identical because for 13F filings the rollups coincide for ~all entities); `ticker_flow_stats` 18,986 → **80,322**. Runtime **18.1s**. **(3) `build_summaries.py` rewrite** (~330 lines): same source swap + N-PORT integration via `fund_holdings_v2`. `summary_by_ticker.total_value` uses `COALESCE(market_value_live, market_value_usd)` so the table is correct both before/after `enrich_holdings.py` for the quarter. `summary_by_parent` adds true `total_nport_aum` from `fund_holdings_v2.market_value_usd` scoped to latest `report_month` per `series_id`; `nport_coverage_pct = MIN(100, total_nport_aum*100/total_aum)`. CLI: `--staging --dry-run --rebuild`. Final row counts: `summary_by_ticker` 24,570 → **47,642** (4 quarters × ~12K distinct tickers — broader because enrich_holdings.py populated tickers on REITs/CEFs/ADRs/warrants legacy dropped); `summary_by_parent` 8,417 → **63,916** (4 quarters × 2 worldviews × ~8K rollups). Runtime **4.6s**. **Worldview divergence visible for the first time** in `summary_by_parent`: Fidelity EC `total_nport_aum=$4.03T` vs DM `$3.57T` (sub-adviser routing); State Street EC $792.6B vs DM $790.6B; Vanguard/BlackRock/Capital Group identical EC=DM (no sub-adviser splits). `data_freshness` stamped on all 4 output tables. App back-compat preserved (`queries.py:745/1407/4263` reads land on EC rows by default; DM rows present for future use). Ruff + pylint 10/10 + bandit clean across all three scripts. **Legacy `holdings` retirement now fully closed.** |
| 2026-04-16 | **Batch 3 — `enrich_holdings.py` shipped + executed against prod** | New `scripts/enrich_holdings.py` (~480 lines, Pass A NULL cleanup → Pass B cusip-keyed UPDATE → Pass C `fund_holdings_v2.ticker`). Three pre-write design adjustments vs original proposal: switched from `(accession_number, cusip, quarter)` join key (1.29M dup groups, would corrupt `mvl`/`pof`) to lookup keyed by `cusip` (1:1 verified); switched `security_type_inferred` source from `cusip_classifications.canonical_type` (12.27M unwanted changes) to `securities.security_type_inferred` (0 changes, "values agree"); confirmed `fund_holdings_v2.ticker` real uplift is +1.45M not +6.7M (N-PORT CUSIP universe dominated by bonds/ABS). **Final prod deltas:** `holdings_v2.ticker` -831,467 (11.23M → 10.40M), `sti` 0 (unchanged), `market_value_live` -1,346,985 (10.87M → 9.53M), `pct_of_float` -1,873,408 (9.46M → 7.59M); `fund_holdings_v2.ticker` +1,447,216 (3.74M → 5.18M). `data_freshness('holdings_v2_enrichment')` stamped 2026-04-16 05:33:50 UTC, row_count=10,395,053. Pass B took 706.7s on 12.27M rows. NULL-tolerance spot-check on `query1/query3/query7` confirms readers handle the new NULL distribution (query1 AAPL returns 104 institutional rows, $2.52T total `value_live`, 67.23% `pct_float`, 5,797 institutions). Ruff + pylint 10/10 + bandit clean. CLI: `--staging --dry-run --quarter YYYYQN --fund-holdings`. Per-pass CHECKPOINT, per-run log to `logs/enrich_holdings_{ts}.log`. **Next unblocked:** `compute_flows.py` and `build_summaries.py` rewrites (still read legacy `holdings`). |
| 2026-04-13 | **BL-9 — `/api/v1/short_long` KeyError 'long_value_k' fixed** | Commit `9572844`. Two bugs in `get_short_long_comparison` (`scripts/queries.py:3864`): (1) `_has_table('fund_holdings')` silently short-circuited the endpoint to empty after Stage 5 dropped the legacy table — fixed to `_has_table('fund_holdings_v2')` which is the table the SQL actually reads; (2) `long_row['long_value_k']` raised KeyError because the SELECT aliases the column as `long_value`. Renamed the output dict key to `long_value` for parity with `short_value`. Verified: `?ticker=EQT` returns 200 with 53 long_short_managers + 2 short_only_funds; `?ticker=AAPL` returns 21 + 2. Pre-commit (ruff + pylint + bandit) clean. |
| 2026-04-13 | **BL-10 — 4 broken Excel exports (q6/q10/q11/q15) fixed** | Commit `9ea3557`. Root cause: the extractor in `api_export` (`scripts/api_register.py`) only handled dict responses with `positions` or `rows` keys. q6/q10/q11 are multi-list dicts; q15 is `list[1]` wrapping nested lists + a nested dict. The whole structure was being passed into openpyxl which choked on dict-as-row iteration and nested list cell values. **Fix:** new `build_excel_multisheet(sheets)` helper in `scripts/export.py` taking `{sheet_name: list[dict] | dict}` → one formatted sheet per entry. Sheet name sanitization (31-char cap, Excel-illegal char stripping, collision suffixes). Cell writer defensively str()s any list/dict the flattener misses. `api_export` detects multi-table shapes at runtime: (a) dict without `positions`/`rows` → one sheet per key; (b) `list[1]` with list/dict-valued fields → Overview sheet for scalars + one sheet per nested list/dict. Single-sheet path (q1, q2, q7, q16) unchanged. Verified: q6→5 sheets (activist_13d, passive_5pct, history, activist_13f, short_interest), q10→2 (new_entries, exits), q11→2 (same), q15→3 (Overview, quarters, coverage); all xlsx magic bytes `504b0304`; openpyxl round-trip load succeeds. Pre-commit clean. |
| 2026-04-13 | **ARCH-4C-followup discovered — React type migration blocked on schemas.py** | Original queue: "migrate React tabs from hand-written `src/types/api.ts` to auto-generated `src/types/api-generated.ts`, tab by tab". Attempt during this session uncovered that `api-generated.ts` has 7 named schemas vs ~55 in `api.ts`, and 5 of 7 generated schemas (`ConvictionPayload`, `OwnershipTrendPayload`, `FlowAnalysisPayload`, `EntityGraphPayload`, `RegisterPayload`) are `{[key: string]: unknown}` opaque or only type top-level keys while row shapes stay opaque. `RegisterRow` generated has 1 field typed (`institution`) vs 17 in `api.ts`. Mechanical migration would regress type safety. **Deferred as ARCH-4C-followup (two-step):** (1) expand `scripts/schemas.py` Pydantic models to carry full field-level shapes for all ~55 responses currently in `api.ts` (4-6 hours of schema authoring); (2) regenerate `api-generated.ts`, migrate React tabs, delete `api.ts`. Step 2 must not start until step 1 is complete. Zero code changes this session — ROADMAP + NEXT_SESSION_CONTEXT notes only. |
| 2026-04-13 | **Stage 5 cleanup — 3 legacy pre-v2 tables dropped; 4 INF9d eids preserved** | Authorized 2026-04-09, executed 2026-04-13. Dropped `holdings` (12,270,984 rows), `fund_holdings` (6,385,709 rows), `beneficial_ownership` (51,905 rows prod / 0 staging) from both `data/13f.duckdb` and `data/13f_staging.duckdb`, followed by `CHECKPOINT`. Full `EXPORT DATABASE` backup to `data/backups/13f_backup_20260413_184518` (2.7GB) before any mutation. Grep (`FROM|JOIN|INSERT|UPDATE|DELETE|TRUNCATE` word-boundary, excluding `_v2`) found zero code references across all `*.py`. Zero view refs (`entity_current` uses `holdings_v2` only). Gates post-drop: `validate_entities.py --prod` 9 PASS / 0 FAIL / 7 MANUAL unchanged; `pytest tests/smoke/ -v` 8/8 green; `entity_current` row count 20205; `entity_overrides_persistent` 47 rows; managers 12005; holdings_v2 12,270,984; fund_holdings_v2 6,385,709 all intact. **4 INF9d eids (20194 Pacific Life Insurance Company, 20196 Stowers Institute, 20201 Stonegate Global Financial, 20203 International Assets Advisory) NOT deleted — closed as won't-fix.** Pre-delete check found each eid is a live PARENT_SEEDS brand shell: 1 brand alias, 2 rollup_history rows (self-root EC+DM), recent manual_l4 classification edits from 2026-04-10, and 1 outgoing `wholly_owned` ADV_SCHEDULE_MANUAL relationship to a real child entity (→1685 Pacific Life Fund Advisors / →8544 American Century / →9990 Catalyst Capital / →2196 International Assets Investment Mgmt). The ROADMAP-table claim "no aliases, no identifiers, no holdings — safe to delete" was correct only on `no identifiers` + `no v2 holdings`; aliases and ADV lineage relationships are load-bearing. Deleting would remove ADV_SCHEDULE_MANUAL parent lineage and fresh manual_l4 classification fixes. INF9d row updated with correction note. |
| 2026-04-13 | Phase 4+ Batch 4-C — Flask → FastAPI cutover | All 8 domain Blueprints converted to FastAPI APIRouters (config, register, fund, flows, entities, market, cross, admin). `scripts/app.py` swapped to FastAPI entry with uvicorn `__main__`, `StaticFiles` mount for React bundle, `Jinja2Templates` for `/admin`, and a `lifespan` handler wrapping `init_db_path()`. `admin_bp.py` token auth moved from `@before_request` to a per-router `Depends(require_admin_token)` reading `Header(alias='X-Admin-Token')`; `hmac.compare_digest` semantics preserved. All routes kept as `def` (not `async def`) so the thread-local `get_db()` cache continues to work under FastAPI's threadpool. Every `jsonify()` replaced with `JSONResponse` or bare dict return. Excel export in `/api/v1/export/query{qnum}` uses `StreamingResponse` with explicit `Content-Disposition` headers instead of Flask's `send_file(download_name=...)`. `respond()` retired; `envelope_success()` and `envelope_error()` now construct JSONResponse envelopes with starlette `Request` access to `query_params` for meta. Smoke test harness swapped to `fastapi.testclient.TestClient`: `.get_json()` → `.json()`, `.data` → `.content`; zero snapshot changes needed because the wire format is byte-identical. `requirements.txt` + `.github/workflows/smoke.yml`: `flask==3.1.1` removed; `fastapi==0.115.4`, `uvicorn==0.39.0`, `httpx==0.28.1`, `jinja2==3.1.6` added. **Verification:** 8/8 smoke tests green (pytest via TestClient), 11/11 Playwright visual tests green against live uvicorn at :8001, pre-commit (ruff + pylint + bandit) clean, `npm run build` clean. `/api/v1/summary?ticker=AAPL` returns APPLE INC; `/api/v1/tickers` envelope.data length = 6,511 (matches Flask baseline). `/docs` + `/redoc` OpenAPI surface now available. Fast-forward merged from `phase4-fastapi` branch. **Out of scope:** React api.ts regeneration via openapi-typescript — follow-up (wire format unchanged so hand-written types still valid). BackgroundTasks for admin shell-outs — current sync semantics preserved. Commits `386c066` (infra) + `a9bc423` (cutover). |
| 2026-04-13 | Phase 4 Batch 4-B — queries.py service layer split | `cache.py` (43 lines) + `serializers.py` (211 lines) extracted. `queries.py` 5,703 → 5,523 lines. Zero `jsonify()` calls. Cache keys as explicit string constants. 8/8 smoke green. Commit `125d86d`. |
| 2026-04-13 | **Phase 4 Batch 4-A — Blueprint split of `scripts/app.py`** | Split 1,788-line `scripts/app.py` into 9 modules on the `phase4-blueprint-split` feature branch, fast-forward merged to main after 8/8 smoke + 11/11 Playwright green. **Layout:** `app.py` (103 lines — Flask creation, Blueprint registration, `/`, `/admin`, `__main__`), `app_db.py` (173 — `get_db`, `has_table`, `_resolve_db_path`, switchback monitor, thread-local caching — distinct from pipeline `scripts/db.py`), `api_common.py` (138 — `respond()` envelope helper, `_get_rollup_type`, `TICKER_RE`/`QUARTER_RE`/`VALID_ROLLUP_TYPES`, `_RT_AWARE_QUERIES`, `QUERY_FUNCTIONS`, `LQ`/`FQ`/`PQ`, `validate_query_params` before_request), `api_config.py` (64 — /config/quarters, /freshness), `api_register.py` (317 — tickers, summary, query1 envelope, generic query<N>, export/query<N>, amendments, manager_profile, `_execute_query` helper, `QUERY_NAMES`), `api_fund.py` (239 — fund_rollup_context, fund_portfolio_managers, fund_behavioral_profile, nport_shorts — split from api_register to stay under 400-line target), `api_flows.py` (174 — flow_analysis, ownership_trend_summary, cohort_analysis, holder_momentum, peer_rotation, peer_rotation_detail, portfolio_context), `api_entities.py` (222 — entity_search, entity_children, entity_graph, entity_resolve, entity_market_summary, `_eg_quarter` helper), `api_market.py` (272 — sector_flows, sector_flow_movers, sector_flow_detail, short_analysis, short_long, short_volume, crowding, smart_money, heatmap), `api_cross.py` (165 — cross_ownership, cross_ownership_top, two_company_overlap, two_company_subject, peer_groups, peer_groups/<group_id>). Endpoint classification comment block extracted verbatim to `docs/endpoint_classification.md` + Blueprint-mapping table appended. `app_legacy.py` retains the full 1,788-line pre-split module for rollback (1-week retention). Every Blueprint mounts under `/api/v1/` via `url_prefix`. Module-level state (`_db_path_lock`, `_conn_local`, `_available_tables`, etc.) stays in `app_db.py` with `logging.getLogger` in place of `app.logger` since `app_db.py` has no Flask dependency. `api_common.respond()` uses `current_app.logger` for validation-error logging. `queries._setup(get_db, has_table)` + `init_admin_bp(get_db, has_table)` called from `app.py` after imports. All domain files ≤400 lines, `app.py` ≈100 lines — matches spec exit gate. **Verification:** 8/8 `pytest tests/smoke/` against the split backend; 11/11 `npm run test:visual` Playwright against restarted Flask + new React bundle, 0 pixel drift; pre-commit (ruff + pylint + bandit) clean on all 10 files; `npm run build` clean. Admin Blueprint at `/api/admin/*` unchanged. **Gate cleared:** Phase 4 Batch 4-B (queries.py service-layer split — SQL / serializers / cache) now unblocked. |
| 2026-04-13 | **Phase 1-B2 — error envelope + Pydantic schemas + React error boundaries** | Two-commit rollout (infra + rollout). **Infra:** new `scripts/schemas.py` with `Envelope[T]` generic + `ErrorShape` + `MetaShape` + `iso_now()` helper + loose per-endpoint payload schemas + 7 concrete parameterized envelope types (`TickersEnvelope`, `SummaryEnvelope`, etc.). `scripts/app.py` gains a `respond()` helper that wraps data/error in the envelope and validates via Pydantic on the way out; shape mismatches return a 500 with standard `schema_validation_error`. React: new `src/types/envelope.ts` (`Envelope<T>`, `ErrorShape`, `MetaShape`), new `src/hooks/useFetchEnvelope.ts` (drop-in replacement for useFetch; unwraps `.data`, surfaces envelope `.error`, exposes `.meta`), new `src/components/common/ErrorBoundary.tsx` (per-tab React error boundary with reset button). `pydantic==2.12.5` added to `requirements.txt` + `.github/workflows/smoke.yml`. **Rollout:** wraps 6 priority endpoints — `/api/v1/tickers` (payload: `list[TickerRow]`), `/api/v1/query1` (Register; dedicated route added before the generic `/query<N>` handler so queries 2–16 stay bare), `/api/v1/portfolio_context` (Conviction), `/api/v1/flow_analysis`, `/api/v1/ownership_trend_summary`, `/api/v1/entity_graph`. Error paths use structured codes (`missing_param`, `invalid_param`, `not_found`, `db_unavailable`, `internal_error`). React consumers updated: `TickerInput`, `CrossOwnershipTab`, `OverlapAnalysisTab` (all 3 `/api/v1/tickers` consumers) unwrap `.data`; `RegisterTab`/`ConvictionTab`/`FlowAnalysisTab`/`OwnershipTrendTab`/`EntityGraphTab` swap `useFetch` → `useFetchEnvelope`. `AppShell` wraps every tab in `<ErrorBoundary key={activeTab}>` — remount-on-tab-change clears stale error state. Smoke snapshots regenerated for 3 of 4 monitored endpoints (all except `/summary`, which isn't in priority 6). `_counts` helper in `test_smoke_endpoints.py` recurses into `data` when it sees the envelope shape, preserving row-count coverage. 8/8 smoke tests green, `npm run build` + `tsc` clean, pre-commit (ruff + pylint + bandit) pass. Queries 2–16 still use the bare `/api/v1/query<N>` handler — scoped per spec. `_execute_query(qnum)` helper factored out and shared by both routes. **Gate cleared:** ready for Phase 4-C (FastAPI) which replaces these hand-written schemas with openapi-typescript-generated types. |
| 2026-04-13 | **Vanilla-JS frontend retirement** | Authorized early (7 days before the 2026-04-20 window) after 1 day of Phase 4 cutover stability. Deletions: `web/react-src/` (React POC), `web/templates/index.html` (vanilla-JS shell), `web/static/app.js` (5,600-line vanilla-JS frontend). Flask changes: 38 public `@app.route('/api/X')` decorators rewritten to `/api/v1/X`; `_register_v1_aliases()` helper + its invocation deleted; legacy `/api/admin/quarter_config` route removed (duplicate of `/api/v1/config/quarters`); `_validate_query_params()` before_request updated to check only `/api/v1/*` (admin_bp handles its own path `/api/admin/*`); endpoint classification comment block updated to `/api/v1/*` throughout. React changes: ~30 `fetch()` calls across 13 files rewritten from `/api/*` to `/api/v1/*` (`src/store/useAppStore.ts`, `src/components/header/TickerInput.tsx`, 11 tab components; `FreshnessBadge.tsx` was already on `/api/v1/`). Smoke test: `tests/smoke/endpoints.py` updated to `/api/v1/*`; 4 response snapshots recaptured via `capture_snapshots.py --update`; 8/8 pytest suite green. Build guardrail: `npm run build` + `tsc` clean, pre-commit (ruff + pylint + bandit) pass. Admin routes unchanged at `/api/admin/*`. **Follow-up:** `web/static/{dist,vendor,style.css}` directories are orphaned (only index.html referenced them) — safe to remove in a separate PR. |
| 2026-04-13 | **Phase 0-B2 — smoke CI fixture + response snapshot tests** | `scripts/build_fixture.py` + `tests/fixtures/13f_fixture.duckdb` (11.3 MB, 4 reference tickers — AAPL/MSFT/EQT/NVDA × 2025Q4 + entity closure to 9,145 eids) + `tests/smoke/{endpoints,test_smoke_endpoints,capture_snapshots}.py` + `tests/fixtures/responses/*.json` + `.github/workflows/smoke.yml`. Minimal DB_PATH_OVERRIDE env var in `scripts/app.py` lets smoke tests point Flask at the fixture without touching any runtime path. **Test suite:** 8 tests, 2 per endpoint — (a) `test_smoke_endpoint_ok` asserts HTTP 200 + non-empty JSON body; (b) `test_smoke_response_equality` asserts keys match snapshot, list-valued rows within ±5 %, and an endpoint-specific sentinel (AAPL ticker in `/api/tickers`, Vanguard Group in `/api/query1` rows, AAPL echo in `/api/summary`, `nodes` non-empty in `/api/entity_graph`). **Endpoints monitored:** `/api/tickers`, `/api/query1?ticker=AAPL`, `/api/summary?ticker=AAPL`, `/api/entity_graph?entity_id=2` (BlackRock — stable big holder across all 4 tickers). **Fixture-build trims applied** (per `docs/ci_fixture_design.md` acceptance): `entity_rollup_history` current-only (`valid_to = DATE '9999-12-31'`) — 40.4k→31.2k; `summary_by_parent` filtered to entity closure — 8.4k→6.5k; `ncen_adviser_map` + `fund_universe` filtered to fund_cik IN fixture — 11.1k→9.3k and 6.7k→5.6k; `investor_flows` excluded entirely (no smoke endpoint reads it). `entity_current` view re-emitted in fixture after tables land (view, not table — must be recreated after schema materialization). **Size note:** <1 MB design target was aspirational pre-schema-inspection; actual binary is 11 MB. User confirmed acceptable — still well below GitHub's 50 MB LFS threshold, no performance impact for `cp`-based CI setup. **CI workflow:** `.github/workflows/smoke.yml` installs pinned runtime deps (duckdb 1.4.4, flask 3.1.1, pandas 2.3.3, etc.) + pytest, verifies fixture present, runs `pytest tests/smoke/ -v`. **Safety rails in build_fixture.py:** source opened with `ATTACH … (READ_ONLY)`, aborts if source==dest, aborts if dest exists without `--force`, interactive confirmation unless `--yes`, supports `--dry-run`. All 4 response snapshots captured via `tests/smoke/capture_snapshots.py --update`. 8/8 tests pass locally. **Gate cleared — Phase 4 Batch 4-A (Blueprint split) now unblocked.** |
| 2026-04-13 | **FreshnessBadge rollout — all 11 tabs** (Batch 3-A follow-on) | `web/react-app/src/components/tabs/*.tsx` — wired `FreshnessBadge` into the 10 remaining tabs per the mapping in `NEXT_SESSION_CONTEXT.md` gotcha z. Register / Conviction / CrossOwnership / OverlapAnalysis / EntityGraph → `summary_by_parent` (label "register"). OwnershipTrend / PeerRotation / SectorRotation → `investor_flows` (label "flows"). FundPortfolio → `fund_holdings_v2` (label "N-PORT"). ShortInterest → `beneficial_ownership_current` (label "SC-13"). Each tab's control bar `<div style={{ marginLeft: 'auto' }}>` wrapper upgraded to `display: flex, alignItems: center, gap: 10` and badge placed left of `ExportBar` — same pattern as the FlowAnalysisTab pilot. FundPortfolioTab has a flat control row (no marginLeft wrapper) — badge inlined before the ExportBar. Module-level fetch cache in `FreshnessBadge.tsx` means the 11 badges share one `/api/v1/freshness` call. `fund_holdings_v2` and `beneficial_ownership_current` have no pipeline write hook yet — those badges will render "No data" until hooks land; acceptable per component design. `npm run build` + `tsc --noEmit` clean. |
| 2026-04-13 | **Phase 0-B1 — CI fixture DB design decision** | New `docs/ci_fixture_design.md`, ~210 lines. Evaluates three fixture options (SQL seed script / committed binary snapshot / stripped EXPORT DATABASE dump) against the Phase 0-B2 smoke test requirements. **Recommendation: Option 2 — committed binary snapshot + rebuild script.** Rationale: entity-graph consistency (ADV / parent-bridge / classification / rollup chains) is the dominant complexity driver, which Options 2 & 3 get for free by filtering a validated prod DB while Option 1 requires hand-authoring an unblessed parallel validator. Option 2 edges Option 3 on CI-runtime simplicity (`cp` vs `IMPORT DATABASE`) and ergonomics (one file vs parquet dir). Explicit Phase 0-B2 acceptance criteria included. Commit `7f62b7d`. |
| 2026-04-13 | **data_freshness pipeline write hooks + FreshnessBadge** (Batch 3-A follow-on) | New `record_freshness(con, table_name, row_count=None)` helper in `scripts/db.py` — INSERT OR REPLACE on `data_freshness` with CURRENT_TIMESTAMP. Auto-count if row_count omitted. Safe no-op (warning only) if `data_freshness` table is missing (pre-3A DB). Wired into `scripts/compute_flows.py` main() — stamps `investor_flows` + `ticker_flow_stats` after CHECKPOINT. Wired into `scripts/build_summaries.py` main() — stamps `summary_by_ticker` + `summary_by_parent`. New React component `web/react-app/src/components/common/FreshnessBadge.tsx` — per-table pill that fetches `/api/v1/freshness` once (module-level cache shared across all badges), renders color-coded pill with status (fresh / amber / red / never) per the ARCH-3A SLA thresholds. Pilot integration: FlowAnalysisTab toolbar next to ExportBar, surfacing `investor_flows`. Other tabs will wire their relevant tables in a follow-up. Graceful degradation to neutral "No data" pill on 404 or empty response. Pre-commit + tsc + `npm run build` all clean. Running Flask needs restart to serve `/api/v1/freshness`. Commit `2892009`. |
| 2026-04-13 | **ARCH-3A — DB schema cleanup (Batch 3-A)** | `scripts/queries.py` + `scripts/app.py` + new `scripts/migrate_batch_3a.py` + `scripts/merge_staging.py` TABLE_KEYS additions. **(1) `fund_family_patterns` table** — DDL `(pattern VARCHAR, inst_parent_name VARCHAR, PK(inst_parent_name, pattern))` created in staging first, verified (83 rows × 50 groups), then applied to prod. Seeded from the in-code `get_nport_family_patterns()` dict. `match_nport_family()` now reads via `get_nport_family_patterns()` which queries the DB with module-level memoization. In-code `_FAMILY_PATTERNS_FALLBACK` preserved verbatim as defensive fallback for fresh-deploy / missing-DDL scenarios; removed in a later pass once deploy paths confirmed. **(2) `data_freshness` table** — DDL `(table_name VARCHAR PRIMARY KEY, last_computed_at TIMESTAMP, row_count BIGINT)`. Created empty in staging + prod. Pipeline write hooks + React footer badge are follow-on tasks (not in 3-A scope). **(3) `/api/freshness` endpoint** — new Flask route returning `{data: [{table_name, last_computed_at, row_count}]}`. Auto-dual-mounted at `/api/v1/freshness` by the Batch 1-A `_register_v1_aliases()` helper. Graceful 200 + empty array if `data_freshness` table is missing (pre-migration safety). Added to the endpoint classification comment block. **(4) merge_staging.py TABLE_KEYS** — added `fund_family_patterns: None` (full-replace — code is authoritative until an editor UI arrives) and `data_freshness: ["table_name"]` (upsert PK — preserves pipeline-written rows across future merges). **Workflow note:** entity staging workflow (`sync_staging` / `diff_staging` / `promote_staging`) is entity-graph only and does not cover new reference tables. Flagged to user before apply; used `merge_staging.py` semantics instead, with the initial migration running `migrate_batch_3a.py --prod` directly after staging verification (both tables new, no prod data to protect, rollback = `DROP TABLE`). **Equivalence** — 6 ticker/rollup combos (AAPL/EC, AAPL/DM, EQT/EC × {query1, portfolio_context}) bit-identical to pre-2A baseline after 2dp rounding. DB-backed + fallback dicts match exactly. `/api/freshness` returns `{"data": []}` at both `/api/` and `/api/v1/` paths. Pre-commit all pass. Commit `731f4a0`. |
| 2026-04-13 | **ARCH-2A — Correctness fixes (Batch 2-A)** | `scripts/queries.py` + new `docs/write_path_risk_map.md`. **(1) get_nport_children N+1 fix** — new `get_nport_children_batch()` replaces the per-parent loop in `query1` (Register) and `portfolio_context` (Conviction). SQL: parent-patterns CTE via parameterized VALUES, ILIKE join against `fund_holdings_v2`, per-parent `QUALIFY row_number() OVER (PARTITION BY pp.parent_name ORDER BY value DESC) <= 5`. Sub-adviser exclusion (via `ncen_adviser_map`) refactored into a second CTE + `NOT EXISTS` anti-join. Single ticker-level `float_shares` lookup replaces per-parent (was 25× redundant). **Latency — 25-parent AAPL portfolio, warm cache:** batched 17.8ms median vs legacy 297.4ms median — **14× speedup, well under the ≤50ms spec target**. End-to-end HTTP: `/api/query1?ticker=AAPL` 748→211ms (3.5×), `/api/portfolio_context?ticker=AAPL` 681→350ms (1.9×), `/api/query1?ticker=EQT` 402→137ms (2.9×). Remaining portfolio_context time is downstream pandas work (separate Phase 3+ precompute target). **Equivalence** — bit-identical responses on AAPL/EC, AAPL/DM, EQT/EC × {query1, portfolio_context} after 2dp rounding to neutralize DuckDB float-SUM non-determinism. Legacy `get_nport_children()` retained (referenced by unused `get_children()` fallback path — marked for removal if that path stays dead). `get_nport_children_q2` (Holder Changes FQ↔LQ delta helper, gotcha u) intentionally NOT batched. **(2) summary_by_parent path check** — 3 consumers in `queries.py` (lines 775 / 1442 / 4304) all pure SELECT. No INSERT/UPDATE/DELETE on request path. Writes only in `build_summaries.py`. No code change required; documented. **(3) Write-path consistency audit** — new 170-line `docs/write_path_risk_map.md` grouping 22 non-entity pipeline scripts into 6 risk tiers (T1 atomic CREATE OR REPLACE, T2 drop+recreate without txn, T3 per-chunk streaming by design, T4 per-group atomic not cross-group, T5 idempotent UPDATE, T6 mixed). Key finding: **no audited script uses explicit BEGIN/COMMIT/ROLLBACK**. T2 — `build_cusip / build_managers / load_13f / unify_positions / compute_flows / fetch_adv / fetch_13dg` — is the natural target for BL-3 implementation work. T3 ingesters are intentionally per-chunk atomic per `docs/PROCESS_RULES.md`. Audit only; no code changes; no fixes in this batch. Pre-commit all pass. Commit `700bcdb`. |
| 2026-04-13 | **ARCH-1B1 — Endpoint classification + export parity (Batch 1-B1)** | `scripts/app.py` + a 1-line regex correction in `ARCHITECTURE_REVIEW.md`. **(1) Endpoint classification table** — 39-route comment block committed above the Flask routes section. Two axes: Quarter (`latest-only` vs `quarter-aware`) and Rollup (`rollup-agnostic` vs `rollup-aware`). Distribution: 33 latest-only / 6 quarter-aware × 24 rollup-agnostic / 15 rollup-aware. Freeze artifact consumed by Batch 4-A Blueprint split. **(2) api_export rollup parity** — before the fix, `api_export` read `quarter` but never read `rollup_type`, so Excel export for queries 1/2/3/5/12/14 always returned EC-rollup data regardless of on-screen toggle. Fix: hoisted `_rt_aware` (previously local to `api_query`) to module-level `_RT_AWARE_QUERIES = frozenset({1, 2, 3, 5, 12, 14})` and added the same `elif qnum in _RT_AWARE_QUERIES` branch to `api_export`. Verified end-to-end via Flask test client: `/api/export/query1` with `rollup_type=economic_control_v1` vs `decision_maker_v1` produces different Excel sheets (69896B vs 69895B, sheet1.xml hashes differ); `quarter=2025Q4` vs `2025Q1` also differ. **(3) Export data-shape fix (in-scope scoped extension)** — `api_export` returned 500 for q1/q6/q10/q11/q15/q16 because those query functions return structured dicts and the extractor only handled q7's `{positions}` key. Extended to also handle `{rows}` which unblocks q1 and q16 (Register / Fund Register). The rollup parity fix above is unobservable on q1 without this extraction, so it is in scope. q6/q10/q11/q15 remain 500 (multi-table shapes — `{activist_13d, passive_5pct, history}`, per-category buckets, multi-sheet) — tracked as **BL-10**. **(4) Regex correction in `ARCHITECTURE_REVIEW.md`** — Batch 1-A input-guards row updated from `^[A-Z]{1,6}[.A-Z]?$` to `^[A-Z]{1,6}(\.[A-Z])?$` matching the stated intent (accepts BRK.B). **Known limitations (self-heal at 2026-04-20 vanilla-JS retirement):** (a) `web/static/app.js` Excel export uses `window.location.href` so the `fetch()` monkeypatch that auto-appends `rollup_type` doesn't apply — vanilla-JS exports will still produce EC-rollup until retirement; (b) vanilla-JS never sends `quarter` at all. React bypasses `/api/export/*` entirely (client-side CSV from in-state data), so React exports are parity-correct today. **Not doing in 1-B1:** error envelope, Pydantic schemas, React error boundaries — those are Batch 1-B2, gated on vanilla-JS retirement. Pre-commit all pass. Commit `d3a2fcb`. |
| 2026-04-13 | **ARCH-1A — Routing hygiene (Batch 1-A)** | `scripts/app.py` + 1 React comment. **(1) Dual-mount:** new `_register_v1_aliases()` iterates `app.url_map` at import time and registers `/api/v1/<name>` for every `/api/<name>` except `/api/admin/*`. Net: 38 public routes dual-mounted, 16 admin routes excluded. Legacy `/api/*` stays live for vanilla-JS frontend until 2026-04-20 retirement window; drop the helper + switch to a `url_prefix` Blueprint after. **(2) quarter_config rename:** `/api/admin/quarter_config` is misnamed — public endpoint loaded by UI on every page. New canonical `/api/config/quarters`; both routes call a shared `_quarter_config_payload()`. Legacy retained for vanilla-JS. No live React fetch call to update (`QuarterConfigResponse` type is declared but unused — React tabs hardcode quarter arrays today); comment at `web/react-app/src/types/api.ts:748` updated to new path. **(3) Rollup param audit:** Register / Conviction / Ownership Trend / Fund Portfolio all thread `rollup_type` through to `queries.py`. `query7` (Fund Portfolio) is rollup-agnostic by design (fund-internal on `holdings_v2.cik`). Two gaps on Short Interest tab endpoints — `/api/short_analysis` called `short_interest_analysis(ticker)` without `rollup_type`, `/api/short_long` called `get_short_long_comparison(ticker)` without it. Both now pass `rollup_type=_get_rollup_type(request)`. Behavioral change is latent — Short Interest tab doesn't currently expose a rollup toggle. **(4) Input guards:** single `@app.before_request` hook on the main app validates ticker / quarter / rollup_type shape for `/api/*` paths (admin_bp path excluded, it has its own token auth). Empty/missing values pass through; handlers still enforce required-param presence. Invalid → HTTP 400 with JSON error. Ticker regex corrected to `^[A-Z]{1,6}(\.[A-Z])?$` — the spec literal `^[A-Z]{1,6}[.A-Z]?$` would reject `BRK.B` despite the inline comment saying it should accept it. Corrected form flagged inline in `app.py` and in the commit message. Transitional — replaced by Pydantic in Batch 4-C (FastAPI). **Smoke coverage:** Flask test client ran 10 ticker variants (incl. `BRK.B`, `BF.B`, 7-letter, SQL-injection `AAPL;DROP`), 5 quarter variants, 3 rollup_type variants; all behaved as expected. `/api/query1` v1 vs legacy parity confirmed functionally identical (4-byte float-sum non-determinism is pre-existing DuckDB aggregate behavior, reproducible on repeated calls to the same endpoint). **Surfaced pre-existing bug:** `/api/short_long` returns 500 with `KeyError 'long_value_k'` — reproduces with direct `get_short_long_comparison('AAPL')` call, unrelated to this batch. Filed as follow-up before Short Interest tab starts using the endpoint. **Not doing in 1-A:** response schemas, error envelope, `queries.py` changes beyond rollup gaps, vanilla-JS retirement, DB-universe ticker validation (BL-7). Pre-commit (ruff + pylint + bandit) all pass. Commit `a8dd77a`. |
| 2026-04-13 | **ARCH-0A — Lint + static analysis CI** | New `.github/workflows/lint.yml`: runs `pre-commit run --all-files --show-diff-on-failure` on push (main) + PR. Ubuntu + Python 3.11, pre-commit 4.3.0, hook envs cached by `.pre-commit-config.yaml` hash. First run on `e201885` → green in 1m0s. **Gate cleared: Phase 1 (Batch 1-A routing hygiene) now unblocked.** Baseline cleanup to get `--all-files` green: (a) ruff auto-fixed 5 F401 unused imports in `scripts/compute_flows.py` (`pandas`, `config.QUARTERS`, `config.FIRST_QUARTER`, `config.PREV_QUARTER`) and `scripts/fix_fund_classification.py` (`re`); (b) pylint `--disable` expanded to suppress 51 pre-existing warnings across 11 rules — W1514 unspecified-encoding ×14, W0603 global-statement ×8, W1510 subprocess-run-check ×7, W1309 f-string-without-interpolation ×7, E0401 import-error ×4 (curl_cffi), W0212 protected-access ×3, W0108 unnecessary-lambda ×2, I1101 c-extension-no-member ×2 (lxml), E0606 possibly-used-before-assignment ×2, W0640 cell-var-from-loop ×1, E0611 no-name-in-module ×1; (c) bandit `--skip` expanded from `B101,B608` to `B101,B108,B110,B310,B404,B603,B607,B608` to suppress 48 pre-existing findings (19 B110 try_except_pass, 11 B603 subprocess-without-shell, 9 B404 subprocess-import, 7 B607 start_process_with_partial_path, 1 B310 urlopen, 1 B108 hardcoded_tmp). Suppressions are baseline — the motivating B608-class bug would still fail CI because ruff/pylint/bandit run on every push and any NEW violation of the unsuppressed rules triggers a failure. Cleanup of suppressed rules tracked as **BL-8** in Architecture Backlog. Python 3.11 in CI vs 3.9 locally; ruff still targets `py39` for source-code compatibility. Annotation noise: Node.js 20 action deprecation (June 2026) — revisit before then, not urgent. Commit `e201885`. |
| 2026-04-12 | React Phase 4 cutover | Flask now serves web/react-app/dist/ instead of vanilla-JS frontend. static_folder → dist/assets, static_url_path='/assets', index route → send_file(dist/index.html). 11/11 Playwright tabs green at :8001, 0 pixel diff. npm run build required before starting Flask. Commit 002fab0. |
| 2026-04-12 | **ARCHITECTURE_REVIEW.md — full stack architecture review + phased upgrade plan** | 435-line architecture document at repo root. Scope: stack decisions, API contracts, modularization, deployment. No data ops or pipeline work. **Structure:** Current Stack diagram; What Is Solid — Do Not Change (7 items including DuckDB, entity MDM temporal graph, thread-local connection caching, query result caching, snapshot+switchback monitor, precomputed `investor_flows`/`ticker_flow_stats`, admin Blueprint with token auth); Architecture Gaps (G1–G11 covering untyped API contract as highest-consequence, missing error contract, input validation gaps, `quarter_config` namespace drift, no API versioning, `app.py` monolith, `queries.py` mixed concerns, `FAMILY_MAP` hardcoded dict, three conflated snapshot roles, no CI on push, no pipeline dependency enforcement); **Execution logic:** freeze contracts → fix correctness → precompute analytics → modularize → deploy. **6 phases, 9 batches, explicit exit gates:** Phase 1 Contract Stabilization (1-A routing hygiene ~1hr, 1-B response contract ~half day) → Phase 2 Correctness Fixes (2-A known gaps ~2hr) → Phase 3 Precompute + Data Layer (3-A DB schema cleanup ~2hr, 3-B portfolio_context artifact ~half day) → Phase 4 Backend Modularization (4-A runtime split ~1 day, 4-B service layer split ~half day, 4-C Flask→FastAPI ~half day) → Phase 5 Deployment Migration (gated on React Phase 4 cutover stable ≥1 week) → Phase 6 Repo reshape (only if 4 insufficient). Backlog section BL-1 through BL-6 for phase-independent work (GitHub Actions CI, Makefile/DAG, write-path consistency implementation, three snapshot roles doc, Zustand scope rule, loading state standardization). **Recommended next Claude Code task: Batch 1-A — routing hygiene, `scripts/app.py` only, low risk, ~1 hour.** Tracked items added to new ROADMAP section "ARCHITECTURE BACKLOG" (before DECISION MAKER). Commit pending. |
| 2026-04-12 | Backend cleanup — shadow diff removal, quarter param threading, portfolio_context vectorization | Three related backend fixes in one session. **(1) Dropped `log_shadow_diff()`** (commit `c2c5441`): Phase 4 shadow logging no longer needed. Removed function definition, `_SHADOW_LOG_PATH` constant, and 4 call sites in `query1`, `query2`, `ownership_trend_summary`, and `flow_analysis`. `-51/+1` lines in `scripts/queries.py`. **(2) Threaded `quarter` param through query endpoints** (commit `94b0402`): `api_query` and `api_export` now read `quarter` from request args (default `LATEST_QUARTER`) and pass it through every dispatched `fn()` call. Added `quarter=LQ` kwarg to 25 query functions — `query1-16`, `holder_momentum`, `portfolio_context`, `cohort_analysis`, `flow_analysis`, `short_interest_analysis`, `get_short_long_comparison`, `_get_summary_impl`, `_cross_ownership_query`, `get_market_summary`, `get_peer_rotation` — with all `'{LQ}'` SQL f-strings and bareword `LQ` references rewritten to use the local param (`quarter_to = LQ` → `quarter_to = quarter`, `q_date_map.get(LQ, ...)` → `q_date_map.get(quarter, ...)`, `get_nport_children(..., LQ, ...)` → `get_nport_children(..., quarter, ...)`, `x.get(LQ, 0)` → `x.get(quarter, 0)`). `query11` redirect passes quarter through to `query10`. `get_nport_children_q2` intentionally left alone (semantically a FQ↔LQ delta helper, fixed at both ends). All defaults are `quarter=LQ` so existing callers unchanged. Smoke test on `/api/query1?ticker=EQT&quarter=2025Q1` vs `&quarter=2025Q4` returned 69 vs 89 rows — divergence confirms param flows end-to-end. **(3) Vectorized `portfolio_context._compute_metrics`** (commit `251072b`, 2.7s → 730ms HTTP warm / 632ms in-process): Moved GICS sector mapping from Python row-level `_gics_sector()` calls into SQL as `CASE WHEN` columns on all three portfolio queries (parent top-25, fund top-25, N-PORT child batch) — REIT-under-Financial-Services branch first so it wins over plain Financial Services. With `gics_sector`+`gics_code` pre-computed in the fetchdf, `_compute_metrics` drops its two `iterrows()` loops and the `apply(lambda r: _gics_sector(...), axis=1)` call: sector totals now come from a single `groupby(['gics_sector','gics_code'])['value'].sum()`, and both rank lookups collapse to boolean mask + `idxmax()`. Return dict shape and callers unchanged. 600ms soft target missed by ~130ms HTTP — remaining hotspot is the 25-call `get_nport_children` N+1 loop (286ms, 45% of total budget), explicitly out of scope for this pass. Next optimization target if latency needed further is batching that loop. |
| 2026-04-12 | **React Phase 3 — Visual polish** | Five sub-items from `REACT_MIGRATION.md`: (1) **RegisterTab badge consolidation** — removed local `typeBadgeStyle()`, switched to shared `getTypeStyle()` from `common/typeConfig.ts`. User-visible behavior changes aligned Register with the other 8 tabs that already use the shared helper: `hedge_fund` badge now purple `#7B2D8B` (was navy `#002147`), labels switched from raw type names to friendly forms (`wealth_management` → `wealth mgmt`, `hedge_fund` → `quant/hedge`). Fallback `unknown` label preserved. (2) **Column alignment audit** — surveyed all 11 tabs. Register/Conviction/CrossOwnership share canonical 60/440/120 (Rank/Institution/Type); OverlapAnalysis (36/flex/80), FlowAnalysis (30/flex/62), PeerRotation (30/64/flex) deviate intentionally because they must fit additional period columns in the same viewport width. No changes — deviations validated. (3) **Cross-tab navigation — Fund Portfolio → Register** — ticker cells in the positions table are now clickable (except the subject row which is already the current ticker). Click calls `loadCompany(ticker.toUpperCase())` + `setActiveTab('register')`. Cursor pointer + hover underline via new `.fp-ticker-link` class + `title="Open Register for XYZ"`. (4) **Print CSS gaps** — extended `@media print` rules on ShortInterest, PeerRotation, SectorRotation from controls-only (`.si-controls { display:none }` etc.) to also expand the scroll wrappers: new classes `.si-wrap`, `.pr-wrap`, `.sr-wrap` with `height:auto!important; max-height:none!important; overflow:visible!important` + universal descendant `max-height:none!important` to defeat nested `maxHeight: 240/280/500` scroll containers on print. EntityGraph left as-is (ReactFlow canvas doesn't print usefully — controls already hidden). (5) **Playwright visual regression scaffold** — `@playwright/test@^1.59` installed, `playwright.config.ts` (Chromium, 1440×900, `reuseExistingServer`, 2% pixel-diff tolerance, animations disabled), `tests/visual/tabs.spec.ts` covers all 11 tabs with fixed `TICKER=AAPL`, each test loads ticker → waits for `/api/summary` 200 → clicks sidebar button by role+exact name → waits `networkidle` + 400ms settle → full-page screenshot. New scripts: `npm run test:visual`, `npm run test:visual:update`. `.gitignore` covers `test-results/` + `playwright-report/`. Baselines not captured in this session — requires one-time `npx playwright install chromium` + Flask on :8001 + `npm run test:visual:update`. `npm run build` passes. Commit `c836813`. |
| 2026-04-12 | **React Phase 2 COMPLETE — all 11 tabs ported** | All 11 sidebar tabs fully ported to `web/react-app/`: Register, Ownership Trend (3 sub-views), Conviction, Fund Portfolio, Flow Analysis, Cross-Ownership, Overlap Analysis, Peer Rotation, Sector Rotation, Entity Graph (Market + Company modes), Short Interest. Shared infrastructure: 9 common components, `useFetch` hook, `typeConfig.ts`, Zustand store with `rollupType`, Recharts + React Flow libraries, `api.ts` with 60+ interfaces covering all endpoints. Global formatting rules: 2-decimal percentages with trailing zeros, 0→em-dash, consistent highlight yellow `#fef9c3`. Backend fixes shipped alongside: query7 f-string bug, flow_analysis pct_float semantics, portfolio_context top3 objects, entity_market_summary endpoint. Flask app at :8001 untouched. Next: Phase 3 (visual polish, Playwright tests) → Phase 4 (remaining tabs) → Phase 5 (cut over). |
| 2026-04-12 | INF8 — Trian Fund Management entity merge (eid=1090 + eid=9075 → eid=107) | Merged three Trian entities into eid=107. eid=107 ("Trian Fund Management", PARENT_SEEDS brand ghost, activist/True, self-rooted, $3.98B Q4 as rollup target) is canonical survivor — already has correct rollup_entity_id on all Trian holdings_v2 rows. eid=1090 ("TRIAN FUND MANAGEMENT, L.P.", CIK `0001345471`, CRD `000154172`, 13F-HR filer) and eid=9075 ("Trian Fund Management GP, LLC", CIK `0001345472`, 13F-NT GP entity) both merged in. **Transfers to eid=107:** CIK `0001345471` + CRD `000154172` + CIK `0001345472` (3 identifiers moved), secondary aliases "TRIAN FUND MANAGEMENT, L.P." + "Trian Fund Management GP, LLC" (2 legal_name aliases added). eid=107 now has 4 aliases (2 PARENT_SEEDS originals + 2 new) and 3 identifiers. **Closed:** relationships 48 + 452 (fund_sponsor/fuzzy_match/parent_bridge — wrong type for institution-to-institution); classification history on 1090 + 9075 (duplicate activist/fuzzy_match copies of 107's PARENT_SEEDS/exact entry); aliases on 1090 + 9075; identifiers on 1090 + 9075. Both merged entities get `merged_into` rollup rows pointing at 107 (structural gate). No DM semantic issue — 107 is self-rooted for both EC and DM, correct for a standalone activist firm. Root cause: CRD format mismatch (same as INF4/INF6, tracked as INF4b) + PARENT_SEEDS/N-CEN feeder split. **Also flagged:** two `managers` rows with `parent_name='Trian Fund Management'` are fuzzy_match pollutions — CIK `0001541353` Triangle Securities Wealth Management ($1.29B) and CIK `0001779763` Iron Triangle Partners LP ($0.87B). Neither is a Trian entity. Added to INF17 Phase 1 scrub scope. Snapshot `20260412_064353`, 22 changes, validation 9/0/7. Commit pending. |
| 2026-04-12 | INF6 — Tortoise Capital Advisors entity merge (eid=20187 → eid=2273) | Merged eid=20187 ("Tortoise Capital Advisors, LLC", N-CEN padded CRD `000123711`, unknown classification) INTO eid=2273 ("TORTOISE CAPITAL ADVISORS, L.L.C.", 13F filer CIK `0001280965`, CRD `123711`, $7.86B Q4). Same root cause as INF4: CRD format mismatch (tracked as INF4b). eid=20187 was already routing to 2273 via `circle_fix`; this merge completes the structural cleanup. 6 fund_sponsor relationships (Tortoise Essential Energy, Energy Infrastructure Total Return, North American Pipeline, Global Water, Tortoise Energy, S000093687) re-pointed from 20187 to 2273. Circular wholly_owned pair (relationships 15200 + 15201 from orphan_scan) closed. eid=20187 rollups changed from circle_fix to merged_into → 2273. Classification/alias/CRD identifier on 20187 closed. Secondary alias "Tortoise Capital Advisors, LLC" (legal_name) added on 2273. No DM semantic issue — 2273 is self-rooted for both EC and DM (standalone advisory, no parent chain to split). Fund series rollup rows already had rollup_entity_id=2273 via transitive_flatten — zero behavior change. Snapshot `20260412_063446`, 17 row-level changes, validation 9/0/7. Commit `eaab03b`. |
| 2026-04-12 | L4-2 — Fix 3 classification mismatches vs N-PORT series split | All three entities reclassified to `mixed` with `confidence='high'`, `source='manual_l4'`. **(1) eid=4 Invesco ($652.5B 13F, $352.5B N-PORT, 54% coverage):** `passive` → `mixed`. N-PORT split is 96.7% active across 89 series / 3.3% passive across 5 series, but coverage is only 54% — the uncovered $300B likely includes Invesco's large passive ETF franchise (QQQ, S&P 500 ETF, PowerShares legacy). Pure `active` not defensible at 54% coverage. **(2) eid=9 Nuveen/TIAA ($423.6B 13F, $292.2B N-PORT, 69% coverage):** `passive` → `mixed`. N-PORT split is 90.1% active (26 series $263B) / 9.9% passive (2 series $29B), but coverage is only 69% — uncovered $131B could include TIAA-CREF index accounts. Borderline ≥90% on covered portion but insufficient coverage confidence for `active`. **(3) eid=9191 Security Investors LLC ($0 13F, $6.9B N-PORT, 100% coverage):** `active` → `mixed`. 81% passive (29 series $5.6B) / 19% active (55 series $1.3B) across full N-PORT coverage. Below 90% passive threshold for `passive`; the 19% active minority is non-trivial. **Also checked:** Geode Capital Management — all 4 Geode entities already correctly classified `passive` (Geode is Fidelity's internal index sub-adviser, no alpha-seeking mandate, 0% N-PORT coverage structurally expected since series file under Fidelity). Excluded from fix. Promotion snapshot `20260412_061258`, diff_staging.py 3 classification changes only, validate_entities.py 9/0/7 both staging and prod. Commit `a3c20e8`. |
| 2026-04-12 | React Phase 2 — Ownership Trend tab complete | Three sub-views in one tab: Quarterly Summary (`/api/ownership_trend_summary`), Holder Changes (`/api/holder_momentum`), Cohort Analysis (`/api/cohort_analysis`). All accept `rollup_type` + `level` + `active_only` and re-fetch on change. New types in `api.ts`: `HolderMomentumRow` (flat array with dynamic quarter keys, child rows lack rank), `CohortDetailRow` + `CohortDetailChild` (nested level-2 children), `CohortSummary` + `CohortRetentionTrend`, `CohortAnalysisResponse`. Quarterly Summary: 9-col table with signal badges (↑/↓/→) + 3 metric tiles. Holder Changes: dynamic quarter columns extracted via regex, collapsible hierarchy, type badges, signed change/chg% formatting. Cohort Analysis: category rows with color-coded left borders and bg tints by cohort type, expandable top-5 investor children, "From Quarter" selector (excludes latest), summary metrics bar below table. Shared controls: sub-view pill tabs, RollupToggle, ActiveOnlyToggle, FundViewToggle (level=parent/fund), ExportBar (CSV per sub-view). 540 lines in `OwnershipTrendTab.tsx`. Commit `ed94016`. |
| 2026-04-11 | INF4 — Loomis Sayles entity merge (eid=17973 → eid=7650) | Merged eid=17973 ("Loomis, Sayles & Company, L.P.", N-CEN padded CRD `000105377`, `unknown` classification, 25 DM rollups + 38 sub_adviser relationships) INTO eid=7650 ("LOOMIS SAYLES & CO L P", 13F filer CIK `0000312348`, CRD `105377`, `active` classification, $82.95B Q4 2025 book). Root cause of fragmentation: CRD string-format mismatch — `managers` stored unpadded `105377`, `ncen_adviser_map` stored padded `000105377`, entity resolver compared as strings without normalization. Tracked as **INF4b** for systematic CRD normalization fix. **Merge operations (136 total):** (a) 38 sub_adviser relationships closed on eid=17973, 38 equivalent relationships inserted on eid=7650; (b) 25 decision_maker_v1 rollup rows re-pointed from 17973→7650; (c) eid=7650's own decision_maker_v1 rollup changed from 8386 (Natixis) to self-root (Option A — Loomis is the decision maker, not Natixis; economic_control_v1 stays at Natixis, correct); (d) 2 self-root rollups on 17973 closed, replaced with 2 `merged_into` rows pointing at 7650 (preserves structural gate + shows merge provenance); (e) classification/alias/identifier on 17973 closed; (f) secondary alias "LOOMIS, SAYLES & COMPANY, L.P." (legal_name) added on 7650. validate_entities.py --staging: 9/0/7 (initial FAIL on structural_no_rollup caught and fixed via merged_into rows). diff_staging.py: 32 line-level changes + 38 relationship re-parenting. promote_staging.py: snapshot `20260411_221854`, 99 row-level changes, auto-validation green. Commit `ff49dbc`. **Loomis Sayles Trust Company LLC (CIK 0001435066)** is a separate related entity (13F-NT filer, no CRD) — flagged for separate review, not part of this merge. |
| 2026-04-11 | React Phase 2 — Register tab complete | Full port of Register tab to `web/react-app/`. 20 commits from `c38a866` (doc fix) through `ab199e5` (spellcheck disable). Covers: Phase 1 shell scaffold (Vite + React 19 + Tailwind v3 + Zustand, port 5174, 11 tab placeholders), Phase 2 infra (Recharts, endpoint type audit `api.ts` with interfaces for all 11 tabs, `RollupType` in Zustand store, 9 shared components in `src/components/common/`), and Register tab implementation (plain HTML table, `table-layout: fixed` 12-column layout with 2 gap spacers, `useFetch` hook, collapsible parent/child hierarchy with N-PORT fund-series drill-down, Fund view mode flattening children sorted by value, `RollupToggle` wired to `rollup_type` query param so Fund Sponsor / Inv. Decision toggle re-fetches, `QuarterSelector` + `ActiveOnlyToggle` + `InvestorTypeFilter` + `FundViewToggle` + `InvestorSearchWithDropdown` + `ExportBar` (CSV download + print), `ColumnGroupHeader` with dark fill + sandstone underline, sticky `TableFooter` with skip props for gap-shifted layouts, `Port. Coverage` column header tooltip, fixed-width badges, `data-institution` attribute for scroll-to-highlight). Existing Flask app at `:8001` untouched. Known gap: `query7` f-string bug blocks Fund Portfolio drilldown tab (logged in INFRASTRUCTURE as BUG item). |
| 2026-04-11 | INF17 Phase 3 — Fix upstream CRD fuzzy-match bug in `build_managers.py` | Root cause of the 127-row pollution: `rapidfuzz.fuzz.token_sort_ratio` with `score_cutoff=85` matches firm names that share generic corporate-suffix tokens (LLC, Capital, Management) without requiring any brand-word overlap. Added two new gates to `link_cik_to_crd()`: **(a)** brand-token Jaccard > 0 (at least one non-stopword token must match) via new `_brand_tokens()` + `_brand_tokens_overlap()` module helpers using a 21-word `_BRAND_STOPWORDS` frozenset; **(b)** city+state secondary verification via new `_city_state_compatible()` helper (short-circuits to accept when filer side lacks city/state — future-proofing, no-op today since `filings_deduped` has no location columns). Rejected candidates logged to `logs/build_managers_rejected_crds.csv` with `(manager_cik, manager_name, candidate_crd, candidate_firm, score, reason)`. Dropped dead `batch_size = 500`. Added pre-existing `# nosec B110` annotation on the unrelated `ALTER TABLE` try/except/pass block to keep pre-commit clean. Pre-commit ruff + pylint + bandit all pass. Pipeline NOT re-run — existing `cik_crd_links` still has the 114 pre-fix bad matches; Phase 1 blanket scrub of `managers.crd_number` + `aum_total` still required. The fix only prevents FUTURE pollutions on the next `--reset`. Commit `0634682`. **INF17b opened as follow-up:** same bug exists in `fetch_ncen.py:328` for `adviser_cik` — new ROADMAP row added in the same commit, not yet fixed. |
| 2026-04-11 | INF17 Phase 2 — Self-root 5 misattributed entities from CRD pollution audit | 5 entities self-rooted + 5 bad CRDs closed via INF1 staging workflow. eid=2195 7G Capital Management ($161M Q4, was rolling to ARS Investment Partners via `wholly_owned/ADV_SCHEDULE_A`), eid=6285 Capital Insight Partners ($473M Q4, was rolling to Indus Capital Partners), eid=7606 Compton Financial Group ($442M Q4, was rolling to Calton & Associates), eid=10514 IFS Advisors ($192M Q4, was rolling to Parker Financial), eid=6267 Scion Capital Group ($0 Q4, was rolling to Capital Group / American Funds via `fund_sponsor/N-CEN` — Michael Burry's firm wrongly co-branded with the $3T asset manager). **~$1.27B of misattributed Q4 AUM corrected.** Transaction: 10 rollup closes + 10 self-root inserts + 5 CRD closes = 25 row ops. diff_staging.py: 15 line-level changes. validate_entities.py --staging: 9 PASS / 0 FAIL / 7 MANUAL. promote_staging.py --approved: snapshot `20260411_214440`, auto-validation green. Final prod validate: 9/0/7 — no regression. Commit `6743f11`. Root cause (`managers.crd_number` fuzzy-match pollution in `build_managers.py:276`) still requires Phase 3 upstream fix — `build_entities.py --reset` would re-introduce the pollution today. Phases 1 (blanket `managers` CRD scrub for all ~114 CIKs), 3 (upstream matcher fix), and 4 (preserve coincidentally-correct rollups via `parent_bridge_sync`) remain. |
| 2026-04-11 | INF7 — Soros/VSS + Peter Soros/Orion fuzzy-match cleanup | Two independent fuzzy-match pollutions in the `managers` reference table had cascaded into the entity layer. **(1)** CIK `0001029160` (real Soros Fund Management) was paired with CRD `156600` (VSS FUND MANAGEMENT LLC, unrelated $977M NY firm) — `managers.aum_total` on the polluted row = VSS's exact ADV RAUM. The VSS CRD then walked through ADV Schedule A on Apr 7 and wired eid=2218 briefly under eid=1630 (Verus Capital Partners) before Apr 10 self-root broke the chain, leaving the CRD behind on `entity_identifiers`. **(2)** CIK `0001748240` (Peter Soros's SOROS CAPITAL MANAGEMENT LLC, Coronado CA) was paired with CRD `119428` (ORION CAPITAL MANAGEMENT LLC, also Coronado CA) with Orion's exact RAUM on the row. Same city + "Capital Management LLC" name similarity drove the mispair. **(3)** eid=92 was a PARENT_SEEDS brand-parent ghost with zero identifiers, acting as a fuzzy_match rollup target for three Soros-named CIKs with no verified corporate relationship. Option A fix via INF1 staging workflow, 9 edits in one transaction: closed CRD 156600 on eid=2218; closed relationships 418 (92→8443) + 507 (92→10228); self-rooted eid=8443 + eid=10228 in both rollup_types; closed both eid=92 aliases (row preserved); `UPDATE managers SET crd_number=NULL` on both Soros CIKs; `UPDATE managers SET parent_name=NULL WHERE cik='0001748240'`. `h-bis` (the Orion CRD scrub) added during precondition review after discovering the second pollution — symmetric to (g). diff_staging.py: 9 line-level changes (2 relationships, 4 rollups, 2 aliases, 1 identifier). validate_entities.py --staging: 9 PASS / 0 FAIL / 7 MANUAL exact baseline match. promote_staging.py --approved: snapshot `20260411_180047`, 13 row-level changes, auto-validation green. merge_staging.py --tables managers: prod.managers rebuilt via DROP+CREATE, 12,005 rows preserved, two target CIKs verified clean. Final prod validate_entities.py: 9/0/7 — no regression. Real Soros Fund Management ($8.6B Q4 book, 839 positions) now cleanly attributed to eid=2218 with CIK `0001029160` as sole identifier. Follow-ups deferred: (i) `managers.aum_total` still holds VSS's $977M and Orion's $257M on the two CIKs — wrong numbers but no longer feeding downstream lookups since CRD links are broken; (ii) staging `managers` has an extra `adviser_cik` column not in prod, silently dropped by merge — pre-existing schema drift. |
| 2026-04-11 | 2 Companies Overlap — React port (scaffold → production-ready) | Full migration of the `two-co-overlap` tab from vanilla-JS IIFE (~497 lines) to React 18 + TypeScript + Vite + Tailwind (preflight disabled) + AG Grid Community v35 with legacy theming. Scaffold at `web/react-src/` with 9 source files: `components/TwoCompanyOverlap.tsx` (main), `OverlapTable.tsx` (AG Grid with grouped headers + pinned totals), `SummaryTable.tsx` (cohort cards), `SecondTickerSearch.tsx` (autocomplete), `QuarterSelector.tsx`, `hooks/useOverlapData.ts` (abort-safe fetch), `hooks/useTickers.ts` (module-cached list), `types/overlap.ts`, `main.tsx` (mount + race-fix pending-ticker queue). Vite build output to `web/static/dist/tco-bundle.{js,css}` (gitignored); `web/templates/index.html` gains a `<link>` + module `<script>` tag; `switchTab('two-co-overlap')` routes through `window.tcoActivate(ticker)` which main.tsx exposes. Bugs hit and fixed along the way: (1) Tailwind v4 default dropped `init -p` — pinned to v3; (2) AG Grid v32+ dropped community module auto-registration — `ModuleRegistry.registerModules([AllCommunityModule])` at bundle boundary; (3) AG Grid v33+ new Theming API conflicts with legacy CSS imports — `theme="legacy"` prop on AgGridReact; (4) Tailwind Preflight `input { color: inherit }` leaked globally and made main `#ticker-input` text invisible inside `.header` — `corePlugins.preflight: false`; (5) vanilla switchTab's `_tcoDeactivate` clobbered React's DOM via `innerHTML=''` — replaced with panel `.hidden` class toggle + a `mountTco` defensive root-recreation guard; (6) race between module-script-defer and initial tab click — `window.__tcoPendingTicker` stash read by auto-mount; (7) AG Grid v35 cellRenderer returning `HTMLElement` crashes the React wrapper ("Objects are not valid as a React child") — switched bold pinned cells to JSX `<span style={{fontWeight:600}}>`; (8) rank column via `valueGetter` with rowIndex fallback chain; (9) `.ag-header-group-cell` border-bottom stacked with `.ag-header` default creating double-underline — removed my rule; (10) AG Grid's internal horizontal scroll viewport showed empty tracks — `domLayout="autoHeight"` + `suppressHorizontalScroll={true}`; (11) fixed-basis flex panels (`flex: '0 0 689px'`) keep grids full-width on narrow viewports, tab-container scrolls at page level; (12) cohort highlight alpha stacking at spacer boundary fixed with row-only selector `.ag-theme-alpine .ag-row.tco-overlap-row` + `!important` at 0.28 alpha; (13) bidirectional cohort math with both Direction A (`% sec by top N of subj`) exact and Direction B (`% subj by top N of sec`) approximated from the top-50-subject universe; (14) summary reworked from single table to two stacked bordered cards per cohort with "Top 25"/"Top 50" headers and metric rows embedding the cohort number directly. Final width spec: `36/240/82/82/14 spacer/115/115/5` = 689px per table. Active-only toggles per panel (inst: `manager_type !== 'passive'`, fund: `is_active === true`). Bundle 1,294.98 kB / gzip 369.77 kB. 16 commits: `e200623`, `5a7fcde`, `d48a8fd`, `68b40ff`, `cee679f`, `71da99b`, `c2cbec1`, `c7feb65`, `f0bcc14`, `59410e7`, `5303b0f`, `a5c3b99`, `64e4692`, `39c9bb9`, `013cf3b`, `ce2a356`. |
| 2026-04-11 | INF12 — admin Blueprint with token auth | New `scripts/admin_bp.py` Flask Blueprint mounted at `/api/admin/` with a `before_request` token guard (503 when `ENABLE_ADMIN` or `ADMIN_TOKEN` unset, 403 on missing/wrong `X-Admin-Token` header, `hmac.compare_digest` for timing-safe compare). 15 routes moved onto admin_bp: `add_ticker`, `stats`, `progress`, `errors`, `run_script`, `manager_changes`, `ticker_changes`, `parent_mapping_health`, `stale_data`, `merger_signals`, `new_companies`, `data_quality`, `staging_preview`, `running`, `entity_override`. `POST /api/add_ticker` renamed to `POST /api/admin/add_ticker` (only caller was `admin.html`, updated in same commit). `run_pipeline.sh` and `merge_staging.py` dropped from the `run_script` whitelist — never web-triggerable. Exception: `/api/admin/quarter_config` stays on the main app because the public main UI fetches it on every page load to populate the quarter dropdown. `admin.html` gained an `adminFetch()` helper that prompts for the token on first load, stores it in `localStorage` under `admin_token`, and injects `X-Admin-Token` on all 11 admin calls (clears on 403, surfaces 503 as "Admin disabled on server"). Bind unchanged (`0.0.0.0` for Render) — auth is enforced at the Blueprint layer, so the original "bind 127.0.0.1" step was dropped as unnecessary. Smoke tests: port 8001 (no env) → 503; port 8002 (`ENABLE_ADMIN=1 ADMIN_TOKEN=test123`) → 403 without header / 200 with correct header / 200 on ungated `quarter_config`. `pre-commit pylint + bandit` on `app.py` and `admin_bp.py` both clean. Commit `d51db60`. |
| 2026-04-11 | INF9 Route A — persist 24 reclassify overrides to staging | Wrote 24 rows to `data/13f_staging.duckdb` `entity_overrides_persistent` covering the Section 3 L4 audit reclassifications: 23 `market_maker` (Susquehanna ×3, Jane Street ×7, Optiver ×3, CTC, Citadel Securities, DRW, Flow Traders, HRT, IMC, SIG ×2, Two Sigma Securities, Virtu) + 1 SC US (TTGP) → venture_capital. All 24 CIKs pre-validated to resolve against current `entity_identifiers`. Validation: 9 PASS / 0 FAIL / 7 MANUAL — no regression. `diff_staging.py`: 0 line-level changes on all 6 diffed table categories (overrides table not yet in diff coverage — tracked as INF9e). Scope narrowed from the original "39 fixes" framing after reading `replay_persistent_overrides()` and finding the action taxonomy supports only `reclassify classification` / `alias_add` / `merge → economic_control_v1`. Remaining gaps split into INF9a (is_activist), INF9b (rollup_type on merge), INF9c (delete_relationship for parent_bridge fuzzy), INF9d (CIK-less entities), INF9e (diff + promote + prod DDL for overrides table). Commit `b53e3fa`. Staging holds the rows safely; they reach prod only after INF9e lands. |
| 2026-04-11 | Entity Graph tab (vis.js) | Institution → filer → fund hierarchy visualization with sub-adviser edges. 4 new endpoints (`/api/entity_search`, `/api/entity_children`, `/api/entity_graph`, `/api/entity_resolve`), `build_entity_graph()` + `search_entity_parents()` + `get_entity_filer_children()` + `get_entity_fund_children()` + `get_entity_sub_advisers()` + `compute_aum_for_subtree()` helpers in queries.py, vis.js 9.1.9 vendored to `web/static/vendor/`. LR hierarchical layout, Oxford Blue / Glacier Blue / Green / Sandstone node colors. Filer model walks `entity_relationships` (excluding sub_adviser edges) for CIK-bearing descendants at shallowest depth; fallback returns institution itself when no CIK children exist (Vanguard pattern). Funds attach at institution root, not per-filer — discovered empirically since BlackRock's 26 filer subsidiaries have zero direct fund children. Expand trigger node for truncated fund lists, quarter selector, type-ahead institution search. AUM holdings-based (SUM market_value_usd from holdings_v2), not managers.aum_total. Commits a028153, 9080885. |
| 2026-04-11 | 2 Companies Overlap tab | Pairwise institutional + fund-level holder comparison for two tickers. `/api/two_company_overlap` + `/api/two_company_subject` endpoints, `get_two_company_overlap()` + `get_two_company_subject()` in queries.py. Two side-by-side tables with grouped `% Owned` / `Value ($M)` headers, top 15 body rows, Top 15 + Top 25 tfoot totals, Top 25 / Top 50 cohort summary per panel. Subject auto-fills from main header ticker input on tab activation, Cross-Ownership-style autocomplete for second ticker (reuses pre-loaded `tickerList` + `filterTickers()`), quarter buttons populated from `/api/admin/quarter_config`, per-panel "Active only" toggles (institutional: `manager_type !== 'passive'`, fund: `fund_universe.is_actively_managed`). Overlap rows highlighted Glacier Blue 8% opacity. Client-side re-render via `_tcoLastData` cache — toggle changes don't re-fetch. Table-layout:fixed with explicit per-column widths (36/240/62×4/16 spacer/3px trailing). Group header underline in sandstone (#C9B99A) to contrast with Oxford Blue thead background. Safari-specific `background-clip: padding-box` added to thead ths to suppress WebKit phantom 1px bleed. IIFE integration wired through `switchTab()` branch calling `window.loadTwoCoOverlap`, panel toggled via `.hidden` class (not inline style) to survive Entity Graph IIFE's parallel click listener collision. EQT/RRC sanity check: 76% institutional overlap (38 of 50), 24% fund overlap (12 of 50), EQT top-25 total = 67.8% of float, RRC cross-held = 43.9%. Commits 5942449, 978dcec, f4b5cae, ed892f1, fb6bcfc, 10ccea7, eb74487, 7514e67, 887e469, 2df5a2c, ba8bfd0, 95d80f7, de09d87, 33a39f9. |
| 2026-04-11 | B608 nosec placement fix — 26 endpoints | Preexisting breakage since ee5e7cb: `# nosec B608` placed on `con.execute(f"""` lines, injecting the comment text into the SQL string. DuckDB errors on `#` as non-comment character. 26 endpoints returned HTTP 500 including `/api/tickers` (autocomplete root — broke the entire header search), `/api/admin/manager_changes` (tracked as INF15), `/api/heatmap`, `/api/fund_portfolio_managers`, `/api/amendments`, `/api/short_long`, `/api/manager_detail`, `/api/nport_shorts`, and 18 more. Rewrote each block to split `con.execute(` onto its own line, use implicit `f""` empty-string concat to anchor bandit's nosec recognition on a physical line that DuckDB never sees, and maintain the original f-string interpolation. Fixes autocomplete root (INF14 smoke-test gap discovered too late), heatmap, fund portfolio, short interest, manager detail, and all 20+ other silently-broken endpoints. Closes INF15 as duplicate of this root cause. Commit 794c81e. Added SMOKE TEST comment above `api_tickers` in app.py (a841ddb) to pin `/api/tickers` to any future post-commit curl check. |
| 2026-04-09 | Item 2 — Filing agent name resolution | `resolve_bo_agents.py`: dual-source (EFTS primary + SEC .hdr.sgml fallback) with auto-failover after 10 consecutive failures. Incremental UPDATE + CHECKPOINT every 500 rows. Restart-safe (WHERE clause skips resolved rows). 14,870 agent rows → 100% resolved. |
| 2026-04-09 | Item 9 — Final pre-Phase 4 validation | ALL GATES PASS. beneficial_ownership: 51,905 rows clean. holdings: 14 categories, 0 NULL, $67.3T. parent_bridge: 0 dupes. fund_classification: 5,717 series. Entity tables: 20K entities, 13.7K relationships. READY FOR PHASE 4. |
| 2026-04-09 | Item 8 — Investor type classification (full review) | Manager-level: NULL 787→0 via 6 sources (parent_seeds, adv_strategy, ticker_count, keyword, manual_review, default_active). Fund-level: 6,325 series classified (passive 1,056 $12.6T, active 4,845 $10.2T, mixed 424 $3.1T). S&P500 overlap + 8-index cross-validation. 210 funds reclassified by multi-index correlation. New: fund_classification, index_proxies, fund_index_scores tables. |
| 2026-04-09 | Item 7 — app.py lint debt | flake8 116→0, bandit B608 28→0, 17 bare exceptions → as e + logging. setup.cfg + .bandit config. |
| 2026-04-09 | Item 6 — R1/R2/R3 13D/G data quality audit | R1: 13G null 4-5% (good), 13D 96-98% (structural). 34 outliers >100% nullified, avg corrected 3,880%→8.61%. R2: 12.3% match 13F parents (expected — individuals/small funds). R3: 8,227 duplicates removed (60,135→51,908). 0 remaining. |
| 2026-04-09 | Item 5 — Fidelity intl sub-adviser dedup verification | Confirmed N13/N14/N15 all working: series-level dedup (GROUP BY series_id + MAX) in 4 queries, Geode exclusion active, 174 shared series correctly handled. ~116% ratio is structural (monthly MAX vs quarter-end). No code changes needed. |
| 2026-04-09 | Item 4 — Top 50 self-rollup verification | Capital Group: unwired 44 false matches, consolidated $1.9T (Capital World $735B + Capital International $638B). Franklin Templeton +ClearBridge +advisers = $533B. Ameriprise +Columbia Threadneedle = $443B. MFS consolidated $310B. BMO $289B. PGIM +Jennison = $256B. parent_bridge deduplicated 12,005→11,135. |
| 2026-04-09 | Item 3 — International parent entities | 77 rows wired in parent_bridge across 10 groups: Amundi ($368B, 14), MUFG AM ($190B, 4), Sumitomo Trust ($183B, 4), Allianz AM ($99B, 10), Natixis IM ($28B, 5), Macquarie ($23B, 3), Nikko AM ($17B, 4), BNP Paribas AM (7), Daiwa (8), AXA IM (6). Banks/insurance/brokers left independent per operating AM rollup policy. |
| 2026-04-09 | Process rules for large-data scripts | `docs/PROCESS_RULES.md`: 9 rules — incremental save, restart-safe design, multi-source failover, rate limiting per endpoint, error thresholds (>5% = STOP, 1-5% = WARN), progress reporting, dry-run default. Applies to all future batch/fetch scripts. |
| 2026-04-02 | Pipeline 1 — 13D/G beneficial ownership | `fetch_13dg.py`: 74 filings tested (AR,AM,DVN,WBD,CVX), `beneficial_ownership` + `beneficial_ownership_current` tables, activist tab upgraded to 4-section view |
| 2026-04-02 | Pipeline 2 — N-CEN identity join | `fetch_ncen.py`: 9,363 adviser-series mappings, 978 advisers, Wellington found subadvising 123 series across Hartford/JH/etc |
| 2026-04-02 | Pipeline 3 — Unified position table | `unify_positions.py`: 18.7M rows (12.3M 13F + 6.4M N-PORT), added to `update.py` |
| 2026-04-02 | Pipeline 4 — Monthly N-PORT | `fetch_nport.py` updated: `report_month` column, `MONTHLY_TARGETS` for all 3 months/quarter, backfilled 4.2M rows |
| 2026-04-02 | Pipeline 5 — Share class + LEI | `build_fund_classes.py`: 31,056 classes, 13,143 LEIs, `fund_classes` + `lei_reference` tables, 99% LEI coverage on fund_holdings |
| 2026-04-02 | Pipeline 6 — Peer groups | `peer_groups` table seeded (5 groups), `/api/peer_groups` endpoint, cross-ownership dropdown |
| 2026-04-02 | Pipeline 7 — LEI standardization | LEI column on positions table, `/api/fund_behavioral_profile` endpoint |
| 2026-04-02 | Pipeline 8 — FINRA short volume | `fetch_finra_short.py`: 102K rows, 9 dates, 12K tickers. `short_interest` table with daily short sale volume |
| 2026-04-02 | N-PORT short positions | `/api/nport_shorts` endpoint: 21,904 equity short positions from 92 funds |
| 2026-04-02 | `/api/short_volume` endpoint | Daily FINRA short sale volume per ticker |
| 2026-04-02 | `fetch_13dg.py` rewrite — edgar library | Removed all custom HTTP/sessions/rate limiting. `edgar.Company(ticker).get_filings()` handles EDGAR access. Two-phase architecture: list (1494 tickers/min) then parse |
| 2026-04-02 | 4-worker parallel fetch | ThreadPoolExecutor, workers do I/O, main thread writes DB |
| 2026-04-02 | Ticker checkpoint table | `fetched_tickers_13dg` — Phase 1 fully resumable |
| 2026-04-02 | `listed_filings_13dg` crash-resilient resume | Phase 1 results persisted to DB; survives process kills between Phase 1 and Phase 2 |
| 2026-04-02 | `--update` flag | `run_update()`: scans EDGAR quarterly indexes since MAX(filing_date), date-based incremental |
| 2026-04-02 | `--workers N` CLI flag | Default 4, configurable |
| 2026-04-02 | Thread-local `requests.Session` | Fixed FD exhaustion crash at ~3,800 tickers (later replaced by edgar library) |
| 2026-04-02 | Exponential backoff on 429s | 10→20→40→80→120s cap, `_retry_edgar()` wrapper |
| 2026-04-02 | Catastrophic regex fix | Purpose regex O(n²) → linear via slice; group members bounded to cover page |
| 2026-04-02 | 2MB filing truncation | Skip proxy statements bundled with 13D that caused 100% CPU hang |
| 2026-04-02 | `compute_flows.py` confirmed | investor_flows table populated; needs re-run after full 13D/G fetch |
| 2026-04-02 | Pre-commit linting | `.pre-commit-config.yaml` with flake8, pylint, bandit hooks |
| 2026-04-02 | Code review bug fixes | `update.py`: dynamic quarter via MAX(quarter), exit code enforcement. `app.py`: Query 2 join on (cik, manager_name) |
| 2026-04-02 | queries.py service layer | 34 query functions extracted from app.py (1,789 lines). app.py reduced to 618 lines |
| 2026-04-02 | export.py | `build_excel()` + Excel style constants extracted (83 lines) |
| 2026-04-02 | run_pipeline.sh hardened | Process guard, phase timestamps, macOS desktop notification |
| 2026-04-02 | Global rate limiter + Phase 2 stall fix | 2 req/s cap, Retry-After parsing, 2 workers for Phase 2, reduced timeouts |
| 2026-04-02 | Phase CLI flags | `--phase1-only`, `--phase2-only`, `--phase3-only` for independent execution |
| 2026-04-02 | Crash handlers | All 7 fetch scripts log to `logs/<script>_crash.log` on unhandled exception |
| 2026-04-02 | Test isolation | `--test` uses `13f_test.duckdb` via `db.py`, write guard prevents production writes |
| 2026-04-02 | Bug 6 + Bug 7 | fetch_market.py upsert (never drops), app.py snapshot switchback monitor |
| 2026-04-02 | Pyflakes cleanup | 31 unused imports removed across 17 scripts |
| 2026-04-02 | SHOW TABLES cached | 6 per-request queries replaced with `has_table()` at startup |
| 2026-04-02 | Ruff + pre-commit config | Replaced flake8 with ruff, documented semgrep status |
| 2026-04-03 | Item 5 — pct_owned regex tuning | 4 new patterns for 13D cover page format. 13G null rate: 4.5%. 13D structural gap addressed |
| 2026-04-03 | Item 9 — Short vs long comparison | `get_short_long_comparison()` + `/api/short_long` endpoint. Matches 13F longs to N-PORT shorts by adviser name |
| 2026-04-03 | Item 10 — Short squeeze signal | `get_short_squeeze_candidates()` + `/api/short_squeeze` endpoint. Squeeze score = short_pct × inst/float ratio |
| 2026-04-03 | Item 23 — Incremental load_13f.py | `--quarter` flag for single-quarter reload, `--staging` support, crash handler |
| 2026-04-03 | Item 27 — app.py quarter centralization | All 6 hardcoded quarter strings replaced with config.py variables (LQ/PQ) |
| 2026-04-03 | N11 — Filer name resolution | `resolve_names.py` 3-pass pipeline: holdings (219 CIKs/12K rows), EDGAR API (353 CIKs/25K rows), 98 filing agents marked. `name_resolved` column added. query6 fallback JOIN. Data quality endpoint updated |
| 2026-04-03 | N-PORT index/ETF merge | 1.6M new rows (4.2M→5.8M), 6,306 series, 98.4% adviser-mapped. Fixed CUSIP→ticker for 2.1M rows |
| 2026-04-03 | market_value_usd 1000x fix | SEC bulk VALUE is in dollars, not thousands. Removed `*1000` from load_13f.py, divided all existing values by 1000. Removed compensating `/1000` in 6 locations |
| 2026-04-03 | FLOW_PERIODS fix | 1Q/2Q were identical (both Q3→Q4). Fixed to 4 distinct periods: 4Q, 3Q, 2Q, 1Q |
| 2026-04-03 | manager_type expansion | 50→110 PARENT_SEEDS + keyword-based fallback classification. NULL rate: 71%→40% |
| 2026-04-03 | pct_of_float fallback | shares_outstanding used where float_shares NULL. NULL rate: 30%→24% |
| 2026-04-03 | fund_classes.quarter fix | Derived from report_date for all 31,056 rows |
| 2026-04-03 | FINRA short interest refresh | 102K→328K rows, 29 dates, 12,511 tickers |
| 2026-04-03 | enrich_tickers.py | +1,076 CUSIPs resolved, Q4 ticker coverage 91.6% |
| 2026-04-03 | Staging support | Added --staging to enrich_tickers.py, auto_resolve.py. fetch_market.py fixed for staging isolation |
| 2026-04-05 | fetch_market.py rewrite (H8-H11) | Bypassed yfinance entirely via new `yahoo_client.py` (direct curl_cffi to Yahoo's JSON API). Added `sec_shares_client.py` for authoritative shares outstanding from SEC XBRL (EntityCommonStockSharesOutstanding + EntityPublicFloat, from 10-K/10-Q covers, 90-day cache). New incremental update protocol: prices 7d / metadata 30d / SEC 90d staleness thresholds; `--force`, `--missing-only`, `--metadata-only`, `--sec-only`, `--limit` flags. Ticker pre-filter flags bonds/warrants/preferreds/FX-suffix as unfetchable. pct_of_float now uses SEC shares_outstanding as primary source. Schema additions: unfetchable, unfetchable_reason, metadata_date, sec_date, public_float_usd, shares_as_of, shares_form, shares_filed, cik. |
| 2026-04-06 | Sector Rotation tab redesign (H15) | Multi-quarter institutional money flow analysis by GICS sector. Financial-statement layout showing active dollar flows (shares × price, stripping price effects) across all quarter transitions. Expandable detail: top 5 net buyers/sellers per sector per quarter. By Parent/By Fund + Active Only toggles. Pre-aggregated holdings prevent cross-product inflation. Short Squeeze tab removed (broken SQL + stale data). |
| 2026-04-06 | Period-accurate pct_of_float (H14) | New `shares_outstanding_history` table (317K facts, 4,450 tickers). DuckDB ASOF JOIN matches each holding to period-correct SEC shares. GOOGL 2020 pct_of_float corrected from 0.4% to 7.3% (20:1 split). `build_shares_history.py` script with `--update-holdings` flag. |
| 2026-04-06 | Peer Rotation tab | Per-ticker substitution analysis within sector. Shows subject vs sector flow summary with grouped bar chart, substitution waterfall chart (top 5 peer swaps), industry + broader sector peer tables with expandable entity detail, top 5 sector movers with horizontal bar chart, and top 10 entity rotation stories. Three Chart.js charts. By Parent/By Fund + Active Only toggles. N-PORT fund-level support. New `get_peer_rotation()` + `get_peer_rotation_detail()` in queries.py, `/api/peer_rotation` + `/api/peer_rotation_detail` endpoints. |
| 2026-04-07 | Phase 3.5 parse hardening | Memory leak fix (4.5GB→300MB), parallel parse (4 workers), SIGALRM timeout (180s), crash resilience (checkpoint file, SIGTERM handler, temp CSV recovery), progress logging. 6 bug fixes: atomic SCD, deterministic matching, duplicate staging guard, PDF validation, explicit checkpoint, alias cache reuse. |
| 2026-04-07 | Phase 3.5 full parse run | 3,026 PDFs parsed in 9.8h (4 workers, pdfplumber). 21,695 rows, 833 relationships. |
| 2026-04-07 | Phase 3.5 parser fixes | Scan-based ownership_code extraction, expanded match universe (all aliases not just rollup parents), evidence resolution policy (ADV supersedes legacy at score ≥90). Match: 12→895 owners, 8→833 relationships. |
| 2026-04-08 | PyMuPDF parser | 100-400x faster. 88.3% recall, 99.5% accuracy vs pdfplumber. Parsed all 112 oversized PDFs in 391s (pdfplumber: 0 in 8h). 15,902 new rows. Full coverage: 3,585 CRDs (98.2%). |
| 2026-04-08 | Phase 3.5 match on full data | 1,059 ADV relationships in DB (350 wholly_owned primary, 157 mutual_structure, 56 parent_brand). 825 JV structures. SCD integrity: 0 broken. |
| 2026-04-08 | --refresh mode + QC + manual adds | Dual-parser pipeline (pymupdf primary → pdfplumber fallback, 4 workers, ~25 min). QC report (1,926 CRDs with 0 entity owners). Manual adds via CSV. Interactive review HTML (1,926 items, 20,416 searchable aliases). |
| 2026-04-08 | External review import | 17 high-confidence + 20 medium corporate parents wired. 12 new parent entities created (Pacific Life, Stowers Institute, TA Associates, Virtus, Himalaya Capital, F-Prime, etc.). Total: 36 manual relationships, $3.8T AUM covered. |
| 2026-04-08 | Orphan subsidiary scan | Full dataset scan: 11,644 self-rollup entities vs 866 parents. 174 orphans wired (Dimensional $836B, Lord Abbett $270B, Thornburg $45B, 150+ others). Firm identity verification prevents false merges. |
| 2026-04-08 | Entity review complete | 1,926 unresolved CRDs triaged: 99 wired, 1,827 confirmed independent. Baillie Gifford Overseas ($163B) → Baillie Gifford & Co. |
| 2026-04-08 | D1 accuracy audit | 90 PDFs, 6 strata. Zero false positives. 75% parser agreement on comparable files. Pymupdf recall gap = Schedule B indirect owners. |
| 2026-04-08 | DBA/legal name check | 236 firms flagged where firm_name ≠ legal_name. Integrated into --qc pipeline. Catches holding company structures missed by ADV parser. |
| 2026-04-08 | Name normalization | Corp/Corporation, Inc/Incorporated, Co/Company standardized before fuzzy matching. BNY Mellon 83→100, Franklin Resources 83→97. |
| 2026-04-08 | Phase 3.5 complete | 12,862 relationships, 20,205 entities. All deferred items (D1, D7) resolved. Phase 4 ready to scope. |
| 2026-04-08 | N-PORT orphan cleanup | 858 of 920 orphan fund series wired (99.3%). BlackRock/iShares 278, SPDR 80, Fidelity 67, Allspring 27, and 30+ others. $337B → $2.4B remaining. |
| 2026-04-08 | Rollup chain flatten + circle fix | 23 circular pairs broken (Vanguard↔Vanguard Group Inc, etc.). All multi-level chains flattened to one-hop. Zero remaining chains. |
| 2026-04-08 | Classification sync | 974 unknowns classified from ADV strategy_inferred. 204 corrections (active→hedge_fund/PE). 5 PE misclassifications fixed. Unknown: 7,607→6,633. |
| 2026-04-08 | Operating AM rollup policy | Only asset managers as rollup targets. PE, insurance, foundation, VC unwired: Stowers (117 children), Pacific Life (63), TA Associates (40), DAG Ventures (3). |
| 2026-04-08 | Final data quality audit | 0 chains, 0 circles, 0 SCD breaks, 0 multi-primary, 0 orphan rels, 0 duplicate IDs, 0 pending staging. 13,541 active relationships. |

---

## FUTURE — Performance & Stability Hardening

| # | Item | Priority | Notes |
|---|------|----------|-------|
| H1 | N+1 query fix — batch children in query1 | Done | 80+ queries → 2 queries. Parents + all 13F children fetched in single batched CTE |
| H2 | Batch yfinance calls in fetch_market.py | Done | Two-pass: batch `yf.download()` for prices (500/batch), then `yf.Tickers()` for metadata |
| H3 | Exception handling in fetch_nport.py executemany | Done | Batch fails → falls back to row-by-row insert with error counting |
| H4 | Incremental N-PORT parsing | Done | `is_already_loaded()` + local XML cache via `download_xml()` already skip loaded filings |
| H5 | Phase 2 accession checkpoint | Done | Already handled via NOT IN query on beneficial_ownership (0.03s) |
| H6 | Connection pooling for Flask | Done | Thread-local connection cache in get_db() — reuses connections across requests |
| H7 | Query result caching | Done | _cached() with 5-min TTL on get_summary. Extensible to other queries |
| H8 | fetch_market.py rewrite — bypass yfinance | Done | yfinance 1.2.0 ignores `session=` param; internal requests hit YFRateLimitError at scale. New `scripts/yahoo_client.py` hits `/v7/quote`, `/v10/quoteSummary`, `/v8/chart` directly via curl_cffi with chrome impersonation. No rate limiting. 1,079 symbols/sec on batch quote. |
| H9 | SEC XBRL shares outstanding | Done | `scripts/sec_shares_client.py` pulls `EntityCommonStockSharesOutstanding` + `EntityPublicFloat` from SEC companyfacts. Sourced from 10-K/10-Q cover pages — authoritative. 90-day cache on disk per CIK. `pct_of_float` now prefers SEC shares_outstanding over Yahoo float_shares. **`market_cap` is computed as `shares_outstanding (SEC) × price_live (Yahoo)`** — Yahoo's marketCap field is never written. NULL if either input is missing (no silent fallback). |
| H10 | Ticker pre-filter for Yahoo | Done | `classify_unfetchable()` flags bonds (" " in ticker), warrants (WT/-WT/WS suffix), preferreds (-P*), class markers (*), FX-suffixed foreign OTC. Persisted as `market_data.unfetchable` + reason. Skipped on future runs. |
| H11 | Incremental update protocol | Done | fetch_market.py defaults to incremental: skip prices <7d old, metadata <30d, SEC shares <90d. New flags: `--force`, `--missing-only`, `--metadata-only`, `--sec-only`, `--limit N`. Schema additions: `metadata_date`, `sec_date`, `public_float_usd`, `shares_as_of`, `shares_form`, `shares_filed`, `cik`, `unfetchable`, `unfetchable_reason`. |
| H12 | Migrate remaining yfinance callers to yahoo_client | Done | All 6 scripts migrated: `refetch_missing_sectors.py`, `approve_overrides.py` (also fixed latent `FROM d` bug), `enrich_tickers.py`, `build_cusip.py`, `auto_resolve.py` (3 call sites), `app.py` `/api/add_ticker`. Zero `yfinance` imports remain in `scripts/`. yfinance dependency can be dropped from requirements. Along the way: added `long_name`/`short_name` to YahooClient.fetch_metadata, added `us-gaap:CommonStockSharesOutstanding` + `us-gaap:WeightedAverageNumberOfSharesOutstandingBasic` fallbacks to SECSharesClient (for multi-class filers like GOOGL/META/F/CMCSA), 2-year staleness guard on SEC facts (rejects BRK's 2011 legacy record), new `market_data.shares_source_tag` column for provenance, and `data/reference/shares_overrides.csv` for filers with broken XBRL (Visa, BRK-A/B). |
| H13 | merge_staging.py column-swap bug | Done | Root cause: `merge_table()` built INSERT using staging's column order (positional), causing value corruption when staging and production had the same columns in different order. Hit during 2026-04-05 market_data merge — `cik` and `shares_source_tag` values were swapped in production after first merge. Fix: explicit named columns on both sides of `INSERT INTO t (cols) SELECT cols FROM staging_db.t`, with intersection of staging and production column sets (schema-drift tolerant). Regression test added. |
| H14 | Period-accurate pct_of_float (historical shares outstanding) | Done | Previously `holdings.pct_of_float` used `market_data.shares_outstanding` (latest) as denominator for ALL historical quarters — materially wrong across splits, buybacks, offerings. GOOGL 2020Q1 was 18× off; TSLA 2019Q4 20× off; AAPL 2020Q1 3.4× off. New `shares_outstanding_history` table populated from SEC XBRL (all facts, not just latest) via `scripts/build_shares_history.py`. New `SECSharesClient.fetch_history()` method iterates all share facts from local companyfacts cache with same tag-preference fallback as `fetch()`. Holdings update uses DuckDB ASOF JOIN on `strptime(report_date, '%d-%b-%Y')::DATE >= soh.as_of_date`. 317K historical facts across 4,450 tickers, 70% of (ticker, report_date) pairs match to SEC history. Fallback to latest market_data shares for tickers without SEC XBRL coverage. `market_data.shares_outstanding` retains "latest" semantic for current market_cap calculation. |
| H15 | Sector Rotation tab redesign | Done |
| H16 | Pre-computed entity×ticker flow cache | Not started | `sector_flows_cache` table: entity, ticker, sector, industry, q_from, q_to, active_flow, manager_type. Materialized once after each pipeline run (post merge_staging or fetch_market). Eliminates per-request CTE recomputation for both Sector Rotation and Peer Rotation tabs. Both 13F (cik/inst_parent) and N-PORT (series_id/fund_name) levels. Responses drop from seconds to milliseconds. Build as pipeline step in `compute_flows.py` or standalone script. | Replaced old query13 (buyer/seller counts) with multi-quarter institutional money flow analysis. Shows active dollar flows by GICS sector across all quarter transitions in financial-statement layout (one row per sector, columns per quarter, green/red). Strips price effects via `active_flow = (shares_cur - shares_prior) × implied_price`. Expandable detail per sector/quarter: top 5 net buyers + top 5 net sellers. By Parent / By Fund toggle + Active Only filter. Pre-aggregates holdings per (cik, ticker, quarter) to prevent cross-product inflation. Short Squeeze tab removed (broken + stale data). New endpoints: `/api/sector_flows`, `/api/sector_flow_movers`. Old query13 + QUERY_COLUMNS[9]/[13] removed. |

---

## FUTURE — Data Integrity & On-Demand Updates

| # | Item | Priority | Notes |
|---|------|----------|-------|
| D1 | On-demand single-ticker add — `/api/add_ticker` | Done | POST endpoint: fetches CUSIP (OpenFIGI), market data (yfinance), lists 13D/G filings. Admin UI form included. |
| D2 | Manager change detection | Done | Flag new CIKs, disappeared CIKs, name changes (fuzzy match old→new). Surface in admin UI for review. |
| D3 | Ticker change tracker | Done | Compare securities table across quarters. Cross-reference SEC company_tickers.json. Flag CUSIP→ticker changes (FB→META). |
| D4 | Parent mapping health check | Done | Re-match parent_bridge against ADV data. Log new parent assignments, broken links, orphaned CIKs. |
| D5 | Stale data detection | Done | Tickers with no market data >30 days, managers with no filings >4 quarters, delisted securities. |
| D6 | Merger signal detection | Done | When a CIK stops filing and another CIK's holdings jump, flag as potential merger. Link old→new CIK. |
| D7 | New company alerts | Done | Cross-reference SEC company_tickers.json weekly for new entries. Show which funds are accumulating. |

---

## FUTURE — Admin UI (all pipeline operations via web interface)

| # | Item | Priority | Notes |
|---|------|----------|-------|
| F1 | Admin dashboard — pipeline status, progress bars, log viewer | Done | `/admin` page with DB stats, live Phase 2 progress bar, error log, add-ticker form. Auto-refresh. |
| F2 | One-click monthly update | Done | Button triggers: seed staging → fetch updates → merge → refresh snapshot |
| F3 | Individual script triggers | Done | Dropdown: select script + flags (--update, --staging, --quarter). Start/stop/monitor |
| F4 | Schedule recurring updates | Done | Monthly 13D/G update, weekly FINRA short, daily market data refresh |
| F5 | Staging review before merge | Done | Show merge_staging --dry-run output. Approve/reject before merging to production |
| F6 | Data quality dashboard | Done | Show regex timeout count, missing tickers, pct_owned null rate per batch |
| F7 | Quarter config viewer | Done | Form to add new quarter URLs, snapshot dates. Auto-generates config.py diff |

---

## FUTURE — New Features & Enhancements

| # | Item | Priority | Notes |
|---|------|----------|-------|
| N1 | N-PORT index/ETF fund download | Done | Merged 1.6M new rows (4.2M→5.8M). 6,306 series, 98.4% mapped to advisers. Vanguard 30→96, BlackRock 132→243, Fidelity 281→449 |
| N2 | Short squeeze UI tab | Done | Frontend tab with `/api/short_squeeze`. Shows candidates >15% short, squeeze score, inst % float |
| N3 | Short vs long UI integration | Done | Long/short comparison in Smart Money tab via `/api/short_long`. Shows managers both long+short, short-only funds |
| N4 | Ownership concentration heatmap | Done | Peer Matrix tab replaced with interactive heatmap. `/api/heatmap` endpoint: top 15 managers × selected tickers, colored by pct_of_float. Hover shows value + exact % |
| N5 | Historical flow trend charts | Done | Multi-period flow trend table in Flow Analysis tab. Shows flow intensity (total/active/passive) and churn across all computed periods (4Q/3Q/2Q/1Q) |
| N6 | Manager profile page | Done | `/api/manager_profile` endpoint: top holdings, sector allocation, quarterly trend, summary stats. `loadManagerProfile()` in frontend with back-navigation |
| N7 | Export improvements — Print/PDF | Done | "Print / PDF" button in action bar. Print styles hide UI chrome, optimize table/chart layout for print. Uses browser native print-to-PDF |
| N8 | Webhook/alert system | Low | Configurable alerts: new 13D filing, >5% ownership change, short squeeze threshold crossed |
| N16 | Query consolidation | Done | Fixed missing /api/cross_ownership route. Merged query10+11→Position Changes (entries+exits in one view). Improved query14 with parent grouping. Made query13 sector-generic (accepts ?sector= param). Consolidated Ownership Trend 3→2 sub-views (Summary+Cohort combined, Holder Changes) |
| N9 | 13F-HR amendment reconciliation | Done | `/api/amendments` endpoint: shows managers who filed 13F-HR/A amendments for a ticker. Identifies amended vs original filing status |
| N10 | Multi-quarter position timeline | Partial | Flow trend table added to Flow Analysis. Register tab sparklines deferred — requires query1 modification to return per-quarter share arrays |
| N11 | Filer name resolution pipeline | Done | `resolve_names.py`: 3-pass resolution (holdings→EDGAR API→company_tickers.json). Added `name_resolved` column. 83.9%→90.0% resolved, 10% filing agents marked. `resolve_agent_names.py` extracts reporting person from filing text |
| N12 | Investor name standardization | Done | `normalize_names.py`: smart Title Case for 8.6M rows, 8 table/columns. Handles acronyms, canonical names, dotted abbrevs. ALL CAPS 27%→0% |
| N13 | N-PORT series-level deduplication | Done | All N-PORT rollup queries GROUP BY series_id (MAX per series). get_nport_position, get_nport_coverage, get_nport_children, get_nport_children_q2 all deduplicated. Fidelity NVDA: 1.39B → 1.07B shares after dedup |
| N14 | Geode/Fidelity sub-adviser exclusion | Done | `SUBADVISER_EXCLUSIONS` dict in config.py. Geode excluded from Fidelity rollup. Applied in get_nport_position, get_nport_children, get_nport_children_q2 via `_build_excl_clause()`. Extensible for future sub-advisers |
| N15 | International sub-adviser analysis | Done | Investigated: 183 shared series deduped by N13 (GROUP BY series_id). 83 intl-only series are real fund positions, excluding them loses data. Remaining ~10% excess (Fidelity 110%) is structural: N-PORT uses MAX of monthly snapshots vs 13F quarter-end. Only Fidelity and RBC (2 series) affected. No further exclusions needed |
| N17 | Economic hold period | Medium | Weighted-average hold period per investor based on economic exposure, not binary presence. An investor holding 5M shares for 4 quarters who trims to 100 shares in Q5 should show ~4Q hold period, not 5Q. **Method:** For each investor-ticker pair across all quarters, compute exposure weight = `position_value / peak_position_value`. Hold period = sum of quarterly weights (e.g., 1.0 + 1.0 + 1.0 + 1.0 + 0.002 = 4.0Q). Threshold: ignore quarters where exposure weight < 5% of peak (de minimis). Surface on Register tab as "Held" column and on Conviction tab. Requires: multi-quarter position history per investor (query all quarters in holdings). Consider: cost basis approximation via VWAP for $-weighted hold period |
| N18 | Portfolio positioning analysis — active investors | Medium | For each active investor holding the target company, analyze where the position sits within that investor's portfolio by market cap. **Method:** Pull full 13F portfolio for the investor, compute market cap distribution (25th/50th/75th percentile). Show where the target company's market cap falls relative to the investor's typical range. **Output:** Per-investor row showing: portfolio mkt cap P25/P50/P75, target company mkt cap, percentile rank within their portfolio, position size vs median position. Flag outliers where target is significantly outside the investor's normal mkt cap range (potential conviction signal). **UI:** Table on Conviction or Register tab showing active holders with their portfolio positioning. Visual indicator (dot on range bar) showing where company sits in each investor's mkt cap spectrum |

---

## FUTURE — Pipelines

| # | Item | Priority | Notes |
|---|------|----------|-------|
| P4 | iShares Trust N-PORT | Done | Fetched 3 iShares entities (Trust, Inc, U.S. ETF Trust). +391K ETF holdings. BlackRock NVDA coverage: 6.4% → 42.5%. Fixed ETF exclusion filter in fetch_nport.py |
| P5 | SPDR / State Street N-PORT | Done | Fetched 6 entities (SPDR Series Trust, Index Shares, Select Sector, SS Institutional, SSGA Funds, Navigator). +170K rows. State Street NVDA: 9% → 16.4%. Note: SPY is a UIT, exempt from N-PORT |

---

## PIPELINE 9 — 13D/G Cleanup & Integration

| # | Item | Priority | Notes |
|---|------|----------|-------|
| R1 | Data quality audit — 13D/G `pct_owned` coverage | Done | Item 6: 13G null 4-5% (good). 13D null 96-98% (structural — cover page layout). 34 outliers >100% nullified. See R7 for 13D parser fix. |
| R2 | Data quality audit — 13D/G name matching to 13F parents | Done | Item 6: 12.3% match (591 filers). 87.7% unmatched expected: ~1,976 individuals, ~639 funds/trusts, ~32 law firms, ~5 agents. See R8 for entity linking. |
| R3 | 13D/G data cleanup — dedup, amendment reconciliation, stale filing removal | Done | Item 6: 8,227 duplicates removed (60,135→51,908). ROW_NUMBER dedup keeps latest per filer/ticker. |
| R7 | 13D/G full data quality pass — shares + pct | Done | Multi-pass: (1) `reparse_13d.py` 7,271 13D filings re-parsed, pct 96%→6.3%. (2) `reparse_all_nulls.py` 3,330 remaining filings (13G+13D) with improved patterns for compact/colon formats. (3) 2,333 suspect shares QC'd and re-parsed. (4) 1,117 exit filings (pct=0%) marked shares=0. (5) 168 shares computed from pct×outstanding. (6) 159 shares cross-validated from 13F holdings. Full rescan of 928 suspect rows: 908 shares + 55 pct corrected. 3 duplicate pairs resolved. 5 manual pct fixes from filing text. Final: 51,905 rows, pct_null=0, shares_null=1, duplicates=0, range errors=0. DATA QUALITY: CLEAN. All 3 parser scripts synced (12 pct + 8 shares patterns, QC gates, enhanced clean_text). |
| R7b | 13D amendment backfill | Done | Same-filer+ticker backfill from most recent non-null filing. 13D: 215 pct + 67 shares backfilled (96%→3.4% pct null, 16.5%→2.8% shares null). 13G: +214 pct + 322 shares. All types now 2-3.5% null. |
| R9 | Foreign filer format parser (HM Treasury) | Done | 37 HM Treasury/NWG TR-1 filings parsed — pct_owned extracted from "Resulting situation" field. All 37 pct fixed. Shares still null (TR-1 reports disposals not holdings; would need pct × shares_outstanding). |
| R10 | Residual filing agent name resolution | Done | 5,128 of 5,130 agent/law-firm filer names resolved to actual beneficial owners via .hdr.sgml headers. Edgarfilings Ltd (995), Adviser Compliance (870), Advisor Consultant (650), Olshan (629), Seward & Kissel (476), etc. 2 Foley & Lardner rows unresolved. |
| R8 | 13D/G filer → entity linking for non-13F filers | Medium | 4,223 unique filers in beneficial_ownership_current have no parent_bridge entry: ~1,976 individuals, ~639 funds/trusts, ~32 law firms. Phase 4 entity_id FK will cover the 591 institutional matches; the rest need entity_ids as individuals/small filers. Not blocking Phase 4 but improves activist tab. |
| R4 | Schema design — merge 13D/G into Register view | Medium | Decide: separate rows with badge, or enrich existing parent rows with 13D/G reported %. Handle conflicts (13F computed % vs 13D/G self-reported %). Priority: 13D/G % is more authoritative for 5%+ holders |
| R5 | Intent tracking in Register | Medium | Surface 13D vs 13G filing type (activist intent vs passive). Show intent changes (13G→13D = going activist). Link to `prior_intent` column |
| R6 | Threshold crossing alerts | Low | Flag when 13D/G shows new 5%+ holder or existing holder drops below 5%. Timeline of crossings per ticker |

---

## PIPELINE 10 — Monthly N-PORT Update Flow

| # | Item | Priority | Notes |
|---|------|----------|-------|
| M1 | Design monthly update cadence | High | N-PORT filings arrive monthly (60-day lag). Define schedule: which months to fetch, how to detect new filings, incremental vs full refresh. Current fetch_nport.py supports `is_already_loaded()` skip logic |
| M2 | Monthly → quarterly rollup logic | High | N-PORT has monthly snapshots; 13F is quarterly. Define how monthly fund_holdings roll up to quarter-end positions for comparison with 13F parents. Options: use quarter-end month only, or average across 3 months, or MAX (current approach). Document tradeoffs |
| M3 | Fund → parent rollup reconciliation | High | Ensure N-PORT fund positions aggregate correctly to 13F parent totals. Handle: sub-adviser exclusions (Geode/Fidelity), share class dedup (series_id grouping), cross-filing families (Capital Group, American Funds). Quantify gap between N-PORT rollup and 13F reported value per parent |
| M4 | Incremental fund_holdings update script | Medium | Fetch only new filings since last run. Use EDGAR XBRL index or filing date filter. Append to fund_holdings without dropping existing data. Update fund_universe metadata (total_net_assets) from latest filing |
| M5 | Stale fund detection & cleanup | Medium | Identify funds that stopped filing (merged, liquidated, renamed). Flag in fund_universe. Exclude from active cohort analysis. Cross-reference with N-CEN adviser map for fund status changes |
| M6 | Monthly position change tracking | Low | Track month-over-month position changes within a quarter at fund level. Surface intra-quarter trading patterns (e.g., fund bought in month 1, sold in month 3 of same quarter) |

---

## UI/UX Improvements

| # | Item | Priority | Notes |
|---|------|----------|-------|
| U1 | N-PORT coverage disclaimer tooltip | Done | Hover tooltip on "N-PORT Coverage" label in summary card. Dotted underline indicates hoverable. Explains mutual fund + ETF coverage and deduplication |
| U2 | Row numbers on all tables | Done | # column added to all 3 table renderers (renderHierarchicalTable, buildSimpleTable, _flowTable) + Excel export. Parent rows only numbered in hierarchical tables. Children get blank |
| U3 | Top 25 with tier separators and subtotals | Done | Faint border-bottom at rows 10, 15, 20. Bold subtotal rows at 10 and 25 (sums numeric columns). Applied to buildSimpleTable and renderHierarchicalTable. _flowTable gets tier breaks |
| U4 | Nested fund children — limit 5 largest under parent | Done | get_nport_children default limit=5 already in place. Cross-Ownership (query8) doesn't use N-PORT children — pure 13F cross-holder. Consistent across Register, Conviction, Ownership Trend |
| U5 | Institution column width — expand to min 280px | Done | Single CSS rule covers all tabs via `th:nth-child(2)` and `.col-text-overflow`. Fixes truncation on parent names and N-PORT fund series children |
| U5b | Fund Portfolio tab — charts + clickable drill-down | Medium | Add Recharts visualizations to the Fund Portfolio tab: (1) sector allocation pie/donut chart from the portfolio positions, (2) top-10 holdings horizontal bar chart, (3) portfolio concentration curve. Make table rows clickable — clicking a ticker in the portfolio opens that company's Register tab (cross-tab navigation via useAppStore.setTicker + setActiveTab). Adds analytical depth beyond the current flat table. |
| U5d | Entity Graph — dynamic fund count + ticker-relevant fund highlighting | Medium | Currently hardcoded `top_n_funds=20` in entity_graph modal fetch. Two improvements: (1) increase to 50+ or auto-size based on modal viewport, add "Show all" button via `/api/entity_children?top_n=0`; (2) when a ticker is set in Company mode, highlight or filter funds that actually hold the current ticker — requires new query joining `fund_holdings_v2` with the entity's fund series by `series_id`. Shows "of Vanguard's 115 funds, which ones hold EQT?" — directly relevant to the holder analysis context. |
| U5c | Peer group management — update existing groups + create new | Medium | Current `peer_groups` table has 5 static groups (Appalachian E&P, Large Cap Energy, Media & Streaming, Midstream, Permian E&P) loaded manually. Need: (1) admin UI to create/edit/delete peer groups — form with group name + ticker multi-select, wired to new `/api/admin/peer_groups` POST/PUT/DELETE endpoints on admin_bp (INF12 token-gated); (2) update existing groups with current sector compositions (PXD acquired by EQT Oct 2023 — remove from Permian, MMP merged into ONEOK — remove from Midstream); (3) add new groups for sectors the user covers (e.g., "Haynesville Gas" CNX/AR/SWN/RRC, "Marcellus Midstream" AM/ETRN/EQM); (4) option for user-created custom groups saved to localStorage (no admin required) for ad-hoc comparisons on the Cross-Ownership tab. |
| U6 | ETF/index fund passive classification | Medium | Fund series with "Index", "ETF", "S&P", "Russell", "MSCI" in name incorrectly showing as active. Fix classification in fund_universe. Affects Register, Conviction, Crowding tabs |
| U7 | Flow Analysis — fund-level churn toggle | Medium | Add "By Institution / By Fund" toggle above Row 2 churn charts. Institution = current 13F manager-level churn (default). Fund = N-PORT fund series level churn computed live from fund_holdings. Requires: QoQ fund_holdings comparison query (same quarter pairs as institutional), active-only filter for fund names, fund-level entry/exit counting. Toggle applies to both churn charts simultaneously. Deferred because precomputed `ticker_flow_stats` only has institutional data — fund-level needs live computation or a new precomputed table |
| U8 | Name casing cleanup — Title Case pass | Medium | Many investor and fund names still display in ALL CAPS (e.g., "JPMORGAN ASSET MANAGEMENT" instead of "JPMorgan Asset Management"). N12 (normalize_names.py) fixed 27%→0% ALL CAPS in bulk, but new data loads and some edge cases reintroduce uppercase names. Need: re-run normalize_names.py on latest data, add Title Case normalization to the load pipeline so new data arrives clean, handle acronyms (ETF, LLC, LP, SPDR, iShares) and known canonical forms (BlackRock not Blackrock, JPMorgan not Jpmorgan). Affects all tabs — Register, Conviction, Flow, Cohort, Momentum |
| U10 | Extend FINRA short volume lookback to 90+ days | Medium | Current fetch_finra_short.py only pulls ~29 days. Re-run with wider date range to support 90-day trend chart on Short Interest tab. Consider fetching 6 months for longer-term context |
| U11 | Source true short interest (shares shorted as % of float) | Low | FINRA daily volume is a poor proxy for actual short interest. True SI is reported bi-monthly by NYSE/NASDAQ. Options: scrape exchange reports, use commercial API (S3 Partners, Ortex), or derive from FINRA RegSHO threshold list. Would enable days-to-cover calculation and proper squeeze scoring |
| U12 | Short analysis cross-ref matching improvement | Medium | Long/short cross-reference on Short Interest tab uses naive family name matching. Many misses (Hartford funds → Hartford Funds parent not found). Need: use match_nport_family patterns, fall back to fuzzy matching, handle sub-adviser relationships (same patterns as Register tab fund matching) |
| N22 | Validate benchmark_weights against external source | Medium | We derive US market sector weights from Vanguard Total Stock Market Index Fund (S000002848) N-PORT holdings, per quarter. Need periodic validation against external sources to confirm accuracy. Options: (1) Fetch iShares ITOT or Schwab SCHB sector weights and cross-check, (2) Scrape S&P 500 sector weights from S&P factsheet and compare (will differ since SPX vs total market), (3) Pull sector ETF AUM (XLK/XLF/XLE/etc.) as an alternate benchmark, (4) Add a validation report to the quarterly pipeline showing weight deltas between our derived values and external sources. Flag any sector with >1pp deviation for review. Current Q4 2025 weights: TEC 33.5%, FIN 13.3%, CND 10.9%, COM 9.5%, HCR 9.5%, IND 8.8%, CNS 4.6%, ENE 3.1%, REA 2.6%, UTL 2.3%, MAT 1.8% — need external confirmation these are reasonable |
| N21 | Investor type classification overhaul (audit + rebuild) | **In Progress** | **Key finding:** ALL investor type classifications in the system are DERIVED BY US, not from SEC. **Sub-item DONE (2026-04-05): fund_universe.is_actively_managed backfilled.** The field was hardcoded to `true` for every fund by `fetch_nport.py` classify_fund(). Created `scripts/fix_fund_classification.py` to backfill via name-pattern keyword matching (INDEX, ETF, SPDR, iShares, S&P 500, Russell, MSCI, etc.). Ran in staging, verified spot-checks, merged to production. Result: 6,671 funds → 5,636 active / 1,035 passive (was 6,671 / 0). Also fixed fetch_nport.py so new data arrives correct — classify_fund now returns `not is_passive_name` for is_actively_managed. **Still TODO:** (a) holdings.manager_type — 40% NULL rate from PARENT_SEEDS gap, (b) managers.strategy_type, (c) adv_managers is_activist, (d) classification source/confidence audit column, (e) PARENT_SEEDS expansion 50 → 250, (f) spot-validate 100 largest holders across 10 benchmark tickers |
| N23 | Document classification methodology in user docs | Done | Updated OneDrive platform documentation (20260404): added "Data Classification Methodology" section explaining all types are derived by us, not SEC. Documented 5 classification sources (entity_classification_history, ADV strategy, parent_seeds, keywords, defaults), entity_type vs manager_type distinction, confidence levels, known gaps. Added caveats to Type column description. Updated database architecture for Phase 4 v2 tables and entity MDM. Created `docs/CLASSIFICATION_METHODOLOGY.md` developer reference. |
| N20 | Fill sector/industry data gaps (Unknown reduction) | Done | Reduced from 30.0% → 1.4% missing across 3 phases: (1) ETF detection via securities.security_type_inferred + curated known-ETF list of 249 tickers — caught 956 ETFs, (2) yfinance refetch for 848 non-ETF missing — recovered 328, (3) Expanded ETF pattern matching via issuer names + preferred/warrant parent inheritance + derivative tagging + targeted yfinance pass on residual — recovered 434 more. Final: 4,563 real GICS sectors (75.8%), 1,276 ETF-tagged (21.2%), 96 Derivative-tagged (1.6%), 86 still missing (1.4%). Remaining are edge cases: delisted tickers, SPAC units, fund wrapper codes (XEVVX, XBOEX), preferred shares without parents. REIT handling added via industry keyword match. Exported to 20260405_missing_sectors_final.csv for manual review |
| N19 | Full investor portfolio concentration review | Medium | Dedicated tab for deep-dive into a specific investor's full portfolio. Shows: complete sector breakdown as horizontal bar chart, top holdings ranked, HHI concentration index, top 10 vs rest ratio, sector peers within each sector (e.g., their full Energy book), overweight/underweight vs benchmark (S&P 500 sector weights), quarterly sector rotation trend (% changes across quarters). Triggered by clicking an investor name from Register, Conviction, or Fund Portfolio tabs. Separates the per-holder deep dive from the cross-holder Conviction tab which only shows summary columns |
| U9 | Summary dashboard tab | Medium | New tab (or landing view) with visual charts summarizing key analysis for a ticker at a glance. Candidates: (1) Ownership composition donut — active/passive/hedge/other by value, (2) QoQ share change waterfall — retained + new - exits = current, (3) Top 10 holders horizontal bar ranked by % float, (4) Active share retention trend sparkline (last 3 quarters), (5) Flow intensity mini bar chart (same as Flow tab but inline), (6) Short interest vs institutional ownership scatter or dual-axis chart, (7) Holder count trend line across quarters. Goal: one-page executive view before drilling into individual tabs. Should load fast — pull from precomputed summary tables where possible. Consider making this the default landing view when a ticker is entered |

---

## SEQUENCE — NEXT STEPS

1. ~~N-PORT index/ETF download + merge~~ Done
2. ~~Data quality fixes (market_value, manager_type, flows, pct_of_float)~~ Done
3. ~~FINRA short interest refresh + enrich_tickers~~ Done
4. ~~Retry `fetch_market.py --staging` when Yahoo rate limit resets~~ Done (2026-04-05) — root cause was yfinance 1.2.0 ignoring passed sessions; bypassed via direct curl_cffi client (H8). SEC XBRL added for authoritative shares outstanding (H9).
5. ~~N12 — Investor name standardization~~ Done
6. ~~N2 — Short squeeze UI tab~~ Done
7. ~~N3 — Short vs long UI integration~~ Done
8. ~~N13 — N-PORT series-level deduplication~~ Done
9. ~~N14 — Geode/Fidelity sub-adviser exclusion~~ Done
10. ~~U1 — N-PORT coverage disclaimer tooltip~~ Done
11. ~~P4 — iShares Trust N-PORT~~ Done
**PRE-PHASE 4 REQUIRED:**
12. ~~N15 — Fidelity international sub-adviser deduplication~~ ✅ Done (Item 5)
13. ~~R1/R2/R3 — 13D/G data quality audit + cleanup~~ ✅ Done (Item 6 + R7/R7b/R9/R10). pct_null 96%→0.02%, shares_null 16.5%→0.02%. 20K+ agent names resolved. Parser hardened.
14. ~~Item 43 — app.py lint debt fix~~ ✅ Done (Item 7). flake8 0, bandit B608 0.
15. ~~N21 TODOs a/b/c — investor type classification~~ ✅ Done (Item 8). Manager-level: NULL 787→0, 14 categories, 8,639 managers, $67.3T. ALL categories reviewed 1-by-1: passive 45 ($22.2T), mixed 1,657 ($16.1T), active 4,126 ($14.2T), WM 500 ($4.5T), quant 68 ($4.3T), HF 1,342 ($2.4T), pension 139 ($2.0T), strategic 543 ($0.6T), SWF 17 ($0.3T), endowment 61 ($0.3T), PE 76 ($0.2T), activist 31 ($0.1T), VC 32, multi 2. 177 LOW>$10B manually fixed. Fund-level: 5,717 series via S&P500 overlap + 8-index correlation.

**POST-PHASE 4:**
16. M1/M2/M3 — Monthly N-PORT update flow design — design decisions, not migration blockers
17. R4/R5/R6 — 13D/G Register integration — UI integration, pending Phase 4 cutover
18. M4/M5/M6 — Incremental N-PORT updates — operational cadence, implement after cutover
19. H16 — Pre-computed entity×ticker flow cache — performance enhancement
20. U6, U8, N17, N18, N19, N22, N23 — UI/UX enhancements
21. Refresh readonly snapshot
22. Items N4-N10 as capacity allows
